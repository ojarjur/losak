#ifdef BARE_HARDWARE
#include "multiboot.h"
#endif
#include "io.h"
#include "mem.h"
#include "eval.h"
pointer arguments = NIL;
#ifdef BARE_HARDWARE
pointer get_cmd(unsigned long flags, char* command) {
  pointer r = NIL;
  int i = 0;
  if (getFlag(flags,2) != 0) {
    while ((command > 0) && (command[i] != '\0')) {
      i++;
    }
    while (i > 0) {
      i--;
      r = cons(new_number(command[i]), r);
    }
  }
  return r;
}
#endif
inline void pop_function() {
  decrement_count(env);
  temp = car(call_stack);
  if (is_function(temp)) {
    env = environment(temp);
    increment_count(env);
    return_location = address(temp);
  } else {
    error(ERR_INTERNAL);
    env = NIL;
    return_location = (&end_function);
  }
  increment_count(temp = cdr(call_stack));
  decrement_count(call_stack);
  call_stack = temp;
}
void push_env() {
  stack = cons(env, stack);
}
void pop_env() {
  decrement_count(env);
  increment_count(env = car(stack));
  increment_count(temp = cdr(stack));
  decrement_count(stack);
  stack = temp;
}
void push_args() {
  stack = cons(args, stack);
}
void pop_args() {
  decrement_count(args);
  increment_count(args = car(stack));
  increment_count(temp = cdr(stack));
  decrement_count(stack);
  stack = temp;
}
void function_0() {
  if (is_function(val)) {
    increment_count(env=environment(val));
    env = cons(cons(free_memory_size(),
                   cons(arguments,NIL)),
               env);
    return_location = address(val);
    decrement_count(val);
  } else {
    error(ERR_NO_OS);
    env = NIL;
    return_location = (&end_function);
  }
}
void function_1() {
  pop_function();
}
void function_2() {
  pop_function();
}
void function_3() {
  pop_function();
}
void function_4() {
  pop_function();
}
void function_5() {
  pop_function();
}
void function_6() {
  pop_function();
}
void function_7() {
  pop_function();
}
void function_8() {
  pop_function();
}
void function_9() {
  pop_function();
}
void function_10() {
  pop_function();
}
void function_11() {
  pop_function();
}
void function_12() {
  pop_function();
}
void function_13() {
  pop_function();
}
void function_14() {
  pop_function();
}
void function_15() {
  pop_function();
}
void function_16() {
  pop_function();
}
void function_17() {
  pop_function();
}
void function_18() {
  pop_function();
}
void function_19() {
  pop_function();
}
void function_20() {
  pop_function();
}
void function_21() {
  pop_function();
}
void function_22() {
  pop_function();
}
void function_23() {
  pop_function();
}
void function_24() {
  pop_function();
}
void function_25() {
  pop_function();
}
void function_26() {
  pop_function();
}
void function_27() {
  pop_function();
}
void function_28() {
  pop_function();
}
void function_29() {
  pop_function();
}
void function_30() {
  pop_function();
}
void function_31() {
  pop_function();
}
void function_32() {
  pop_function();
}
void function_33() {
  pop_function();
}
void function_34() {
  pop_function();
}
void function_35() {
  pop_function();
}
void function_36() {
  pop_function();
}
void function_37() {
  pop_function();
}
void function_38() {
  pop_function();
}
void function_39() {
  pop_function();
}
void function_40() {
  pop_function();
}
void function_41() {
  pop_function();
}
void function_42() {
  pop_function();
}
void function_43() {
  pop_function();
}
void function_44() {
  pop_function();
}
void function_45() {
  pop_function();
}
void function_46() {
  pop_function();
}
void function_47() {
  pop_function();
}
void function_48() {
  pop_function();
}
void function_49() {
  pop_function();
}
void function_50() {
  pop_function();
}
void function_51() {
  pop_function();
}
void function_52() {
  pop_function();
}
void function_53() {
  pop_function();
}
void function_54() {
  pop_function();
}
void function_55() {
  pop_function();
}
void function_56() {
  pop_function();
}
void function_57() {
  pop_function();
}
void function_58() {
  pop_function();
}
void function_59() {
  pop_function();
}
void function_60() {
  pop_function();
}
void function_61() {
  pop_function();
}
void function_62() {
  pop_function();
}
void function_63() {
  pop_function();
}
void function_64() {
  pop_function();
}
void function_65() {
  pop_function();
}
void function_66() {
  pop_function();
}
void function_67() {
  pop_function();
}
void function_68() {
  pop_function();
}
void function_69() {
  pop_function();
}
void function_70() {
  pop_function();
}
void function_71() {
  pop_function();
}
void function_72() {
  pop_function();
}
void function_73() {
  pop_function();
}
void function_74() {
  pop_function();
}
void function_75() {
  pop_function();
}
void function_76() {
  pop_function();
}
void function_77() {
  pop_function();
}
void function_78() {
  pop_function();
}
void function_79() {
  pop_function();
}
void function_80() {
  pop_function();
}
void function_81() {
  pop_function();
}
void function_82() {
  pop_function();
}
void function_83() {
  pop_function();
}
void function_84() {
  pop_function();
}
void function_85() {
  pop_function();
}
void function_86() {
  pop_function();
}
void function_87() {
  pop_function();
}
void function_88() {
  pop_function();
}
void function_89() {
  pop_function();
}
void function_90() {
  pop_function();
}
void function_91() {
  pop_function();
}
void function_92() {
  pop_function();
}
void function_93() {
  pop_function();
}
void function_94() {
  pop_function();
}
void function_95() {
  pop_function();
}
void function_96() {
  pop_function();
}
void function_97() {
  pop_function();
}
void function_98() {
  pop_function();
}
void function_99() {
  pop_function();
}
void function_100() {
  pop_function();
}
void function_101() {
  pop_function();
}
void function_102() {
  pop_function();
}
void function_103() {
  pop_function();
}
void function_104() {
  pop_function();
}
void function_105() {
  pop_function();
}
void function_106() {
  pop_function();
}
void function_107() {
  pop_function();
}
void function_108() {
  pop_function();
}
void function_109() {
  pop_function();
}
void function_110() {
  pop_function();
}
void function_111() {
  pop_function();
}
void function_112() {
  pop_function();
}
void function_113() {
  pop_function();
}
void function_114() {
  pop_function();
}
void function_115() {
  pop_function();
}
void function_116() {
  pop_function();
}
void function_117() {
  pop_function();
}
void function_118() {
  pop_function();
}
void function_119() {
  pop_function();
}
void function_120() {
  pop_function();
}
void function_121() {
  pop_function();
}
void function_122() {
  pop_function();
}
void function_123() {
  pop_function();
}
void function_124() {
  pop_function();
}
void function_125() {
  pop_function();
}
void function_126() {
  pop_function();
}
void function_127() {
  pop_function();
}
void function_128() {
  pop_function();
}
void function_129() {
  increment_count(val = car(car(env)));
  pop_function();
}
void function_130() {
  pop_function();
}
void function_131() {
  args = cons(val, args);
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_130);
  }
}
void function_132() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_131), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_131);
  }
}
void function_133() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_132), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_129), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_128);
  }
}
void function_134() {
  pop_function();
}
void function_135() {
  args = cons(val, args);
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_134);
  }
}
void function_136() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = new_number(75);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_135), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_135);
  }
}
void function_137() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_136), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_133), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_127);
  }
}
void function_138() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_137), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_126);
  }
}
void function_139() {
  pop_function();
}
void function_140() {
  pop_function();
}
void function_141() {
  pop_function();
}
void function_142() {
  pop_function();
}
void function_143() {
  pop_function();
}
void function_144() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = cdr(car(car(cdr(cdr(env))))));
  args = cons(val, args);
  increment_count(val = cdr(car(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(car(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(cdr(env)))))))));
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = new_number(8);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_143);
    }
  } else {
    val = NIL;
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_143);
    }
  }
}
void function_145() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_144), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_142);
  }
}
void function_146() {
  pop_function();
}
void function_147() {
  pop_function();
}
void function_148() {
  pop_function();
}
void function_149() {
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_148);
  }
}
void function_150() {
  pop_function();
}
void function_151() {
  pop_function();
}
void function_152() {
  pop_function();
}
void function_153() {
  pop_function();
}
void function_154() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = cdr(car(car(cdr(cdr(cdr(cdr(env))))))));
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_153);
  }
}
void function_155() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_154), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_152);
  }
}
void function_156() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_155), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_151);
  }
}
void function_157() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(car(env)))));
  args = cons(val, args);
  increment_count(val = car(car(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_156), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_156);
  }
}
void function_158() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_157), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_150);
  }
}
void function_159() {
  pop_function();
}
void function_160() {
  pop_function();
}
void function_161() {
  pop_function();
}
void function_162() {
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_161);
  }
}
void function_163() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(env)))));
  args = cons(val, args);
  increment_count(val = cdr(car(car(cdr(cdr(cdr(env)))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(cdr(cdr(env))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_162), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_162);
  }
}
void function_164() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(car(cdr(cdr(env))))));
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_163), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_160);
  }
}
void function_165() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = cdr(car(car(cdr(env)))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(13);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_149), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_149);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    val = -228;
    args = cons(val, args);
    increment_count(val = car(car(car(cdr(env)))));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_158), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_158);
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      val = new_number(40);
      args = cons(val, args);
      increment_count(val = car(car(car(cdr(env)))));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        val = new_number(1);
        args = cons(val, args);
        increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_number(car(args))) &&
            (is_number(car(cdr(args))))) {
          val = new_number(value(car(args)) + value(car(cdr(args)))); 
        } else { val = NIL; }
        pop_args();
        args = cons(val, args);
        increment_count(env);
        val = new_function((&function_164), env);
        if (is_function(val)) {
          decrement_count(env);
          return_location = address(val);
          increment_count(env = environment(val));
          decrement_count(val);
          increment_count(args);
          env = cons(args, env);
          pop_args();
        } else {
          if (is_primitive(val)) {
            temp = val;
            val = eval_primitive(val, args);
            decrement_count(temp);
          } else {
            decrement_count(val);
            val = NIL;
          }
          pop_args();
          return_location = (&function_159);
        }
      } else {
        push_args();
        val = NIL;
        args = val;
        val = new_number(41);
        args = cons(val, args);
        increment_count(val = car(car(car(cdr(env)))));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          val = new_number(1);
          args = cons(val, args);
          if (is_number(car(args))) {
            if ((length(args) == 2) &&
                is_number(car(cdr(args)))) {
              val = new_number(value(car(args)) - 
                               value(car(cdr(args))));
            } else if (length(args) == 1) {
              val = new_number(-value(car(args)));
            } else { val = NIL; }
          } else { val = NIL; }
          pop_args();
          args = cons(val, args);
          increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_number(car(args))) &&
              (is_number(car(cdr(args))))) {
            val = new_number(value(car(args)) + value(car(cdr(args)))); 
          } else { val = NIL; }
          pop_args();
          args = cons(val, args);
          increment_count(env);
          val = new_function((&function_164), env);
          if (is_function(val)) {
            decrement_count(env);
            return_location = address(val);
            increment_count(env = environment(val));
            decrement_count(val);
            increment_count(args);
            env = cons(args, env);
            pop_args();
          } else {
            if (is_primitive(val)) {
              temp = val;
              val = eval_primitive(val, args);
              decrement_count(temp);
            } else {
              decrement_count(val);
              val = NIL;
            }
            pop_args();
            return_location = (&function_159);
          }
        } else {
          val = new_number(0);
          args = cons(val, args);
          increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_number(car(args))) &&
              (is_number(car(cdr(args))))) {
            val = new_number(value(car(args)) + value(car(cdr(args)))); 
          } else { val = NIL; }
          pop_args();
          args = cons(val, args);
          increment_count(env);
          val = new_function((&function_164), env);
          if (is_function(val)) {
            decrement_count(env);
            return_location = address(val);
            increment_count(env = environment(val));
            decrement_count(val);
            increment_count(args);
            env = cons(args, env);
            pop_args();
          } else {
            if (is_primitive(val)) {
              temp = val;
              val = eval_primitive(val, args);
              decrement_count(temp);
            } else {
              decrement_count(val);
              val = NIL;
            }
            pop_args();
            return_location = (&function_159);
          }
        }
      }
    }
  }
}
void function_166() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = cdr(car(car(cdr(env)))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(env))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_147);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    val = new_number(0);
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_number(car(args))) &&
        (is_number(car(cdr(args))))) {
      if (value(car(args)) > value(car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    val = -228;
    args = cons(val, args);
    increment_count(val = car(car(car(cdr(env)))));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_165), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_165);
    }
  }
}
void function_167() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = cdr(car(car(cdr(env)))));
    args = cons(val, args);
    val = NIL;
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(env))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_146);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    val = new_number(58);
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = car(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    val = -228;
    args = cons(val, args);
    increment_count(val = car(car(car(cdr(env)))));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_166), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_166);
    }
  }
}
void function_168() {
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(car(cdr(env)))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = cdr(car(car(cdr(env)))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
    args = cons(val, args);
    increment_count(val = car(cdr(car(cdr(cdr(cdr(env)))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(env))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_141);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    val = -226;
    args = cons(val, args);
    increment_count(val = car(car(car(cdr(env)))));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(cdr(env)))))))));
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_145), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_145);
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      val = NIL;
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      val = -228;
      args = cons(val, args);
      increment_count(val = car(car(car(cdr(env)))));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_167), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_167);
      }
    }
  }
}
void function_169() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_168), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_140);
  }
}
void function_170() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(cdr(cdr(cdr(car(cdr(cdr(env))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_169), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_169);
  }
}
void function_171() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_170), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_139);
  }
}
void function_172() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(cdr(cdr(car(cdr(env))))))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_171), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_171);
  }
}
void function_173() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_174() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(env);
    val = new_function((&function_172), env);
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(cdr(cdr(cdr(car(env))))))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(cdr(car(env)))))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(car(env))))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = cdr(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_173), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_173);
    }
  }
}
void function_175() {
  increment_count(env);
  val = new_function((&function_174), env);
  pop_function();
}
void function_176() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_175), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_138), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_138);
  }
}
void function_177() {
  pop_function();
}
void function_178() {
  pop_function();
}
void function_179() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(cdr(cdr(car(env)))))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(car(env))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(env))))));
    args = cons(val, args);
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_177);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(cdr(cdr(car(env)))))));
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(cdr(car(env))))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
      args = cons(val, args);
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(env))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_178);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_180() {
  if (val != NIL) {
    decrement_count(val);
    val = new_number(1);
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(58);
    args = val;
    push_args();
    val = new_number(108);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_179), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_179);
    }
  }
}
void function_181() {
  if (val != NIL) {
    decrement_count(val);
    val = new_number(0);
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(58);
    args = val;
    push_args();
    val = new_number(114);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_180), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_180);
    }
  }
}
void function_182() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(113);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_181), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_181);
  }
}
void function_183() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_182), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_176), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_125);
  }
}
void function_184() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_183), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_124);
  }
}
void function_185() {
  pop_function();
}
void function_186() {
  pop_function();
}
void function_187() {
  pop_function();
}
void function_188() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(env))))))));
  args = cons(val, args);
  val = new_number(0);
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_187);
  }
}
void function_189() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_188), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_186);
  }
}
void function_190() {
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(120);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = cdr(car(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_189), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_189);
  }
}
void function_191() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(62);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_190), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_190);
  }
}
void function_192() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_191), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_185);
  }
}
void function_193() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_194() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(cdr(cdr(car(env)))))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_192), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_192);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(cdr(cdr(car(env)))))));
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(cdr(car(env))))));
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = cdr(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(env))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_193), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_193);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_195() {
  increment_count(env);
  val = new_function((&function_194), env);
  pop_function();
}
void function_196() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_195), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_184), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_184);
  }
}
void function_197() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_196), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_123);
  }
}
void function_198() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(34);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(34);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_197), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_197);
  }
}
void function_199() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_198), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_122);
  }
}
void function_200() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(51);
  args = val;
  push_args();
  val = new_number(51);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(66);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(77);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(50);
  args = val;
  push_args();
  val = new_number(49);
  args = val;
  push_args();
  val = new_number(49);
  args = val;
  push_args();
  val = new_number(49);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(49);
  args = val;
  push_args();
  val = new_number(51);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(55);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(70);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(70);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(73);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(53);
  args = val;
  push_args();
  val = new_number(57);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(89);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(118);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(118);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(71);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(71);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(73);
  args = val;
  push_args();
  val = new_number(67);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(69);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(71);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(71);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(77);
  args = val;
  push_args();
  val = new_number(69);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(67);
  args = val;
  push_args();
  val = new_number(72);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(66);
  args = val;
  push_args();
  val = new_number(73);
  args = val;
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(73);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(89);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(70);
  args = val;
  push_args();
  val = new_number(73);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(69);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(70);
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(87);
  args = val;
  push_args();
  val = new_number(73);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(72);
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(89);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(87);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(65);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(89);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(118);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(70);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(71);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(85);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(71);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(70);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(47);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(118);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(50);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(84);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(89);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(67);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(40);
  args = val;
  push_args();
  val = new_number(67);
  args = val;
  push_args();
  val = new_number(41);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(50);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(52);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(50);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(56);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(74);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(106);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_199), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_199);
  }
}
void function_201() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(39);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(76);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(103);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(83);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(50);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(56);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(54);
  args = val;
  push_args();
  val = new_number(48);
  args = val;
  push_args();
  val = new_number(50);
  args = val;
  push_args();
  val = new_number(46);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_200), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_121);
  }
}
void function_202() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_201), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_120);
  }
}
void function_203() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(9);
  args = val;
  push_args();
  val = new_number(60);
  args = val;
  push_args();
  val = new_number(69);
  args = val;
  push_args();
  val = new_number(88);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(62);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(118);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(120);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(60);
  args = val;
  push_args();
  val = new_number(69);
  args = val;
  push_args();
  val = new_number(88);
  args = val;
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(82);
  args = val;
  push_args();
  val = new_number(62);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(9);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(9);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(113);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(113);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(40);
  args = val;
  push_args();
  val = new_number(78);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(44);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(119);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(98);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(41);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(9);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(99);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(9);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(45);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(105);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(104);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(108);
  args = val;
  push_args();
  val = new_number(112);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  push_args();
  val = new_number(67);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(110);
  args = val;
  push_args();
  val = new_number(100);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_202), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_202);
  }
}
void function_204() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_203), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_119);
  }
}
void function_205() {
  pop_function();
}
void function_206() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = cdr(car(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_207() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_206), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_205);
  }
}
void function_208() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = cdr(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    if (length(args) == 1) {
      if (is_atom(car(args))) {
        increment_count(val = true); 
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      val = new_number(40);
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        push_args();
        val = NIL;
        args = val;
        val = new_number(1);
        args = cons(val, args);
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        if (is_number(car(args))) {
          if ((length(args) == 2) &&
              is_number(car(cdr(args)))) {
            val = new_number(value(car(args)) - 
                             value(car(cdr(args))));
          } else if (length(args) == 1) {
            val = new_number(-value(car(args)));
          } else { val = NIL; }
        } else { val = NIL; }
        pop_args();
        args = cons(val, args);
        val = NIL;
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      } else {
        push_args();
        val = NIL;
        args = val;
        val = new_number(41);
        args = cons(val, args);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if (length(args) == 1) {
          if (! is_atom(car(args))) {
          increment_count(val = car(car(args)));
          } else { val = NIL; }
        } else { val = NIL; }
        pop_args();
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          push_args();
          val = NIL;
          args = val;
          val = new_number(1);
          args = cons(val, args);
          increment_count(val = car(cdr(car(env))));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_number(car(args))) &&
              (is_number(car(cdr(args))))) {
            val = new_number(value(car(args)) + value(car(cdr(args)))); 
          } else { val = NIL; }
          pop_args();
          args = cons(val, args);
          val = NIL;
          args = cons(val, args);
          if (length(args) == 2) {
            increment_count(car(args));
            increment_count(car(cdr(args)));
            val = cons(car(args), car(cdr(args)));
          } else { val = NIL; }
          pop_args();
          pop_function();
        } else {
          push_args();
          val = NIL;
          args = val;
          val = new_number(13);
          args = cons(val, args);
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if (length(args) == 1) {
            if (! is_atom(car(args))) {
            increment_count(val = car(car(args)));
            } else { val = NIL; }
          } else { val = NIL; }
          pop_args();
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(cdr(car(env))));
            args = cons(val, args);
            increment_count(val = car(car(env)));
            args = cons(val, args);
            if (length(args) == 2) {
              increment_count(car(args));
              increment_count(car(cdr(args)));
              val = cons(car(args), car(cdr(args)));
            } else { val = NIL; }
            pop_args();
            pop_function();
          } else {
            push_args();
            val = NIL;
            args = val;
            val = new_number(10);
            args = cons(val, args);
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(car(env)));
            args = cons(val, args);
            if (length(args) == 1) {
              if (! is_atom(car(args))) {
              increment_count(val = car(car(args)));
              } else { val = NIL; }
            } else { val = NIL; }
            pop_args();
            args = cons(val, args);
            if ((length(args) == 2) &&
                (is_atom(car(args))) &&
                (is_atom(car(cdr(args)))) &&
                eq(car(args), car(cdr(args)))) {
              increment_count(val = true);
            } else { val = NIL; }
            pop_args();
            if (val != NIL) {
              decrement_count(val);
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(cdr(car(env))));
              args = cons(val, args);
              increment_count(val = car(car(env)));
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            } else {
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(cdr(car(env))));
              args = cons(val, args);
              val = NIL;
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            }
          }
        }
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = cdr(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(cdr(env))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_207), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_207);
      }
    }
  }
}
void function_209() {
  increment_count(env);
  val = new_function((&function_208), env);
  pop_function();
}
void function_210() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_209), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_204), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_204);
  }
}
void function_211() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_210), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_118);
  }
}
void function_212() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(8);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    val = -226;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    val = new_number(10);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      val = -228;
      args = cons(val, args);
      if (length(args) == 2) {
        increment_count(car(args));
        increment_count(car(cdr(args)));
        val = cons(car(args), car(cdr(args)));
      } else { val = NIL; }
      pop_args();
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      val = new_number(13);
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        val = -228;
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      } else {
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      }
    }
  }
}
void function_213() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_212), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_211), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_117);
  }
}
void function_214() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    val = -230;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    val = new_number(57);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      val = new_number(32);
      args = cons(val, args);
      if (length(args) == 2) {
        increment_count(car(args));
        increment_count(car(cdr(args)));
        val = cons(car(args), car(cdr(args)));
      } else { val = NIL; }
      pop_args();
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      val = new_number(58);
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        val = -231;
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      } else {
        push_args();
        val = NIL;
        args = val;
        val = new_number(59);
        args = cons(val, args);
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(cdr(car(env))));
          args = cons(val, args);
          val = -232;
          args = cons(val, args);
          if (length(args) == 2) {
            increment_count(car(args));
            increment_count(car(cdr(args)));
            val = cons(car(args), car(cdr(args)));
          } else { val = NIL; }
          pop_args();
          pop_function();
        } else {
          push_args();
          val = NIL;
          args = val;
          val = new_number(60);
          args = cons(val, args);
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(cdr(car(env))));
            args = cons(val, args);
            val = -233;
            args = cons(val, args);
            if (length(args) == 2) {
              increment_count(car(args));
              increment_count(car(cdr(args)));
              val = cons(car(args), car(cdr(args)));
            } else { val = NIL; }
            pop_args();
            pop_function();
          } else {
            push_args();
            val = NIL;
            args = val;
            val = new_number(61);
            args = cons(val, args);
            increment_count(val = car(car(env)));
            args = cons(val, args);
            if ((length(args) == 2) &&
                (is_atom(car(args))) &&
                (is_atom(car(cdr(args)))) &&
                eq(car(args), car(cdr(args)))) {
              increment_count(val = true);
            } else { val = NIL; }
            pop_args();
            if (val != NIL) {
              decrement_count(val);
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(cdr(car(env))));
              args = cons(val, args);
              val = -234;
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            } else {
              push_args();
              val = NIL;
              args = val;
              val = new_number(62);
              args = cons(val, args);
              increment_count(val = car(car(env)));
              args = cons(val, args);
              if ((length(args) == 2) &&
                  (is_atom(car(args))) &&
                  (is_atom(car(cdr(args)))) &&
                  eq(car(args), car(cdr(args)))) {
                increment_count(val = true);
              } else { val = NIL; }
              pop_args();
              if (val != NIL) {
                decrement_count(val);
                push_args();
                val = NIL;
                args = val;
                increment_count(val = car(cdr(car(env))));
                args = cons(val, args);
                val = -235;
                args = cons(val, args);
                if (length(args) == 2) {
                  increment_count(car(args));
                  increment_count(car(cdr(args)));
                  val = cons(car(args), car(cdr(args)));
                } else { val = NIL; }
                pop_args();
                pop_function();
              } else {
                push_args();
                val = NIL;
                args = val;
                val = new_number(63);
                args = cons(val, args);
                increment_count(val = car(car(env)));
                args = cons(val, args);
                if ((length(args) == 2) &&
                    (is_atom(car(args))) &&
                    (is_atom(car(cdr(args)))) &&
                    eq(car(args), car(cdr(args)))) {
                  increment_count(val = true);
                } else { val = NIL; }
                pop_args();
                if (val != NIL) {
                  decrement_count(val);
                  push_args();
                  val = NIL;
                  args = val;
                  increment_count(val = car(cdr(car(env))));
                  args = cons(val, args);
                  val = -236;
                  args = cons(val, args);
                  if (length(args) == 2) {
                    increment_count(car(args));
                    increment_count(car(cdr(args)));
                    val = cons(car(args), car(cdr(args)));
                  } else { val = NIL; }
                  pop_args();
                  pop_function();
                } else {
                  push_args();
                  val = NIL;
                  args = val;
                  val = new_number(64);
                  args = cons(val, args);
                  increment_count(val = car(car(env)));
                  args = cons(val, args);
                  if ((length(args) == 2) &&
                      (is_atom(car(args))) &&
                      (is_atom(car(cdr(args)))) &&
                      eq(car(args), car(cdr(args)))) {
                    increment_count(val = true);
                  } else { val = NIL; }
                  pop_args();
                  if (val != NIL) {
                    decrement_count(val);
                    push_args();
                    val = NIL;
                    args = val;
                    increment_count(val = car(cdr(car(env))));
                    args = cons(val, args);
                    val = -237;
                    args = cons(val, args);
                    if (length(args) == 2) {
                      increment_count(car(args));
                      increment_count(car(cdr(args)));
                      val = cons(car(args), car(cdr(args)));
                    } else { val = NIL; }
                    pop_args();
                    pop_function();
                  } else {
                    push_args();
                    val = NIL;
                    args = val;
                    val = new_number(65);
                    args = cons(val, args);
                    increment_count(val = car(car(env)));
                    args = cons(val, args);
                    if ((length(args) == 2) &&
                        (is_atom(car(args))) &&
                        (is_atom(car(cdr(args)))) &&
                        eq(car(args), car(cdr(args)))) {
                      increment_count(val = true);
                    } else { val = NIL; }
                    pop_args();
                    if (val != NIL) {
                      decrement_count(val);
                      push_args();
                      val = NIL;
                      args = val;
                      increment_count(val = car(cdr(car(env))));
                      args = cons(val, args);
                      val = -238;
                      args = cons(val, args);
                      if (length(args) == 2) {
                        increment_count(car(args));
                        increment_count(car(cdr(args)));
                        val = cons(car(args), car(cdr(args)));
                      } else { val = NIL; }
                      pop_args();
                      pop_function();
                    } else {
                      push_args();
                      val = NIL;
                      args = val;
                      val = new_number(66);
                      args = cons(val, args);
                      increment_count(val = car(car(env)));
                      args = cons(val, args);
                      if ((length(args) == 2) &&
                          (is_atom(car(args))) &&
                          (is_atom(car(cdr(args)))) &&
                          eq(car(args), car(cdr(args)))) {
                        increment_count(val = true);
                      } else { val = NIL; }
                      pop_args();
                      if (val != NIL) {
                        decrement_count(val);
                        push_args();
                        val = NIL;
                        args = val;
                        increment_count(val = car(cdr(car(env))));
                        args = cons(val, args);
                        val = -239;
                        args = cons(val, args);
                        if (length(args) == 2) {
                          increment_count(car(args));
                          increment_count(car(cdr(args)));
                          val = cons(car(args), car(cdr(args)));
                        } else { val = NIL; }
                        pop_args();
                        pop_function();
                      } else {
                        push_args();
                        val = NIL;
                        args = val;
                        val = new_number(67);
                        args = cons(val, args);
                        increment_count(val = car(car(env)));
                        args = cons(val, args);
                        if ((length(args) == 2) &&
                            (is_atom(car(args))) &&
                            (is_atom(car(cdr(args)))) &&
                            eq(car(args), car(cdr(args)))) {
                          increment_count(val = true);
                        } else { val = NIL; }
                        pop_args();
                        if (val != NIL) {
                          decrement_count(val);
                          push_args();
                          val = NIL;
                          args = val;
                          increment_count(val = car(cdr(car(env))));
                          args = cons(val, args);
                          val = -240;
                          args = cons(val, args);
                          if (length(args) == 2) {
                            increment_count(car(args));
                            increment_count(car(cdr(args)));
                            val = cons(car(args), car(cdr(args)));
                          } else { val = NIL; }
                          pop_args();
                          pop_function();
                        } else {
                          push_args();
                          val = NIL;
                          args = val;
                          val = new_number(68);
                          args = cons(val, args);
                          increment_count(val = car(car(env)));
                          args = cons(val, args);
                          if ((length(args) == 2) &&
                              (is_atom(car(args))) &&
                              (is_atom(car(cdr(args)))) &&
                              eq(car(args), car(cdr(args)))) {
                            increment_count(val = true);
                          } else { val = NIL; }
                          pop_args();
                          if (val != NIL) {
                            decrement_count(val);
                            push_args();
                            val = NIL;
                            args = val;
                            increment_count(val = car(cdr(car(env))));
                            args = cons(val, args);
                            val = -241;
                            args = cons(val, args);
                            if (length(args) == 2) {
                              increment_count(car(args));
                              increment_count(car(cdr(args)));
                              val = cons(car(args), car(cdr(args)));
                            } else { val = NIL; }
                            pop_args();
                            pop_function();
                          } else {
                            push_args();
                            val = NIL;
                            args = val;
                            val = new_number(69);
                            args = cons(val, args);
                            increment_count(val = car(car(env)));
                            args = cons(val, args);
                            if ((length(args) == 2) &&
                                (is_atom(car(args))) &&
                                (is_atom(car(cdr(args)))) &&
                                eq(car(args), car(cdr(args)))) {
                              increment_count(val = true);
                            } else { val = NIL; }
                            pop_args();
                            if (val != NIL) {
                              decrement_count(val);
                              push_args();
                              val = NIL;
                              args = val;
                              increment_count(val = car(cdr(car(env))));
                              args = cons(val, args);
                              val = -166;
                              args = cons(val, args);
                              if (length(args) == 2) {
                                increment_count(car(args));
                                increment_count(car(cdr(args)));
                                val = cons(car(args), car(cdr(args)));
                              } else { val = NIL; }
                              pop_args();
                              pop_function();
                            } else {
                              push_args();
                              val = NIL;
                              args = val;
                              val = new_number(70);
                              args = cons(val, args);
                              increment_count(val = car(car(env)));
                              args = cons(val, args);
                              if ((length(args) == 2) &&
                                  (is_atom(car(args))) &&
                                  (is_atom(car(cdr(args)))) &&
                                  eq(car(args), car(cdr(args)))) {
                                increment_count(val = true);
                              } else { val = NIL; }
                              pop_args();
                              if (val != NIL) {
                                decrement_count(val);
                                push_args();
                                val = NIL;
                                args = val;
                                increment_count(val = car(cdr(car(env))));
                                args = cons(val, args);
                                val = -242;
                                args = cons(val, args);
                                if (length(args) == 2) {
                                  increment_count(car(args));
                                  increment_count(car(cdr(args)));
                                  val = cons(car(args), car(cdr(args)));
                                } else { val = NIL; }
                                pop_args();
                                pop_function();
                              } else {
                                push_args();
                                val = NIL;
                                args = val;
                                val = new_number(71);
                                args = cons(val, args);
                                increment_count(val = car(car(env)));
                                args = cons(val, args);
                                if ((length(args) == 2) &&
                                    (is_atom(car(args))) &&
                                    (is_atom(car(cdr(args)))) &&
                                    eq(car(args), car(cdr(args)))) {
                                  increment_count(val = true);
                                } else { val = NIL; }
                                pop_args();
                                if (val != NIL) {
                                  decrement_count(val);
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  increment_count(val = car(cdr(car(env))));
                                  args = cons(val, args);
                                  increment_count(val = car(cdr(car(env))));
                                  if (val != NIL) {
                                    decrement_count(val);
                                    val = -243;
                                    args = cons(val, args);
                                    if (length(args) == 2) {
                                      increment_count(car(args));
                                      increment_count(car(cdr(args)));
                                      val = cons(car(args), car(cdr(args)));
                                    } else { val = NIL; }
                                    pop_args();
                                    pop_function();
                                  } else {
                                    val = new_number(55);
                                    args = cons(val, args);
                                    if (length(args) == 2) {
                                      increment_count(car(args));
                                      increment_count(car(cdr(args)));
                                      val = cons(car(args), car(cdr(args)));
                                    } else { val = NIL; }
                                    pop_args();
                                    pop_function();
                                  }
                                } else {
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  val = new_number(72);
                                  args = cons(val, args);
                                  increment_count(val = car(car(env)));
                                  args = cons(val, args);
                                  if ((length(args) == 2) &&
                                      (is_atom(car(args))) &&
                                      (is_atom(car(cdr(args)))) &&
                                      eq(car(args), car(cdr(args)))) {
                                    increment_count(val = true);
                                  } else { val = NIL; }
                                  pop_args();
                                  if (val != NIL) {
                                    decrement_count(val);
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    increment_count(val = car(cdr(car(env))));
                                    args = cons(val, args);
                                    increment_count(val = car(cdr(car(env))));
                                    if (val != NIL) {
                                      decrement_count(val);
                                      val = -244;
                                      args = cons(val, args);
                                      if (length(args) == 2) {
                                        increment_count(car(args));
                                        increment_count(car(cdr(args)));
                                        val = cons(car(args), car(cdr(args)));
                                      } else { val = NIL; }
                                      pop_args();
                                      pop_function();
                                    } else {
                                      val = new_number(56);
                                      args = cons(val, args);
                                      if (length(args) == 2) {
                                        increment_count(car(args));
                                        increment_count(car(cdr(args)));
                                        val = cons(car(args), car(cdr(args)));
                                      } else { val = NIL; }
                                      pop_args();
                                      pop_function();
                                    }
                                  } else {
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    val = new_number(73);
                                    args = cons(val, args);
                                    increment_count(val = car(car(env)));
                                    args = cons(val, args);
                                    if ((length(args) == 2) &&
                                        (is_atom(car(args))) &&
                                        (is_atom(car(cdr(args)))) &&
                                        eq(car(args), car(cdr(args)))) {
                                      increment_count(val = true);
                                    } else { val = NIL; }
                                    pop_args();
                                    if (val != NIL) {
                                      decrement_count(val);
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      increment_count(val = car(cdr(car(env))));
                                      args = cons(val, args);
                                      increment_count(val = car(cdr(car(env))));
                                      if (val != NIL) {
                                        decrement_count(val);
                                        val = -245;
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      } else {
                                        val = new_number(57);
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      }
                                    } else {
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      val = new_number(74);
                                      args = cons(val, args);
                                      increment_count(val = car(car(env)));
                                      args = cons(val, args);
                                      if ((length(args) == 2) &&
                                          (is_atom(car(args))) &&
                                          (is_atom(car(cdr(args)))) &&
                                          eq(car(args), car(cdr(args)))) {
                                        increment_count(val = true);
                                      } else { val = NIL; }
                                      pop_args();
                                      if (val != NIL) {
                                        decrement_count(val);
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        increment_count(val = car(cdr(car(env))));
                                        args = cons(val, args);
                                        val = new_number(45);
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      } else {
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        val = new_number(75);
                                        args = cons(val, args);
                                        increment_count(val = car(car(env)));
                                        args = cons(val, args);
                                        if ((length(args) == 2) &&
                                            (is_atom(car(args))) &&
                                            (is_atom(car(cdr(args)))) &&
                                            eq(car(args), car(cdr(args)))) {
                                          increment_count(val = true);
                                        } else { val = NIL; }
                                        pop_args();
                                        if (val != NIL) {
                                          decrement_count(val);
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          increment_count(val = car(cdr(car(env))));
                                          args = cons(val, args);
                                          increment_count(val = car(cdr(car(env))));
                                          if (val != NIL) {
                                            decrement_count(val);
                                            val = -246;
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          } else {
                                            val = new_number(52);
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          }
                                        } else {
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          val = new_number(76);
                                          args = cons(val, args);
                                          increment_count(val = car(car(env)));
                                          args = cons(val, args);
                                          if ((length(args) == 2) &&
                                              (is_atom(car(args))) &&
                                              (is_atom(car(cdr(args)))) &&
                                              eq(car(args), car(cdr(args)))) {
                                            increment_count(val = true);
                                          } else { val = NIL; }
                                          pop_args();
                                          if (val != NIL) {
                                            decrement_count(val);
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            increment_count(val = car(cdr(car(env))));
                                            args = cons(val, args);
                                            val = new_number(53);
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          } else {
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            val = new_number(77);
                                            args = cons(val, args);
                                            increment_count(val = car(car(env)));
                                            args = cons(val, args);
                                            if ((length(args) == 2) &&
                                                (is_atom(car(args))) &&
                                                (is_atom(car(cdr(args)))) &&
                                                eq(car(args), car(cdr(args)))) {
                                              increment_count(val = true);
                                            } else { val = NIL; }
                                            pop_args();
                                            if (val != NIL) {
                                              decrement_count(val);
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              increment_count(val = car(cdr(car(env))));
                                              args = cons(val, args);
                                              increment_count(val = car(cdr(car(env))));
                                              if (val != NIL) {
                                                decrement_count(val);
                                                val = -247;
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              } else {
                                                val = new_number(54);
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              }
                                            } else {
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              val = new_number(78);
                                              args = cons(val, args);
                                              increment_count(val = car(car(env)));
                                              args = cons(val, args);
                                              if ((length(args) == 2) &&
                                                  (is_atom(car(args))) &&
                                                  (is_atom(car(cdr(args)))) &&
                                                  eq(car(args), car(cdr(args)))) {
                                                increment_count(val = true);
                                              } else { val = NIL; }
                                              pop_args();
                                              if (val != NIL) {
                                                decrement_count(val);
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                increment_count(val = car(cdr(car(env))));
                                                args = cons(val, args);
                                                val = new_number(43);
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              } else {
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                val = new_number(79);
                                                args = cons(val, args);
                                                increment_count(val = car(car(env)));
                                                args = cons(val, args);
                                                if ((length(args) == 2) &&
                                                    (is_atom(car(args))) &&
                                                    (is_atom(car(cdr(args)))) &&
                                                    eq(car(args), car(cdr(args)))) {
                                                  increment_count(val = true);
                                                } else { val = NIL; }
                                                pop_args();
                                                if (val != NIL) {
                                                  decrement_count(val);
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  increment_count(val = car(cdr(car(env))));
                                                  args = cons(val, args);
                                                  increment_count(val = car(cdr(car(env))));
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    val = -58;
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  } else {
                                                    val = new_number(49);
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  }
                                                } else {
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  val = new_number(80);
                                                  args = cons(val, args);
                                                  increment_count(val = car(car(env)));
                                                  args = cons(val, args);
                                                  if ((length(args) == 2) &&
                                                      (is_atom(car(args))) &&
                                                      (is_atom(car(cdr(args)))) &&
                                                      eq(car(args), car(cdr(args)))) {
                                                    increment_count(val = true);
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    increment_count(val = car(cdr(car(env))));
                                                    args = cons(val, args);
                                                    increment_count(val = car(cdr(car(env))));
                                                    if (val != NIL) {
                                                      decrement_count(val);
                                                      val = -248;
                                                      args = cons(val, args);
                                                      if (length(args) == 2) {
                                                        increment_count(car(args));
                                                        increment_count(car(cdr(args)));
                                                        val = cons(car(args), car(cdr(args)));
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      pop_function();
                                                    } else {
                                                      val = new_number(50);
                                                      args = cons(val, args);
                                                      if (length(args) == 2) {
                                                        increment_count(car(args));
                                                        increment_count(car(cdr(args)));
                                                        val = cons(car(args), car(cdr(args)));
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      pop_function();
                                                    }
                                                  } else {
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    val = new_number(81);
                                                    args = cons(val, args);
                                                    increment_count(val = car(car(env)));
                                                    args = cons(val, args);
                                                    if ((length(args) == 2) &&
                                                        (is_atom(car(args))) &&
                                                        (is_atom(car(cdr(args)))) &&
                                                        eq(car(args), car(cdr(args)))) {
                                                      increment_count(val = true);
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    if (val != NIL) {
                                                      decrement_count(val);
                                                      push_args();
                                                      val = NIL;
                                                      args = val;
                                                      increment_count(val = car(cdr(car(env))));
                                                      args = cons(val, args);
                                                      increment_count(val = car(cdr(car(env))));
                                                      if (val != NIL) {
                                                        decrement_count(val);
                                                        val = -249;
                                                        args = cons(val, args);
                                                        if (length(args) == 2) {
                                                          increment_count(car(args));
                                                          increment_count(car(cdr(args)));
                                                          val = cons(car(args), car(cdr(args)));
                                                        } else { val = NIL; }
                                                        pop_args();
                                                        pop_function();
                                                      } else {
                                                        val = new_number(51);
                                                        args = cons(val, args);
                                                        if (length(args) == 2) {
                                                          increment_count(car(args));
                                                          increment_count(car(cdr(args)));
                                                          val = cons(car(args), car(cdr(args)));
                                                        } else { val = NIL; }
                                                        pop_args();
                                                        pop_function();
                                                      }
                                                    } else {
                                                      push_args();
                                                      val = NIL;
                                                      args = val;
                                                      val = new_number(82);
                                                      args = cons(val, args);
                                                      increment_count(val = car(car(env)));
                                                      args = cons(val, args);
                                                      if ((length(args) == 2) &&
                                                          (is_atom(car(args))) &&
                                                          (is_atom(car(cdr(args)))) &&
                                                          eq(car(args), car(cdr(args)))) {
                                                        increment_count(val = true);
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      if (val != NIL) {
                                                        decrement_count(val);
                                                        push_args();
                                                        val = NIL;
                                                        args = val;
                                                        increment_count(val = car(cdr(car(env))));
                                                        args = cons(val, args);
                                                        increment_count(val = car(cdr(car(env))));
                                                        if (val != NIL) {
                                                          decrement_count(val);
                                                          val = -250;
                                                          args = cons(val, args);
                                                          if (length(args) == 2) {
                                                            increment_count(car(args));
                                                            increment_count(car(cdr(args)));
                                                            val = cons(car(args), car(cdr(args)));
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          pop_function();
                                                        } else {
                                                          val = new_number(48);
                                                          args = cons(val, args);
                                                          if (length(args) == 2) {
                                                            increment_count(car(args));
                                                            increment_count(car(cdr(args)));
                                                            val = cons(car(args), car(cdr(args)));
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          pop_function();
                                                        }
                                                      } else {
                                                        push_args();
                                                        val = NIL;
                                                        args = val;
                                                        val = new_number(83);
                                                        args = cons(val, args);
                                                        increment_count(val = car(car(env)));
                                                        args = cons(val, args);
                                                        if ((length(args) == 2) &&
                                                            (is_atom(car(args))) &&
                                                            (is_atom(car(cdr(args)))) &&
                                                            eq(car(args), car(cdr(args)))) {
                                                          increment_count(val = true);
                                                        } else { val = NIL; }
                                                        pop_args();
                                                        if (val != NIL) {
                                                          decrement_count(val);
                                                          push_args();
                                                          val = NIL;
                                                          args = val;
                                                          increment_count(val = car(cdr(car(env))));
                                                          args = cons(val, args);
                                                          increment_count(val = car(cdr(car(env))));
                                                          if (val != NIL) {
                                                            decrement_count(val);
                                                            val = -251;
                                                            args = cons(val, args);
                                                            if (length(args) == 2) {
                                                              increment_count(car(args));
                                                              increment_count(car(cdr(args)));
                                                              val = cons(car(args), car(cdr(args)));
                                                            } else { val = NIL; }
                                                            pop_args();
                                                            pop_function();
                                                          } else {
                                                            val = new_number(46);
                                                            args = cons(val, args);
                                                            if (length(args) == 2) {
                                                              increment_count(car(args));
                                                              increment_count(car(cdr(args)));
                                                              val = cons(car(args), car(cdr(args)));
                                                            } else { val = NIL; }
                                                            pop_args();
                                                            pop_function();
                                                          }
                                                        } else {
                                                          push_args();
                                                          val = NIL;
                                                          args = val;
                                                          val = new_number(84);
                                                          args = cons(val, args);
                                                          increment_count(val = car(car(env)));
                                                          args = cons(val, args);
                                                          if ((length(args) == 2) &&
                                                              (is_atom(car(args))) &&
                                                              (is_atom(car(cdr(args)))) &&
                                                              eq(car(args), car(cdr(args)))) {
                                                            increment_count(val = true);
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          if (val != NIL) {
                                                            decrement_count(val);
                                                            push_args();
                                                            val = NIL;
                                                            args = val;
                                                            increment_count(val = car(cdr(car(env))));
                                                            args = cons(val, args);
                                                            val = -252;
                                                            args = cons(val, args);
                                                            if (length(args) == 2) {
                                                              increment_count(car(args));
                                                              increment_count(car(cdr(args)));
                                                              val = cons(car(args), car(cdr(args)));
                                                            } else { val = NIL; }
                                                            pop_args();
                                                            pop_function();
                                                          } else {
                                                            push_args();
                                                            val = NIL;
                                                            args = val;
                                                            val = new_number(85);
                                                            args = cons(val, args);
                                                            increment_count(val = car(car(env)));
                                                            args = cons(val, args);
                                                            if ((length(args) == 2) &&
                                                                (is_atom(car(args))) &&
                                                                (is_atom(car(cdr(args)))) &&
                                                                eq(car(args), car(cdr(args)))) {
                                                              increment_count(val = true);
                                                            } else { val = NIL; }
                                                            pop_args();
                                                            if (val != NIL) {
                                                              decrement_count(val);
                                                              push_args();
                                                              val = NIL;
                                                              args = val;
                                                              increment_count(val = car(cdr(car(env))));
                                                              args = cons(val, args);
                                                              val = -253;
                                                              args = cons(val, args);
                                                              if (length(args) == 2) {
                                                                increment_count(car(args));
                                                                increment_count(car(cdr(args)));
                                                                val = cons(car(args), car(cdr(args)));
                                                              } else { val = NIL; }
                                                              pop_args();
                                                              pop_function();
                                                            } else {
                                                              push_args();
                                                              val = NIL;
                                                              args = val;
                                                              val = new_number(88);
                                                              args = cons(val, args);
                                                              increment_count(val = car(car(env)));
                                                              args = cons(val, args);
                                                              if ((length(args) == 2) &&
                                                                  (is_atom(car(args))) &&
                                                                  (is_atom(car(cdr(args)))) &&
                                                                  eq(car(args), car(cdr(args)))) {
                                                                increment_count(val = true);
                                                              } else { val = NIL; }
                                                              pop_args();
                                                              if (val != NIL) {
                                                                decrement_count(val);
                                                                push_args();
                                                                val = NIL;
                                                                args = val;
                                                                increment_count(val = car(cdr(car(env))));
                                                                args = cons(val, args);
                                                                val = new_number(47);
                                                                args = cons(val, args);
                                                                if (length(args) == 2) {
                                                                  increment_count(car(args));
                                                                  increment_count(car(cdr(args)));
                                                                  val = cons(car(args), car(cdr(args)));
                                                                } else { val = NIL; }
                                                                pop_args();
                                                                pop_function();
                                                              } else {
                                                                push_args();
                                                                val = NIL;
                                                                args = val;
                                                                val = new_number(90);
                                                                args = cons(val, args);
                                                                increment_count(val = car(car(env)));
                                                                args = cons(val, args);
                                                                if ((length(args) == 2) &&
                                                                    (is_atom(car(args))) &&
                                                                    (is_atom(car(cdr(args)))) &&
                                                                    eq(car(args), car(cdr(args)))) {
                                                                  increment_count(val = true);
                                                                } else { val = NIL; }
                                                                pop_args();
                                                                if (val != NIL) {
                                                                  decrement_count(val);
                                                                  push_args();
                                                                  val = NIL;
                                                                  args = val;
                                                                  increment_count(val = car(cdr(car(env))));
                                                                  args = cons(val, args);
                                                                  val = -254;
                                                                  args = cons(val, args);
                                                                  if (length(args) == 2) {
                                                                    increment_count(car(args));
                                                                    increment_count(car(cdr(args)));
                                                                    val = cons(car(args), car(cdr(args)));
                                                                  } else { val = NIL; }
                                                                  pop_args();
                                                                  pop_function();
                                                                } else {
                                                                  push_args();
                                                                  val = NIL;
                                                                  args = val;
                                                                  val = new_number(91);
                                                                  args = cons(val, args);
                                                                  increment_count(val = car(car(env)));
                                                                  args = cons(val, args);
                                                                  if ((length(args) == 2) &&
                                                                      (is_atom(car(args))) &&
                                                                      (is_atom(car(cdr(args)))) &&
                                                                      eq(car(args), car(cdr(args)))) {
                                                                    increment_count(val = true);
                                                                  } else { val = NIL; }
                                                                  pop_args();
                                                                  if (val != NIL) {
                                                                    decrement_count(val);
                                                                    push_args();
                                                                    val = NIL;
                                                                    args = val;
                                                                    increment_count(val = car(cdr(car(env))));
                                                                    args = cons(val, args);
                                                                    val = -243;
                                                                    args = cons(val, args);
                                                                    if (length(args) == 2) {
                                                                      increment_count(car(args));
                                                                      increment_count(car(cdr(args)));
                                                                      val = cons(car(args), car(cdr(args)));
                                                                    } else { val = NIL; }
                                                                    pop_args();
                                                                    pop_function();
                                                                  } else {
                                                                    push_args();
                                                                    val = NIL;
                                                                    args = val;
                                                                    val = new_number(92);
                                                                    args = cons(val, args);
                                                                    increment_count(val = car(car(env)));
                                                                    args = cons(val, args);
                                                                    if ((length(args) == 2) &&
                                                                        (is_atom(car(args))) &&
                                                                        (is_atom(car(cdr(args)))) &&
                                                                        eq(car(args), car(cdr(args)))) {
                                                                      increment_count(val = true);
                                                                    } else { val = NIL; }
                                                                    pop_args();
                                                                    if (val != NIL) {
                                                                      decrement_count(val);
                                                                      push_args();
                                                                      val = NIL;
                                                                      args = val;
                                                                      increment_count(val = car(cdr(car(env))));
                                                                      args = cons(val, args);
                                                                      val = -244;
                                                                      args = cons(val, args);
                                                                      if (length(args) == 2) {
                                                                        increment_count(car(args));
                                                                        increment_count(car(cdr(args)));
                                                                        val = cons(car(args), car(cdr(args)));
                                                                      } else { val = NIL; }
                                                                      pop_args();
                                                                      pop_function();
                                                                    } else {
                                                                      push_args();
                                                                      val = NIL;
                                                                      args = val;
                                                                      val = new_number(93);
                                                                      args = cons(val, args);
                                                                      increment_count(val = car(car(env)));
                                                                      args = cons(val, args);
                                                                      if ((length(args) == 2) &&
                                                                          (is_atom(car(args))) &&
                                                                          (is_atom(car(cdr(args)))) &&
                                                                          eq(car(args), car(cdr(args)))) {
                                                                        increment_count(val = true);
                                                                      } else { val = NIL; }
                                                                      pop_args();
                                                                      if (val != NIL) {
                                                                        decrement_count(val);
                                                                        push_args();
                                                                        val = NIL;
                                                                        args = val;
                                                                        increment_count(val = car(cdr(car(env))));
                                                                        args = cons(val, args);
                                                                        val = -245;
                                                                        args = cons(val, args);
                                                                        if (length(args) == 2) {
                                                                          increment_count(car(args));
                                                                          increment_count(car(cdr(args)));
                                                                          val = cons(car(args), car(cdr(args)));
                                                                        } else { val = NIL; }
                                                                        pop_args();
                                                                        pop_function();
                                                                      } else {
                                                                        push_args();
                                                                        val = NIL;
                                                                        args = val;
                                                                        val = new_number(94);
                                                                        args = cons(val, args);
                                                                        increment_count(val = car(car(env)));
                                                                        args = cons(val, args);
                                                                        if ((length(args) == 2) &&
                                                                            (is_atom(car(args))) &&
                                                                            (is_atom(car(cdr(args)))) &&
                                                                            eq(car(args), car(cdr(args)))) {
                                                                          increment_count(val = true);
                                                                        } else { val = NIL; }
                                                                        pop_args();
                                                                        if (val != NIL) {
                                                                          decrement_count(val);
                                                                          push_args();
                                                                          val = NIL;
                                                                          args = val;
                                                                          increment_count(val = car(cdr(car(env))));
                                                                          args = cons(val, args);
                                                                          val = -246;
                                                                          args = cons(val, args);
                                                                          if (length(args) == 2) {
                                                                            increment_count(car(args));
                                                                            increment_count(car(cdr(args)));
                                                                            val = cons(car(args), car(cdr(args)));
                                                                          } else { val = NIL; }
                                                                          pop_args();
                                                                          pop_function();
                                                                        } else {
                                                                          push_args();
                                                                          val = NIL;
                                                                          args = val;
                                                                          val = new_number(95);
                                                                          args = cons(val, args);
                                                                          increment_count(val = car(car(env)));
                                                                          args = cons(val, args);
                                                                          if ((length(args) == 2) &&
                                                                              (is_atom(car(args))) &&
                                                                              (is_atom(car(cdr(args)))) &&
                                                                              eq(car(args), car(cdr(args)))) {
                                                                            increment_count(val = true);
                                                                          } else { val = NIL; }
                                                                          pop_args();
                                                                          if (val != NIL) {
                                                                            decrement_count(val);
                                                                            push_args();
                                                                            val = NIL;
                                                                            args = val;
                                                                            increment_count(val = car(cdr(car(env))));
                                                                            args = cons(val, args);
                                                                            val = -247;
                                                                            args = cons(val, args);
                                                                            if (length(args) == 2) {
                                                                              increment_count(car(args));
                                                                              increment_count(car(cdr(args)));
                                                                              val = cons(car(args), car(cdr(args)));
                                                                            } else { val = NIL; }
                                                                            pop_args();
                                                                            pop_function();
                                                                          } else {
                                                                            push_args();
                                                                            val = NIL;
                                                                            args = val;
                                                                            val = new_number(96);
                                                                            args = cons(val, args);
                                                                            increment_count(val = car(car(env)));
                                                                            args = cons(val, args);
                                                                            if ((length(args) == 2) &&
                                                                                (is_atom(car(args))) &&
                                                                                (is_atom(car(cdr(args)))) &&
                                                                                eq(car(args), car(cdr(args)))) {
                                                                              increment_count(val = true);
                                                                            } else { val = NIL; }
                                                                            pop_args();
                                                                            if (val != NIL) {
                                                                              decrement_count(val);
                                                                              push_args();
                                                                              val = NIL;
                                                                              args = val;
                                                                              increment_count(val = car(cdr(car(env))));
                                                                              args = cons(val, args);
                                                                              val = -58;
                                                                              args = cons(val, args);
                                                                              if (length(args) == 2) {
                                                                                increment_count(car(args));
                                                                                increment_count(car(cdr(args)));
                                                                                val = cons(car(args), car(cdr(args)));
                                                                              } else { val = NIL; }
                                                                              pop_args();
                                                                              pop_function();
                                                                            } else {
                                                                              push_args();
                                                                              val = NIL;
                                                                              args = val;
                                                                              val = new_number(97);
                                                                              args = cons(val, args);
                                                                              increment_count(val = car(car(env)));
                                                                              args = cons(val, args);
                                                                              if ((length(args) == 2) &&
                                                                                  (is_atom(car(args))) &&
                                                                                  (is_atom(car(cdr(args)))) &&
                                                                                  eq(car(args), car(cdr(args)))) {
                                                                                increment_count(val = true);
                                                                              } else { val = NIL; }
                                                                              pop_args();
                                                                              if (val != NIL) {
                                                                                decrement_count(val);
                                                                                push_args();
                                                                                val = NIL;
                                                                                args = val;
                                                                                increment_count(val = car(cdr(car(env))));
                                                                                args = cons(val, args);
                                                                                val = -248;
                                                                                args = cons(val, args);
                                                                                if (length(args) == 2) {
                                                                                  increment_count(car(args));
                                                                                  increment_count(car(cdr(args)));
                                                                                  val = cons(car(args), car(cdr(args)));
                                                                                } else { val = NIL; }
                                                                                pop_args();
                                                                                pop_function();
                                                                              } else {
                                                                                push_args();
                                                                                val = NIL;
                                                                                args = val;
                                                                                val = new_number(98);
                                                                                args = cons(val, args);
                                                                                increment_count(val = car(car(env)));
                                                                                args = cons(val, args);
                                                                                if ((length(args) == 2) &&
                                                                                    (is_atom(car(args))) &&
                                                                                    (is_atom(car(cdr(args)))) &&
                                                                                    eq(car(args), car(cdr(args)))) {
                                                                                  increment_count(val = true);
                                                                                } else { val = NIL; }
                                                                                pop_args();
                                                                                if (val != NIL) {
                                                                                  decrement_count(val);
                                                                                  push_args();
                                                                                  val = NIL;
                                                                                  args = val;
                                                                                  increment_count(val = car(cdr(car(env))));
                                                                                  args = cons(val, args);
                                                                                  val = -249;
                                                                                  args = cons(val, args);
                                                                                  if (length(args) == 2) {
                                                                                    increment_count(car(args));
                                                                                    increment_count(car(cdr(args)));
                                                                                    val = cons(car(args), car(cdr(args)));
                                                                                  } else { val = NIL; }
                                                                                  pop_args();
                                                                                  pop_function();
                                                                                } else {
                                                                                  push_args();
                                                                                  val = NIL;
                                                                                  args = val;
                                                                                  val = new_number(99);
                                                                                  args = cons(val, args);
                                                                                  increment_count(val = car(car(env)));
                                                                                  args = cons(val, args);
                                                                                  if ((length(args) == 2) &&
                                                                                      (is_atom(car(args))) &&
                                                                                      (is_atom(car(cdr(args)))) &&
                                                                                      eq(car(args), car(cdr(args)))) {
                                                                                    increment_count(val = true);
                                                                                  } else { val = NIL; }
                                                                                  pop_args();
                                                                                  if (val != NIL) {
                                                                                    decrement_count(val);
                                                                                    push_args();
                                                                                    val = NIL;
                                                                                    args = val;
                                                                                    increment_count(val = car(cdr(car(env))));
                                                                                    args = cons(val, args);
                                                                                    val = -250;
                                                                                    args = cons(val, args);
                                                                                    if (length(args) == 2) {
                                                                                      increment_count(car(args));
                                                                                      increment_count(car(cdr(args)));
                                                                                      val = cons(car(args), car(cdr(args)));
                                                                                    } else { val = NIL; }
                                                                                    pop_args();
                                                                                    pop_function();
                                                                                  } else {
                                                                                    push_args();
                                                                                    val = NIL;
                                                                                    args = val;
                                                                                    val = new_number(100);
                                                                                    args = cons(val, args);
                                                                                    increment_count(val = car(car(env)));
                                                                                    args = cons(val, args);
                                                                                    if ((length(args) == 2) &&
                                                                                        (is_atom(car(args))) &&
                                                                                        (is_atom(car(cdr(args)))) &&
                                                                                        eq(car(args), car(cdr(args)))) {
                                                                                      increment_count(val = true);
                                                                                    } else { val = NIL; }
                                                                                    pop_args();
                                                                                    if (val != NIL) {
                                                                                      decrement_count(val);
                                                                                      push_args();
                                                                                      val = NIL;
                                                                                      args = val;
                                                                                      increment_count(val = car(cdr(car(env))));
                                                                                      args = cons(val, args);
                                                                                      val = -251;
                                                                                      args = cons(val, args);
                                                                                      if (length(args) == 2) {
                                                                                        increment_count(car(args));
                                                                                        increment_count(car(cdr(args)));
                                                                                        val = cons(car(args), car(cdr(args)));
                                                                                      } else { val = NIL; }
                                                                                      pop_args();
                                                                                      pop_function();
                                                                                    } else {
                                                                                      push_args();
                                                                                      val = NIL;
                                                                                      args = val;
                                                                                      increment_count(val = car(cdr(car(env))));
                                                                                      args = cons(val, args);
                                                                                      val = NIL;
                                                                                      args = cons(val, args);
                                                                                      if (length(args) == 2) {
                                                                                        increment_count(car(args));
                                                                                        increment_count(car(cdr(args)));
                                                                                        val = cons(car(args), car(cdr(args)));
                                                                                      } else { val = NIL; }
                                                                                      pop_args();
                                                                                      pop_function();
                                                                                    }
                                                                                  }
                                                                                }
                                                                              }
                                                                            }
                                                                          }
                                                                        }
                                                                      }
                                                                    }
                                                                  }
                                                                }
                                                              }
                                                            }
                                                          }
                                                        }
                                                      }
                                                    }
                                                  }
                                                }
                                              }
                                            }
                                          }
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
void function_215() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    val = -229;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    val = new_number(30);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(cdr(car(env))));
      if (val != NIL) {
        decrement_count(val);
        val = new_number(65);
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      } else {
        val = new_number(97);
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      val = new_number(31);
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        increment_count(val = car(cdr(car(env))));
        if (val != NIL) {
          decrement_count(val);
          val = new_number(83);
          args = cons(val, args);
          if (length(args) == 2) {
            increment_count(car(args));
            increment_count(car(cdr(args)));
            val = cons(car(args), car(cdr(args)));
          } else { val = NIL; }
          pop_args();
          pop_function();
        } else {
          val = new_number(115);
          args = cons(val, args);
          if (length(args) == 2) {
            increment_count(car(args));
            increment_count(car(cdr(args)));
            val = cons(car(args), car(cdr(args)));
          } else { val = NIL; }
          pop_args();
          pop_function();
        }
      } else {
        push_args();
        val = NIL;
        args = val;
        val = new_number(32);
        args = cons(val, args);
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(cdr(car(env))));
          args = cons(val, args);
          increment_count(val = car(cdr(car(env))));
          if (val != NIL) {
            decrement_count(val);
            val = new_number(68);
            args = cons(val, args);
            if (length(args) == 2) {
              increment_count(car(args));
              increment_count(car(cdr(args)));
              val = cons(car(args), car(cdr(args)));
            } else { val = NIL; }
            pop_args();
            pop_function();
          } else {
            val = new_number(100);
            args = cons(val, args);
            if (length(args) == 2) {
              increment_count(car(args));
              increment_count(car(cdr(args)));
              val = cons(car(args), car(cdr(args)));
            } else { val = NIL; }
            pop_args();
            pop_function();
          }
        } else {
          push_args();
          val = NIL;
          args = val;
          val = new_number(33);
          args = cons(val, args);
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(cdr(car(env))));
            args = cons(val, args);
            increment_count(val = car(cdr(car(env))));
            if (val != NIL) {
              decrement_count(val);
              val = new_number(70);
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            } else {
              val = new_number(102);
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            }
          } else {
            push_args();
            val = NIL;
            args = val;
            val = new_number(34);
            args = cons(val, args);
            increment_count(val = car(car(env)));
            args = cons(val, args);
            if ((length(args) == 2) &&
                (is_atom(car(args))) &&
                (is_atom(car(cdr(args)))) &&
                eq(car(args), car(cdr(args)))) {
              increment_count(val = true);
            } else { val = NIL; }
            pop_args();
            if (val != NIL) {
              decrement_count(val);
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(cdr(car(env))));
              args = cons(val, args);
              increment_count(val = car(cdr(car(env))));
              if (val != NIL) {
                decrement_count(val);
                val = new_number(71);
                args = cons(val, args);
                if (length(args) == 2) {
                  increment_count(car(args));
                  increment_count(car(cdr(args)));
                  val = cons(car(args), car(cdr(args)));
                } else { val = NIL; }
                pop_args();
                pop_function();
              } else {
                val = new_number(103);
                args = cons(val, args);
                if (length(args) == 2) {
                  increment_count(car(args));
                  increment_count(car(cdr(args)));
                  val = cons(car(args), car(cdr(args)));
                } else { val = NIL; }
                pop_args();
                pop_function();
              }
            } else {
              push_args();
              val = NIL;
              args = val;
              val = new_number(35);
              args = cons(val, args);
              increment_count(val = car(car(env)));
              args = cons(val, args);
              if ((length(args) == 2) &&
                  (is_atom(car(args))) &&
                  (is_atom(car(cdr(args)))) &&
                  eq(car(args), car(cdr(args)))) {
                increment_count(val = true);
              } else { val = NIL; }
              pop_args();
              if (val != NIL) {
                decrement_count(val);
                push_args();
                val = NIL;
                args = val;
                increment_count(val = car(cdr(car(env))));
                args = cons(val, args);
                increment_count(val = car(cdr(car(env))));
                if (val != NIL) {
                  decrement_count(val);
                  val = new_number(72);
                  args = cons(val, args);
                  if (length(args) == 2) {
                    increment_count(car(args));
                    increment_count(car(cdr(args)));
                    val = cons(car(args), car(cdr(args)));
                  } else { val = NIL; }
                  pop_args();
                  pop_function();
                } else {
                  val = new_number(104);
                  args = cons(val, args);
                  if (length(args) == 2) {
                    increment_count(car(args));
                    increment_count(car(cdr(args)));
                    val = cons(car(args), car(cdr(args)));
                  } else { val = NIL; }
                  pop_args();
                  pop_function();
                }
              } else {
                push_args();
                val = NIL;
                args = val;
                val = new_number(36);
                args = cons(val, args);
                increment_count(val = car(car(env)));
                args = cons(val, args);
                if ((length(args) == 2) &&
                    (is_atom(car(args))) &&
                    (is_atom(car(cdr(args)))) &&
                    eq(car(args), car(cdr(args)))) {
                  increment_count(val = true);
                } else { val = NIL; }
                pop_args();
                if (val != NIL) {
                  decrement_count(val);
                  push_args();
                  val = NIL;
                  args = val;
                  increment_count(val = car(cdr(car(env))));
                  args = cons(val, args);
                  increment_count(val = car(cdr(car(env))));
                  if (val != NIL) {
                    decrement_count(val);
                    val = new_number(74);
                    args = cons(val, args);
                    if (length(args) == 2) {
                      increment_count(car(args));
                      increment_count(car(cdr(args)));
                      val = cons(car(args), car(cdr(args)));
                    } else { val = NIL; }
                    pop_args();
                    pop_function();
                  } else {
                    val = new_number(106);
                    args = cons(val, args);
                    if (length(args) == 2) {
                      increment_count(car(args));
                      increment_count(car(cdr(args)));
                      val = cons(car(args), car(cdr(args)));
                    } else { val = NIL; }
                    pop_args();
                    pop_function();
                  }
                } else {
                  push_args();
                  val = NIL;
                  args = val;
                  val = new_number(37);
                  args = cons(val, args);
                  increment_count(val = car(car(env)));
                  args = cons(val, args);
                  if ((length(args) == 2) &&
                      (is_atom(car(args))) &&
                      (is_atom(car(cdr(args)))) &&
                      eq(car(args), car(cdr(args)))) {
                    increment_count(val = true);
                  } else { val = NIL; }
                  pop_args();
                  if (val != NIL) {
                    decrement_count(val);
                    push_args();
                    val = NIL;
                    args = val;
                    increment_count(val = car(cdr(car(env))));
                    args = cons(val, args);
                    increment_count(val = car(cdr(car(env))));
                    if (val != NIL) {
                      decrement_count(val);
                      val = new_number(75);
                      args = cons(val, args);
                      if (length(args) == 2) {
                        increment_count(car(args));
                        increment_count(car(cdr(args)));
                        val = cons(car(args), car(cdr(args)));
                      } else { val = NIL; }
                      pop_args();
                      pop_function();
                    } else {
                      val = new_number(107);
                      args = cons(val, args);
                      if (length(args) == 2) {
                        increment_count(car(args));
                        increment_count(car(cdr(args)));
                        val = cons(car(args), car(cdr(args)));
                      } else { val = NIL; }
                      pop_args();
                      pop_function();
                    }
                  } else {
                    push_args();
                    val = NIL;
                    args = val;
                    val = new_number(38);
                    args = cons(val, args);
                    increment_count(val = car(car(env)));
                    args = cons(val, args);
                    if ((length(args) == 2) &&
                        (is_atom(car(args))) &&
                        (is_atom(car(cdr(args)))) &&
                        eq(car(args), car(cdr(args)))) {
                      increment_count(val = true);
                    } else { val = NIL; }
                    pop_args();
                    if (val != NIL) {
                      decrement_count(val);
                      push_args();
                      val = NIL;
                      args = val;
                      increment_count(val = car(cdr(car(env))));
                      args = cons(val, args);
                      increment_count(val = car(cdr(car(env))));
                      if (val != NIL) {
                        decrement_count(val);
                        val = new_number(76);
                        args = cons(val, args);
                        if (length(args) == 2) {
                          increment_count(car(args));
                          increment_count(car(cdr(args)));
                          val = cons(car(args), car(cdr(args)));
                        } else { val = NIL; }
                        pop_args();
                        pop_function();
                      } else {
                        val = new_number(108);
                        args = cons(val, args);
                        if (length(args) == 2) {
                          increment_count(car(args));
                          increment_count(car(cdr(args)));
                          val = cons(car(args), car(cdr(args)));
                        } else { val = NIL; }
                        pop_args();
                        pop_function();
                      }
                    } else {
                      push_args();
                      val = NIL;
                      args = val;
                      val = new_number(39);
                      args = cons(val, args);
                      increment_count(val = car(car(env)));
                      args = cons(val, args);
                      if ((length(args) == 2) &&
                          (is_atom(car(args))) &&
                          (is_atom(car(cdr(args)))) &&
                          eq(car(args), car(cdr(args)))) {
                        increment_count(val = true);
                      } else { val = NIL; }
                      pop_args();
                      if (val != NIL) {
                        decrement_count(val);
                        push_args();
                        val = NIL;
                        args = val;
                        increment_count(val = car(cdr(car(env))));
                        args = cons(val, args);
                        increment_count(val = car(cdr(car(env))));
                        if (val != NIL) {
                          decrement_count(val);
                          val = new_number(58);
                          args = cons(val, args);
                          if (length(args) == 2) {
                            increment_count(car(args));
                            increment_count(car(cdr(args)));
                            val = cons(car(args), car(cdr(args)));
                          } else { val = NIL; }
                          pop_args();
                          pop_function();
                        } else {
                          val = new_number(59);
                          args = cons(val, args);
                          if (length(args) == 2) {
                            increment_count(car(args));
                            increment_count(car(cdr(args)));
                            val = cons(car(args), car(cdr(args)));
                          } else { val = NIL; }
                          pop_args();
                          pop_function();
                        }
                      } else {
                        push_args();
                        val = NIL;
                        args = val;
                        val = new_number(40);
                        args = cons(val, args);
                        increment_count(val = car(car(env)));
                        args = cons(val, args);
                        if ((length(args) == 2) &&
                            (is_atom(car(args))) &&
                            (is_atom(car(cdr(args)))) &&
                            eq(car(args), car(cdr(args)))) {
                          increment_count(val = true);
                        } else { val = NIL; }
                        pop_args();
                        if (val != NIL) {
                          decrement_count(val);
                          push_args();
                          val = NIL;
                          args = val;
                          increment_count(val = car(cdr(car(env))));
                          args = cons(val, args);
                          increment_count(val = car(cdr(car(env))));
                          if (val != NIL) {
                            decrement_count(val);
                            val = new_number(34);
                            args = cons(val, args);
                            if (length(args) == 2) {
                              increment_count(car(args));
                              increment_count(car(cdr(args)));
                              val = cons(car(args), car(cdr(args)));
                            } else { val = NIL; }
                            pop_args();
                            pop_function();
                          } else {
                            val = new_number(39);
                            args = cons(val, args);
                            if (length(args) == 2) {
                              increment_count(car(args));
                              increment_count(car(cdr(args)));
                              val = cons(car(args), car(cdr(args)));
                            } else { val = NIL; }
                            pop_args();
                            pop_function();
                          }
                        } else {
                          push_args();
                          val = NIL;
                          args = val;
                          val = new_number(41);
                          args = cons(val, args);
                          increment_count(val = car(car(env)));
                          args = cons(val, args);
                          if ((length(args) == 2) &&
                              (is_atom(car(args))) &&
                              (is_atom(car(cdr(args)))) &&
                              eq(car(args), car(cdr(args)))) {
                            increment_count(val = true);
                          } else { val = NIL; }
                          pop_args();
                          if (val != NIL) {
                            decrement_count(val);
                            push_args();
                            val = NIL;
                            args = val;
                            increment_count(val = car(cdr(car(env))));
                            args = cons(val, args);
                            increment_count(val = car(cdr(car(env))));
                            if (val != NIL) {
                              decrement_count(val);
                              val = new_number(126);
                              args = cons(val, args);
                              if (length(args) == 2) {
                                increment_count(car(args));
                                increment_count(car(cdr(args)));
                                val = cons(car(args), car(cdr(args)));
                              } else { val = NIL; }
                              pop_args();
                              pop_function();
                            } else {
                              val = new_number(96);
                              args = cons(val, args);
                              if (length(args) == 2) {
                                increment_count(car(args));
                                increment_count(car(cdr(args)));
                                val = cons(car(args), car(cdr(args)));
                              } else { val = NIL; }
                              pop_args();
                              pop_function();
                            }
                          } else {
                            push_args();
                            val = NIL;
                            args = val;
                            val = new_number(43);
                            args = cons(val, args);
                            increment_count(val = car(car(env)));
                            args = cons(val, args);
                            if ((length(args) == 2) &&
                                (is_atom(car(args))) &&
                                (is_atom(car(cdr(args)))) &&
                                eq(car(args), car(cdr(args)))) {
                              increment_count(val = true);
                            } else { val = NIL; }
                            pop_args();
                            if (val != NIL) {
                              decrement_count(val);
                              push_args();
                              val = NIL;
                              args = val;
                              increment_count(val = car(cdr(car(env))));
                              args = cons(val, args);
                              increment_count(val = car(cdr(car(env))));
                              if (val != NIL) {
                                decrement_count(val);
                                val = new_number(124);
                                args = cons(val, args);
                                if (length(args) == 2) {
                                  increment_count(car(args));
                                  increment_count(car(cdr(args)));
                                  val = cons(car(args), car(cdr(args)));
                                } else { val = NIL; }
                                pop_args();
                                pop_function();
                              } else {
                                val = new_number(92);
                                args = cons(val, args);
                                if (length(args) == 2) {
                                  increment_count(car(args));
                                  increment_count(car(cdr(args)));
                                  val = cons(car(args), car(cdr(args)));
                                } else { val = NIL; }
                                pop_args();
                                pop_function();
                              }
                            } else {
                              push_args();
                              val = NIL;
                              args = val;
                              val = new_number(44);
                              args = cons(val, args);
                              increment_count(val = car(car(env)));
                              args = cons(val, args);
                              if ((length(args) == 2) &&
                                  (is_atom(car(args))) &&
                                  (is_atom(car(cdr(args)))) &&
                                  eq(car(args), car(cdr(args)))) {
                                increment_count(val = true);
                              } else { val = NIL; }
                              pop_args();
                              if (val != NIL) {
                                decrement_count(val);
                                push_args();
                                val = NIL;
                                args = val;
                                increment_count(val = car(cdr(car(env))));
                                args = cons(val, args);
                                increment_count(val = car(cdr(car(env))));
                                if (val != NIL) {
                                  decrement_count(val);
                                  val = new_number(90);
                                  args = cons(val, args);
                                  if (length(args) == 2) {
                                    increment_count(car(args));
                                    increment_count(car(cdr(args)));
                                    val = cons(car(args), car(cdr(args)));
                                  } else { val = NIL; }
                                  pop_args();
                                  pop_function();
                                } else {
                                  val = new_number(122);
                                  args = cons(val, args);
                                  if (length(args) == 2) {
                                    increment_count(car(args));
                                    increment_count(car(cdr(args)));
                                    val = cons(car(args), car(cdr(args)));
                                  } else { val = NIL; }
                                  pop_args();
                                  pop_function();
                                }
                              } else {
                                push_args();
                                val = NIL;
                                args = val;
                                val = new_number(45);
                                args = cons(val, args);
                                increment_count(val = car(car(env)));
                                args = cons(val, args);
                                if ((length(args) == 2) &&
                                    (is_atom(car(args))) &&
                                    (is_atom(car(cdr(args)))) &&
                                    eq(car(args), car(cdr(args)))) {
                                  increment_count(val = true);
                                } else { val = NIL; }
                                pop_args();
                                if (val != NIL) {
                                  decrement_count(val);
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  increment_count(val = car(cdr(car(env))));
                                  args = cons(val, args);
                                  increment_count(val = car(cdr(car(env))));
                                  if (val != NIL) {
                                    decrement_count(val);
                                    val = new_number(88);
                                    args = cons(val, args);
                                    if (length(args) == 2) {
                                      increment_count(car(args));
                                      increment_count(car(cdr(args)));
                                      val = cons(car(args), car(cdr(args)));
                                    } else { val = NIL; }
                                    pop_args();
                                    pop_function();
                                  } else {
                                    val = new_number(120);
                                    args = cons(val, args);
                                    if (length(args) == 2) {
                                      increment_count(car(args));
                                      increment_count(car(cdr(args)));
                                      val = cons(car(args), car(cdr(args)));
                                    } else { val = NIL; }
                                    pop_args();
                                    pop_function();
                                  }
                                } else {
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  val = new_number(46);
                                  args = cons(val, args);
                                  increment_count(val = car(car(env)));
                                  args = cons(val, args);
                                  if ((length(args) == 2) &&
                                      (is_atom(car(args))) &&
                                      (is_atom(car(cdr(args)))) &&
                                      eq(car(args), car(cdr(args)))) {
                                    increment_count(val = true);
                                  } else { val = NIL; }
                                  pop_args();
                                  if (val != NIL) {
                                    decrement_count(val);
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    increment_count(val = car(cdr(car(env))));
                                    args = cons(val, args);
                                    increment_count(val = car(cdr(car(env))));
                                    if (val != NIL) {
                                      decrement_count(val);
                                      val = new_number(67);
                                      args = cons(val, args);
                                      if (length(args) == 2) {
                                        increment_count(car(args));
                                        increment_count(car(cdr(args)));
                                        val = cons(car(args), car(cdr(args)));
                                      } else { val = NIL; }
                                      pop_args();
                                      pop_function();
                                    } else {
                                      val = new_number(99);
                                      args = cons(val, args);
                                      if (length(args) == 2) {
                                        increment_count(car(args));
                                        increment_count(car(cdr(args)));
                                        val = cons(car(args), car(cdr(args)));
                                      } else { val = NIL; }
                                      pop_args();
                                      pop_function();
                                    }
                                  } else {
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    val = new_number(47);
                                    args = cons(val, args);
                                    increment_count(val = car(car(env)));
                                    args = cons(val, args);
                                    if ((length(args) == 2) &&
                                        (is_atom(car(args))) &&
                                        (is_atom(car(cdr(args)))) &&
                                        eq(car(args), car(cdr(args)))) {
                                      increment_count(val = true);
                                    } else { val = NIL; }
                                    pop_args();
                                    if (val != NIL) {
                                      decrement_count(val);
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      increment_count(val = car(cdr(car(env))));
                                      args = cons(val, args);
                                      increment_count(val = car(cdr(car(env))));
                                      if (val != NIL) {
                                        decrement_count(val);
                                        val = new_number(86);
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      } else {
                                        val = new_number(118);
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      }
                                    } else {
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      val = new_number(48);
                                      args = cons(val, args);
                                      increment_count(val = car(car(env)));
                                      args = cons(val, args);
                                      if ((length(args) == 2) &&
                                          (is_atom(car(args))) &&
                                          (is_atom(car(cdr(args)))) &&
                                          eq(car(args), car(cdr(args)))) {
                                        increment_count(val = true);
                                      } else { val = NIL; }
                                      pop_args();
                                      if (val != NIL) {
                                        decrement_count(val);
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        increment_count(val = car(cdr(car(env))));
                                        args = cons(val, args);
                                        increment_count(val = car(cdr(car(env))));
                                        if (val != NIL) {
                                          decrement_count(val);
                                          val = new_number(66);
                                          args = cons(val, args);
                                          if (length(args) == 2) {
                                            increment_count(car(args));
                                            increment_count(car(cdr(args)));
                                            val = cons(car(args), car(cdr(args)));
                                          } else { val = NIL; }
                                          pop_args();
                                          pop_function();
                                        } else {
                                          val = new_number(98);
                                          args = cons(val, args);
                                          if (length(args) == 2) {
                                            increment_count(car(args));
                                            increment_count(car(cdr(args)));
                                            val = cons(car(args), car(cdr(args)));
                                          } else { val = NIL; }
                                          pop_args();
                                          pop_function();
                                        }
                                      } else {
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        val = new_number(49);
                                        args = cons(val, args);
                                        increment_count(val = car(car(env)));
                                        args = cons(val, args);
                                        if ((length(args) == 2) &&
                                            (is_atom(car(args))) &&
                                            (is_atom(car(cdr(args)))) &&
                                            eq(car(args), car(cdr(args)))) {
                                          increment_count(val = true);
                                        } else { val = NIL; }
                                        pop_args();
                                        if (val != NIL) {
                                          decrement_count(val);
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          increment_count(val = car(cdr(car(env))));
                                          args = cons(val, args);
                                          increment_count(val = car(cdr(car(env))));
                                          if (val != NIL) {
                                            decrement_count(val);
                                            val = new_number(78);
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          } else {
                                            val = new_number(110);
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          }
                                        } else {
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          val = new_number(50);
                                          args = cons(val, args);
                                          increment_count(val = car(car(env)));
                                          args = cons(val, args);
                                          if ((length(args) == 2) &&
                                              (is_atom(car(args))) &&
                                              (is_atom(car(cdr(args)))) &&
                                              eq(car(args), car(cdr(args)))) {
                                            increment_count(val = true);
                                          } else { val = NIL; }
                                          pop_args();
                                          if (val != NIL) {
                                            decrement_count(val);
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            increment_count(val = car(cdr(car(env))));
                                            args = cons(val, args);
                                            increment_count(val = car(cdr(car(env))));
                                            if (val != NIL) {
                                              decrement_count(val);
                                              val = new_number(77);
                                              args = cons(val, args);
                                              if (length(args) == 2) {
                                                increment_count(car(args));
                                                increment_count(car(cdr(args)));
                                                val = cons(car(args), car(cdr(args)));
                                              } else { val = NIL; }
                                              pop_args();
                                              pop_function();
                                            } else {
                                              val = new_number(109);
                                              args = cons(val, args);
                                              if (length(args) == 2) {
                                                increment_count(car(args));
                                                increment_count(car(cdr(args)));
                                                val = cons(car(args), car(cdr(args)));
                                              } else { val = NIL; }
                                              pop_args();
                                              pop_function();
                                            }
                                          } else {
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            val = new_number(51);
                                            args = cons(val, args);
                                            increment_count(val = car(car(env)));
                                            args = cons(val, args);
                                            if ((length(args) == 2) &&
                                                (is_atom(car(args))) &&
                                                (is_atom(car(cdr(args)))) &&
                                                eq(car(args), car(cdr(args)))) {
                                              increment_count(val = true);
                                            } else { val = NIL; }
                                            pop_args();
                                            if (val != NIL) {
                                              decrement_count(val);
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              increment_count(val = car(cdr(car(env))));
                                              args = cons(val, args);
                                              increment_count(val = car(cdr(car(env))));
                                              if (val != NIL) {
                                                decrement_count(val);
                                                val = new_number(60);
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              } else {
                                                val = new_number(44);
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              }
                                            } else {
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              val = new_number(52);
                                              args = cons(val, args);
                                              increment_count(val = car(car(env)));
                                              args = cons(val, args);
                                              if ((length(args) == 2) &&
                                                  (is_atom(car(args))) &&
                                                  (is_atom(car(cdr(args)))) &&
                                                  eq(car(args), car(cdr(args)))) {
                                                increment_count(val = true);
                                              } else { val = NIL; }
                                              pop_args();
                                              if (val != NIL) {
                                                decrement_count(val);
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                increment_count(val = car(cdr(car(env))));
                                                args = cons(val, args);
                                                increment_count(val = car(cdr(car(env))));
                                                if (val != NIL) {
                                                  decrement_count(val);
                                                  val = new_number(62);
                                                  args = cons(val, args);
                                                  if (length(args) == 2) {
                                                    increment_count(car(args));
                                                    increment_count(car(cdr(args)));
                                                    val = cons(car(args), car(cdr(args)));
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  pop_function();
                                                } else {
                                                  val = new_number(46);
                                                  args = cons(val, args);
                                                  if (length(args) == 2) {
                                                    increment_count(car(args));
                                                    increment_count(car(cdr(args)));
                                                    val = cons(car(args), car(cdr(args)));
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  pop_function();
                                                }
                                              } else {
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                val = new_number(53);
                                                args = cons(val, args);
                                                increment_count(val = car(car(env)));
                                                args = cons(val, args);
                                                if ((length(args) == 2) &&
                                                    (is_atom(car(args))) &&
                                                    (is_atom(car(cdr(args)))) &&
                                                    eq(car(args), car(cdr(args)))) {
                                                  increment_count(val = true);
                                                } else { val = NIL; }
                                                pop_args();
                                                if (val != NIL) {
                                                  decrement_count(val);
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  increment_count(val = car(cdr(car(env))));
                                                  args = cons(val, args);
                                                  increment_count(val = car(cdr(car(env))));
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    val = new_number(63);
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  } else {
                                                    val = new_number(47);
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  }
                                                } else {
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  val = new_number(55);
                                                  args = cons(val, args);
                                                  increment_count(val = car(car(env)));
                                                  args = cons(val, args);
                                                  if ((length(args) == 2) &&
                                                      (is_atom(car(args))) &&
                                                      (is_atom(car(cdr(args)))) &&
                                                      eq(car(args), car(cdr(args)))) {
                                                    increment_count(val = true);
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    increment_count(val = car(cdr(car(env))));
                                                    args = cons(val, args);
                                                    val = new_number(42);
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  } else {
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    val = new_number(89);
                                                    args = cons(val, args);
                                                    increment_count(val = car(car(env)));
                                                    args = cons(val, args);
                                                    if ((length(args) == 2) &&
                                                        (is_atom(car(args))) &&
                                                        (is_atom(car(cdr(args)))) &&
                                                        eq(car(args), car(cdr(args)))) {
                                                      increment_count(val = true);
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    args = cons(val, args);
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    val = new_number(56);
                                                    args = cons(val, args);
                                                    increment_count(val = car(car(env)));
                                                    args = cons(val, args);
                                                    if ((length(args) == 2) &&
                                                        (is_atom(car(args))) &&
                                                        (is_atom(car(cdr(args)))) &&
                                                        eq(car(args), car(cdr(args)))) {
                                                      increment_count(val = true);
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    args = cons(val, args);
                                                    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
                                                    if (is_function(val)) {
                                                      call_stack = cons(new_function((&function_214), env), call_stack);
                                                      return_location = address(val);
                                                      increment_count(env = environment(val));
                                                      decrement_count(val);
                                                      increment_count(args);
                                                      env = cons(args, env);
                                                      pop_args();
                                                    } else {
                                                      if (is_primitive(val)) {
                                                        temp = val;
                                                        val = eval_primitive(val, args);
                                                        decrement_count(temp);
                                                      } else {
                                                        decrement_count(val);
                                                        val = NIL;
                                                      }
                                                      pop_args();
                                                      return_location = (&function_214);
                                                    }
                                                  }
                                                }
                                              }
                                            }
                                          }
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
void function_216() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    val = -228;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    val = new_number(87);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    val = new_number(29);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_215), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_215);
    }
  }
}
void function_217() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    val = -42;
    args = cons(val, args);
    val = NIL;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    val = new_number(1);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      val = -110;
      args = cons(val, args);
      if (length(args) == 2) {
        increment_count(car(args));
        increment_count(car(cdr(args)));
        val = cons(car(args), car(cdr(args)));
      } else { val = NIL; }
      pop_args();
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      val = new_number(2);
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        increment_count(val = car(cdr(car(env))));
        if (val != NIL) {
          decrement_count(val);
          val = new_number(33);
          args = cons(val, args);
          if (length(args) == 2) {
            increment_count(car(args));
            increment_count(car(cdr(args)));
            val = cons(car(args), car(cdr(args)));
          } else { val = NIL; }
          pop_args();
          pop_function();
        } else {
          val = new_number(49);
          args = cons(val, args);
          if (length(args) == 2) {
            increment_count(car(args));
            increment_count(car(cdr(args)));
            val = cons(car(args), car(cdr(args)));
          } else { val = NIL; }
          pop_args();
          pop_function();
        }
      } else {
        push_args();
        val = NIL;
        args = val;
        val = new_number(3);
        args = cons(val, args);
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(cdr(car(env))));
          args = cons(val, args);
          increment_count(val = car(cdr(car(env))));
          if (val != NIL) {
            decrement_count(val);
            val = new_number(64);
            args = cons(val, args);
            if (length(args) == 2) {
              increment_count(car(args));
              increment_count(car(cdr(args)));
              val = cons(car(args), car(cdr(args)));
            } else { val = NIL; }
            pop_args();
            pop_function();
          } else {
            val = new_number(50);
            args = cons(val, args);
            if (length(args) == 2) {
              increment_count(car(args));
              increment_count(car(cdr(args)));
              val = cons(car(args), car(cdr(args)));
            } else { val = NIL; }
            pop_args();
            pop_function();
          }
        } else {
          push_args();
          val = NIL;
          args = val;
          val = new_number(4);
          args = cons(val, args);
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(cdr(car(env))));
            args = cons(val, args);
            increment_count(val = car(cdr(car(env))));
            if (val != NIL) {
              decrement_count(val);
              val = new_number(35);
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            } else {
              val = new_number(51);
              args = cons(val, args);
              if (length(args) == 2) {
                increment_count(car(args));
                increment_count(car(cdr(args)));
                val = cons(car(args), car(cdr(args)));
              } else { val = NIL; }
              pop_args();
              pop_function();
            }
          } else {
            push_args();
            val = NIL;
            args = val;
            val = new_number(5);
            args = cons(val, args);
            increment_count(val = car(car(env)));
            args = cons(val, args);
            if ((length(args) == 2) &&
                (is_atom(car(args))) &&
                (is_atom(car(cdr(args)))) &&
                eq(car(args), car(cdr(args)))) {
              increment_count(val = true);
            } else { val = NIL; }
            pop_args();
            if (val != NIL) {
              decrement_count(val);
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(cdr(car(env))));
              args = cons(val, args);
              increment_count(val = car(cdr(car(env))));
              if (val != NIL) {
                decrement_count(val);
                val = new_number(36);
                args = cons(val, args);
                if (length(args) == 2) {
                  increment_count(car(args));
                  increment_count(car(cdr(args)));
                  val = cons(car(args), car(cdr(args)));
                } else { val = NIL; }
                pop_args();
                pop_function();
              } else {
                val = new_number(52);
                args = cons(val, args);
                if (length(args) == 2) {
                  increment_count(car(args));
                  increment_count(car(cdr(args)));
                  val = cons(car(args), car(cdr(args)));
                } else { val = NIL; }
                pop_args();
                pop_function();
              }
            } else {
              push_args();
              val = NIL;
              args = val;
              val = new_number(6);
              args = cons(val, args);
              increment_count(val = car(car(env)));
              args = cons(val, args);
              if ((length(args) == 2) &&
                  (is_atom(car(args))) &&
                  (is_atom(car(cdr(args)))) &&
                  eq(car(args), car(cdr(args)))) {
                increment_count(val = true);
              } else { val = NIL; }
              pop_args();
              if (val != NIL) {
                decrement_count(val);
                push_args();
                val = NIL;
                args = val;
                increment_count(val = car(cdr(car(env))));
                args = cons(val, args);
                increment_count(val = car(cdr(car(env))));
                if (val != NIL) {
                  decrement_count(val);
                  val = new_number(37);
                  args = cons(val, args);
                  if (length(args) == 2) {
                    increment_count(car(args));
                    increment_count(car(cdr(args)));
                    val = cons(car(args), car(cdr(args)));
                  } else { val = NIL; }
                  pop_args();
                  pop_function();
                } else {
                  val = new_number(53);
                  args = cons(val, args);
                  if (length(args) == 2) {
                    increment_count(car(args));
                    increment_count(car(cdr(args)));
                    val = cons(car(args), car(cdr(args)));
                  } else { val = NIL; }
                  pop_args();
                  pop_function();
                }
              } else {
                push_args();
                val = NIL;
                args = val;
                val = new_number(7);
                args = cons(val, args);
                increment_count(val = car(car(env)));
                args = cons(val, args);
                if ((length(args) == 2) &&
                    (is_atom(car(args))) &&
                    (is_atom(car(cdr(args)))) &&
                    eq(car(args), car(cdr(args)))) {
                  increment_count(val = true);
                } else { val = NIL; }
                pop_args();
                if (val != NIL) {
                  decrement_count(val);
                  push_args();
                  val = NIL;
                  args = val;
                  increment_count(val = car(cdr(car(env))));
                  args = cons(val, args);
                  increment_count(val = car(cdr(car(env))));
                  if (val != NIL) {
                    decrement_count(val);
                    val = new_number(94);
                    args = cons(val, args);
                    if (length(args) == 2) {
                      increment_count(car(args));
                      increment_count(car(cdr(args)));
                      val = cons(car(args), car(cdr(args)));
                    } else { val = NIL; }
                    pop_args();
                    pop_function();
                  } else {
                    val = new_number(54);
                    args = cons(val, args);
                    if (length(args) == 2) {
                      increment_count(car(args));
                      increment_count(car(cdr(args)));
                      val = cons(car(args), car(cdr(args)));
                    } else { val = NIL; }
                    pop_args();
                    pop_function();
                  }
                } else {
                  push_args();
                  val = NIL;
                  args = val;
                  val = new_number(8);
                  args = cons(val, args);
                  increment_count(val = car(car(env)));
                  args = cons(val, args);
                  if ((length(args) == 2) &&
                      (is_atom(car(args))) &&
                      (is_atom(car(cdr(args)))) &&
                      eq(car(args), car(cdr(args)))) {
                    increment_count(val = true);
                  } else { val = NIL; }
                  pop_args();
                  if (val != NIL) {
                    decrement_count(val);
                    push_args();
                    val = NIL;
                    args = val;
                    increment_count(val = car(cdr(car(env))));
                    args = cons(val, args);
                    increment_count(val = car(cdr(car(env))));
                    if (val != NIL) {
                      decrement_count(val);
                      val = new_number(38);
                      args = cons(val, args);
                      if (length(args) == 2) {
                        increment_count(car(args));
                        increment_count(car(cdr(args)));
                        val = cons(car(args), car(cdr(args)));
                      } else { val = NIL; }
                      pop_args();
                      pop_function();
                    } else {
                      val = new_number(55);
                      args = cons(val, args);
                      if (length(args) == 2) {
                        increment_count(car(args));
                        increment_count(car(cdr(args)));
                        val = cons(car(args), car(cdr(args)));
                      } else { val = NIL; }
                      pop_args();
                      pop_function();
                    }
                  } else {
                    push_args();
                    val = NIL;
                    args = val;
                    val = new_number(9);
                    args = cons(val, args);
                    increment_count(val = car(car(env)));
                    args = cons(val, args);
                    if ((length(args) == 2) &&
                        (is_atom(car(args))) &&
                        (is_atom(car(cdr(args)))) &&
                        eq(car(args), car(cdr(args)))) {
                      increment_count(val = true);
                    } else { val = NIL; }
                    pop_args();
                    if (val != NIL) {
                      decrement_count(val);
                      push_args();
                      val = NIL;
                      args = val;
                      increment_count(val = car(cdr(car(env))));
                      args = cons(val, args);
                      increment_count(val = car(cdr(car(env))));
                      if (val != NIL) {
                        decrement_count(val);
                        val = new_number(42);
                        args = cons(val, args);
                        if (length(args) == 2) {
                          increment_count(car(args));
                          increment_count(car(cdr(args)));
                          val = cons(car(args), car(cdr(args)));
                        } else { val = NIL; }
                        pop_args();
                        pop_function();
                      } else {
                        val = new_number(56);
                        args = cons(val, args);
                        if (length(args) == 2) {
                          increment_count(car(args));
                          increment_count(car(cdr(args)));
                          val = cons(car(args), car(cdr(args)));
                        } else { val = NIL; }
                        pop_args();
                        pop_function();
                      }
                    } else {
                      push_args();
                      val = NIL;
                      args = val;
                      val = new_number(10);
                      args = cons(val, args);
                      increment_count(val = car(car(env)));
                      args = cons(val, args);
                      if ((length(args) == 2) &&
                          (is_atom(car(args))) &&
                          (is_atom(car(cdr(args)))) &&
                          eq(car(args), car(cdr(args)))) {
                        increment_count(val = true);
                      } else { val = NIL; }
                      pop_args();
                      if (val != NIL) {
                        decrement_count(val);
                        push_args();
                        val = NIL;
                        args = val;
                        increment_count(val = car(cdr(car(env))));
                        args = cons(val, args);
                        increment_count(val = car(cdr(car(env))));
                        if (val != NIL) {
                          decrement_count(val);
                          val = new_number(40);
                          args = cons(val, args);
                          if (length(args) == 2) {
                            increment_count(car(args));
                            increment_count(car(cdr(args)));
                            val = cons(car(args), car(cdr(args)));
                          } else { val = NIL; }
                          pop_args();
                          pop_function();
                        } else {
                          val = new_number(57);
                          args = cons(val, args);
                          if (length(args) == 2) {
                            increment_count(car(args));
                            increment_count(car(cdr(args)));
                            val = cons(car(args), car(cdr(args)));
                          } else { val = NIL; }
                          pop_args();
                          pop_function();
                        }
                      } else {
                        push_args();
                        val = NIL;
                        args = val;
                        val = new_number(11);
                        args = cons(val, args);
                        increment_count(val = car(car(env)));
                        args = cons(val, args);
                        if ((length(args) == 2) &&
                            (is_atom(car(args))) &&
                            (is_atom(car(cdr(args)))) &&
                            eq(car(args), car(cdr(args)))) {
                          increment_count(val = true);
                        } else { val = NIL; }
                        pop_args();
                        if (val != NIL) {
                          decrement_count(val);
                          push_args();
                          val = NIL;
                          args = val;
                          increment_count(val = car(cdr(car(env))));
                          args = cons(val, args);
                          increment_count(val = car(cdr(car(env))));
                          if (val != NIL) {
                            decrement_count(val);
                            val = new_number(41);
                            args = cons(val, args);
                            if (length(args) == 2) {
                              increment_count(car(args));
                              increment_count(car(cdr(args)));
                              val = cons(car(args), car(cdr(args)));
                            } else { val = NIL; }
                            pop_args();
                            pop_function();
                          } else {
                            val = new_number(48);
                            args = cons(val, args);
                            if (length(args) == 2) {
                              increment_count(car(args));
                              increment_count(car(cdr(args)));
                              val = cons(car(args), car(cdr(args)));
                            } else { val = NIL; }
                            pop_args();
                            pop_function();
                          }
                        } else {
                          push_args();
                          val = NIL;
                          args = val;
                          val = new_number(12);
                          args = cons(val, args);
                          increment_count(val = car(car(env)));
                          args = cons(val, args);
                          if ((length(args) == 2) &&
                              (is_atom(car(args))) &&
                              (is_atom(car(cdr(args)))) &&
                              eq(car(args), car(cdr(args)))) {
                            increment_count(val = true);
                          } else { val = NIL; }
                          pop_args();
                          if (val != NIL) {
                            decrement_count(val);
                            push_args();
                            val = NIL;
                            args = val;
                            increment_count(val = car(cdr(car(env))));
                            args = cons(val, args);
                            increment_count(val = car(cdr(car(env))));
                            if (val != NIL) {
                              decrement_count(val);
                              val = new_number(95);
                              args = cons(val, args);
                              if (length(args) == 2) {
                                increment_count(car(args));
                                increment_count(car(cdr(args)));
                                val = cons(car(args), car(cdr(args)));
                              } else { val = NIL; }
                              pop_args();
                              pop_function();
                            } else {
                              val = new_number(45);
                              args = cons(val, args);
                              if (length(args) == 2) {
                                increment_count(car(args));
                                increment_count(car(cdr(args)));
                                val = cons(car(args), car(cdr(args)));
                              } else { val = NIL; }
                              pop_args();
                              pop_function();
                            }
                          } else {
                            push_args();
                            val = NIL;
                            args = val;
                            val = new_number(13);
                            args = cons(val, args);
                            increment_count(val = car(car(env)));
                            args = cons(val, args);
                            if ((length(args) == 2) &&
                                (is_atom(car(args))) &&
                                (is_atom(car(cdr(args)))) &&
                                eq(car(args), car(cdr(args)))) {
                              increment_count(val = true);
                            } else { val = NIL; }
                            pop_args();
                            if (val != NIL) {
                              decrement_count(val);
                              push_args();
                              val = NIL;
                              args = val;
                              increment_count(val = car(cdr(car(env))));
                              args = cons(val, args);
                              increment_count(val = car(cdr(car(env))));
                              if (val != NIL) {
                                decrement_count(val);
                                val = new_number(43);
                                args = cons(val, args);
                                if (length(args) == 2) {
                                  increment_count(car(args));
                                  increment_count(car(cdr(args)));
                                  val = cons(car(args), car(cdr(args)));
                                } else { val = NIL; }
                                pop_args();
                                pop_function();
                              } else {
                                val = new_number(61);
                                args = cons(val, args);
                                if (length(args) == 2) {
                                  increment_count(car(args));
                                  increment_count(car(cdr(args)));
                                  val = cons(car(args), car(cdr(args)));
                                } else { val = NIL; }
                                pop_args();
                                pop_function();
                              }
                            } else {
                              push_args();
                              val = NIL;
                              args = val;
                              val = new_number(14);
                              args = cons(val, args);
                              increment_count(val = car(car(env)));
                              args = cons(val, args);
                              if ((length(args) == 2) &&
                                  (is_atom(car(args))) &&
                                  (is_atom(car(cdr(args)))) &&
                                  eq(car(args), car(cdr(args)))) {
                                increment_count(val = true);
                              } else { val = NIL; }
                              pop_args();
                              if (val != NIL) {
                                decrement_count(val);
                                push_args();
                                val = NIL;
                                args = val;
                                increment_count(val = car(cdr(car(env))));
                                args = cons(val, args);
                                val = -226;
                                args = cons(val, args);
                                if (length(args) == 2) {
                                  increment_count(car(args));
                                  increment_count(car(cdr(args)));
                                  val = cons(car(args), car(cdr(args)));
                                } else { val = NIL; }
                                pop_args();
                                pop_function();
                              } else {
                                push_args();
                                val = NIL;
                                args = val;
                                val = new_number(15);
                                args = cons(val, args);
                                increment_count(val = car(car(env)));
                                args = cons(val, args);
                                if ((length(args) == 2) &&
                                    (is_atom(car(args))) &&
                                    (is_atom(car(cdr(args)))) &&
                                    eq(car(args), car(cdr(args)))) {
                                  increment_count(val = true);
                                } else { val = NIL; }
                                pop_args();
                                if (val != NIL) {
                                  decrement_count(val);
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  increment_count(val = car(cdr(car(env))));
                                  args = cons(val, args);
                                  val = -227;
                                  args = cons(val, args);
                                  if (length(args) == 2) {
                                    increment_count(car(args));
                                    increment_count(car(cdr(args)));
                                    val = cons(car(args), car(cdr(args)));
                                  } else { val = NIL; }
                                  pop_args();
                                  pop_function();
                                } else {
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  val = new_number(16);
                                  args = cons(val, args);
                                  increment_count(val = car(car(env)));
                                  args = cons(val, args);
                                  if ((length(args) == 2) &&
                                      (is_atom(car(args))) &&
                                      (is_atom(car(cdr(args)))) &&
                                      eq(car(args), car(cdr(args)))) {
                                    increment_count(val = true);
                                  } else { val = NIL; }
                                  pop_args();
                                  if (val != NIL) {
                                    decrement_count(val);
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    increment_count(val = car(cdr(car(env))));
                                    args = cons(val, args);
                                    increment_count(val = car(cdr(car(env))));
                                    if (val != NIL) {
                                      decrement_count(val);
                                      val = new_number(81);
                                      args = cons(val, args);
                                      if (length(args) == 2) {
                                        increment_count(car(args));
                                        increment_count(car(cdr(args)));
                                        val = cons(car(args), car(cdr(args)));
                                      } else { val = NIL; }
                                      pop_args();
                                      pop_function();
                                    } else {
                                      val = new_number(113);
                                      args = cons(val, args);
                                      if (length(args) == 2) {
                                        increment_count(car(args));
                                        increment_count(car(cdr(args)));
                                        val = cons(car(args), car(cdr(args)));
                                      } else { val = NIL; }
                                      pop_args();
                                      pop_function();
                                    }
                                  } else {
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    val = new_number(17);
                                    args = cons(val, args);
                                    increment_count(val = car(car(env)));
                                    args = cons(val, args);
                                    if ((length(args) == 2) &&
                                        (is_atom(car(args))) &&
                                        (is_atom(car(cdr(args)))) &&
                                        eq(car(args), car(cdr(args)))) {
                                      increment_count(val = true);
                                    } else { val = NIL; }
                                    pop_args();
                                    if (val != NIL) {
                                      decrement_count(val);
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      increment_count(val = car(cdr(car(env))));
                                      args = cons(val, args);
                                      increment_count(val = car(cdr(car(env))));
                                      if (val != NIL) {
                                        decrement_count(val);
                                        val = new_number(87);
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      } else {
                                        val = new_number(119);
                                        args = cons(val, args);
                                        if (length(args) == 2) {
                                          increment_count(car(args));
                                          increment_count(car(cdr(args)));
                                          val = cons(car(args), car(cdr(args)));
                                        } else { val = NIL; }
                                        pop_args();
                                        pop_function();
                                      }
                                    } else {
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      val = new_number(18);
                                      args = cons(val, args);
                                      increment_count(val = car(car(env)));
                                      args = cons(val, args);
                                      if ((length(args) == 2) &&
                                          (is_atom(car(args))) &&
                                          (is_atom(car(cdr(args)))) &&
                                          eq(car(args), car(cdr(args)))) {
                                        increment_count(val = true);
                                      } else { val = NIL; }
                                      pop_args();
                                      if (val != NIL) {
                                        decrement_count(val);
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        increment_count(val = car(cdr(car(env))));
                                        args = cons(val, args);
                                        increment_count(val = car(cdr(car(env))));
                                        if (val != NIL) {
                                          decrement_count(val);
                                          val = new_number(69);
                                          args = cons(val, args);
                                          if (length(args) == 2) {
                                            increment_count(car(args));
                                            increment_count(car(cdr(args)));
                                            val = cons(car(args), car(cdr(args)));
                                          } else { val = NIL; }
                                          pop_args();
                                          pop_function();
                                        } else {
                                          val = new_number(101);
                                          args = cons(val, args);
                                          if (length(args) == 2) {
                                            increment_count(car(args));
                                            increment_count(car(cdr(args)));
                                            val = cons(car(args), car(cdr(args)));
                                          } else { val = NIL; }
                                          pop_args();
                                          pop_function();
                                        }
                                      } else {
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        val = new_number(19);
                                        args = cons(val, args);
                                        increment_count(val = car(car(env)));
                                        args = cons(val, args);
                                        if ((length(args) == 2) &&
                                            (is_atom(car(args))) &&
                                            (is_atom(car(cdr(args)))) &&
                                            eq(car(args), car(cdr(args)))) {
                                          increment_count(val = true);
                                        } else { val = NIL; }
                                        pop_args();
                                        if (val != NIL) {
                                          decrement_count(val);
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          increment_count(val = car(cdr(car(env))));
                                          args = cons(val, args);
                                          increment_count(val = car(cdr(car(env))));
                                          if (val != NIL) {
                                            decrement_count(val);
                                            val = new_number(82);
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          } else {
                                            val = new_number(114);
                                            args = cons(val, args);
                                            if (length(args) == 2) {
                                              increment_count(car(args));
                                              increment_count(car(cdr(args)));
                                              val = cons(car(args), car(cdr(args)));
                                            } else { val = NIL; }
                                            pop_args();
                                            pop_function();
                                          }
                                        } else {
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          val = new_number(20);
                                          args = cons(val, args);
                                          increment_count(val = car(car(env)));
                                          args = cons(val, args);
                                          if ((length(args) == 2) &&
                                              (is_atom(car(args))) &&
                                              (is_atom(car(cdr(args)))) &&
                                              eq(car(args), car(cdr(args)))) {
                                            increment_count(val = true);
                                          } else { val = NIL; }
                                          pop_args();
                                          if (val != NIL) {
                                            decrement_count(val);
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            increment_count(val = car(cdr(car(env))));
                                            args = cons(val, args);
                                            increment_count(val = car(cdr(car(env))));
                                            if (val != NIL) {
                                              decrement_count(val);
                                              val = new_number(84);
                                              args = cons(val, args);
                                              if (length(args) == 2) {
                                                increment_count(car(args));
                                                increment_count(car(cdr(args)));
                                                val = cons(car(args), car(cdr(args)));
                                              } else { val = NIL; }
                                              pop_args();
                                              pop_function();
                                            } else {
                                              val = new_number(116);
                                              args = cons(val, args);
                                              if (length(args) == 2) {
                                                increment_count(car(args));
                                                increment_count(car(cdr(args)));
                                                val = cons(car(args), car(cdr(args)));
                                              } else { val = NIL; }
                                              pop_args();
                                              pop_function();
                                            }
                                          } else {
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            val = new_number(21);
                                            args = cons(val, args);
                                            increment_count(val = car(car(env)));
                                            args = cons(val, args);
                                            if ((length(args) == 2) &&
                                                (is_atom(car(args))) &&
                                                (is_atom(car(cdr(args)))) &&
                                                eq(car(args), car(cdr(args)))) {
                                              increment_count(val = true);
                                            } else { val = NIL; }
                                            pop_args();
                                            if (val != NIL) {
                                              decrement_count(val);
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              increment_count(val = car(cdr(car(env))));
                                              args = cons(val, args);
                                              increment_count(val = car(cdr(car(env))));
                                              if (val != NIL) {
                                                decrement_count(val);
                                                val = new_number(89);
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              } else {
                                                val = new_number(121);
                                                args = cons(val, args);
                                                if (length(args) == 2) {
                                                  increment_count(car(args));
                                                  increment_count(car(cdr(args)));
                                                  val = cons(car(args), car(cdr(args)));
                                                } else { val = NIL; }
                                                pop_args();
                                                pop_function();
                                              }
                                            } else {
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              val = new_number(22);
                                              args = cons(val, args);
                                              increment_count(val = car(car(env)));
                                              args = cons(val, args);
                                              if ((length(args) == 2) &&
                                                  (is_atom(car(args))) &&
                                                  (is_atom(car(cdr(args)))) &&
                                                  eq(car(args), car(cdr(args)))) {
                                                increment_count(val = true);
                                              } else { val = NIL; }
                                              pop_args();
                                              if (val != NIL) {
                                                decrement_count(val);
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                increment_count(val = car(cdr(car(env))));
                                                args = cons(val, args);
                                                increment_count(val = car(cdr(car(env))));
                                                if (val != NIL) {
                                                  decrement_count(val);
                                                  val = new_number(85);
                                                  args = cons(val, args);
                                                  if (length(args) == 2) {
                                                    increment_count(car(args));
                                                    increment_count(car(cdr(args)));
                                                    val = cons(car(args), car(cdr(args)));
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  pop_function();
                                                } else {
                                                  val = new_number(117);
                                                  args = cons(val, args);
                                                  if (length(args) == 2) {
                                                    increment_count(car(args));
                                                    increment_count(car(cdr(args)));
                                                    val = cons(car(args), car(cdr(args)));
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  pop_function();
                                                }
                                              } else {
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                val = new_number(23);
                                                args = cons(val, args);
                                                increment_count(val = car(car(env)));
                                                args = cons(val, args);
                                                if ((length(args) == 2) &&
                                                    (is_atom(car(args))) &&
                                                    (is_atom(car(cdr(args)))) &&
                                                    eq(car(args), car(cdr(args)))) {
                                                  increment_count(val = true);
                                                } else { val = NIL; }
                                                pop_args();
                                                if (val != NIL) {
                                                  decrement_count(val);
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  increment_count(val = car(cdr(car(env))));
                                                  args = cons(val, args);
                                                  increment_count(val = car(cdr(car(env))));
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    val = new_number(73);
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  } else {
                                                    val = new_number(105);
                                                    args = cons(val, args);
                                                    if (length(args) == 2) {
                                                      increment_count(car(args));
                                                      increment_count(car(cdr(args)));
                                                      val = cons(car(args), car(cdr(args)));
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    pop_function();
                                                  }
                                                } else {
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  val = new_number(24);
                                                  args = cons(val, args);
                                                  increment_count(val = car(car(env)));
                                                  args = cons(val, args);
                                                  if ((length(args) == 2) &&
                                                      (is_atom(car(args))) &&
                                                      (is_atom(car(cdr(args)))) &&
                                                      eq(car(args), car(cdr(args)))) {
                                                    increment_count(val = true);
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    increment_count(val = car(cdr(car(env))));
                                                    args = cons(val, args);
                                                    increment_count(val = car(cdr(car(env))));
                                                    if (val != NIL) {
                                                      decrement_count(val);
                                                      val = new_number(79);
                                                      args = cons(val, args);
                                                      if (length(args) == 2) {
                                                        increment_count(car(args));
                                                        increment_count(car(cdr(args)));
                                                        val = cons(car(args), car(cdr(args)));
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      pop_function();
                                                    } else {
                                                      val = new_number(111);
                                                      args = cons(val, args);
                                                      if (length(args) == 2) {
                                                        increment_count(car(args));
                                                        increment_count(car(cdr(args)));
                                                        val = cons(car(args), car(cdr(args)));
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      pop_function();
                                                    }
                                                  } else {
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    val = new_number(25);
                                                    args = cons(val, args);
                                                    increment_count(val = car(car(env)));
                                                    args = cons(val, args);
                                                    if ((length(args) == 2) &&
                                                        (is_atom(car(args))) &&
                                                        (is_atom(car(cdr(args)))) &&
                                                        eq(car(args), car(cdr(args)))) {
                                                      increment_count(val = true);
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    if (val != NIL) {
                                                      decrement_count(val);
                                                      push_args();
                                                      val = NIL;
                                                      args = val;
                                                      increment_count(val = car(cdr(car(env))));
                                                      args = cons(val, args);
                                                      increment_count(val = car(cdr(car(env))));
                                                      if (val != NIL) {
                                                        decrement_count(val);
                                                        val = new_number(80);
                                                        args = cons(val, args);
                                                        if (length(args) == 2) {
                                                          increment_count(car(args));
                                                          increment_count(car(cdr(args)));
                                                          val = cons(car(args), car(cdr(args)));
                                                        } else { val = NIL; }
                                                        pop_args();
                                                        pop_function();
                                                      } else {
                                                        val = new_number(112);
                                                        args = cons(val, args);
                                                        if (length(args) == 2) {
                                                          increment_count(car(args));
                                                          increment_count(car(cdr(args)));
                                                          val = cons(car(args), car(cdr(args)));
                                                        } else { val = NIL; }
                                                        pop_args();
                                                        pop_function();
                                                      }
                                                    } else {
                                                      push_args();
                                                      val = NIL;
                                                      args = val;
                                                      val = new_number(26);
                                                      args = cons(val, args);
                                                      increment_count(val = car(car(env)));
                                                      args = cons(val, args);
                                                      if ((length(args) == 2) &&
                                                          (is_atom(car(args))) &&
                                                          (is_atom(car(cdr(args)))) &&
                                                          eq(car(args), car(cdr(args)))) {
                                                        increment_count(val = true);
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      if (val != NIL) {
                                                        decrement_count(val);
                                                        push_args();
                                                        val = NIL;
                                                        args = val;
                                                        increment_count(val = car(cdr(car(env))));
                                                        args = cons(val, args);
                                                        increment_count(val = car(cdr(car(env))));
                                                        if (val != NIL) {
                                                          decrement_count(val);
                                                          val = new_number(123);
                                                          args = cons(val, args);
                                                          if (length(args) == 2) {
                                                            increment_count(car(args));
                                                            increment_count(car(cdr(args)));
                                                            val = cons(car(args), car(cdr(args)));
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          pop_function();
                                                        } else {
                                                          val = new_number(91);
                                                          args = cons(val, args);
                                                          if (length(args) == 2) {
                                                            increment_count(car(args));
                                                            increment_count(car(cdr(args)));
                                                            val = cons(car(args), car(cdr(args)));
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          pop_function();
                                                        }
                                                      } else {
                                                        push_args();
                                                        val = NIL;
                                                        args = val;
                                                        val = new_number(27);
                                                        args = cons(val, args);
                                                        increment_count(val = car(car(env)));
                                                        args = cons(val, args);
                                                        if ((length(args) == 2) &&
                                                            (is_atom(car(args))) &&
                                                            (is_atom(car(cdr(args)))) &&
                                                            eq(car(args), car(cdr(args)))) {
                                                          increment_count(val = true);
                                                        } else { val = NIL; }
                                                        pop_args();
                                                        if (val != NIL) {
                                                          decrement_count(val);
                                                          push_args();
                                                          val = NIL;
                                                          args = val;
                                                          increment_count(val = car(cdr(car(env))));
                                                          args = cons(val, args);
                                                          increment_count(val = car(cdr(car(env))));
                                                          if (val != NIL) {
                                                            decrement_count(val);
                                                            val = new_number(125);
                                                            args = cons(val, args);
                                                            if (length(args) == 2) {
                                                              increment_count(car(args));
                                                              increment_count(car(cdr(args)));
                                                              val = cons(car(args), car(cdr(args)));
                                                            } else { val = NIL; }
                                                            pop_args();
                                                            pop_function();
                                                          } else {
                                                            val = new_number(93);
                                                            args = cons(val, args);
                                                            if (length(args) == 2) {
                                                              increment_count(car(args));
                                                              increment_count(car(cdr(args)));
                                                              val = cons(car(args), car(cdr(args)));
                                                            } else { val = NIL; }
                                                            pop_args();
                                                            pop_function();
                                                          }
                                                        } else {
                                                          push_args();
                                                          val = NIL;
                                                          args = val;
                                                          push_args();
                                                          val = NIL;
                                                          args = val;
                                                          val = new_number(86);
                                                          args = cons(val, args);
                                                          increment_count(val = car(car(env)));
                                                          args = cons(val, args);
                                                          if ((length(args) == 2) &&
                                                              (is_atom(car(args))) &&
                                                              (is_atom(car(cdr(args)))) &&
                                                              eq(car(args), car(cdr(args)))) {
                                                            increment_count(val = true);
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          args = cons(val, args);
                                                          push_args();
                                                          val = NIL;
                                                          args = val;
                                                          val = new_number(28);
                                                          args = cons(val, args);
                                                          increment_count(val = car(car(env)));
                                                          args = cons(val, args);
                                                          if ((length(args) == 2) &&
                                                              (is_atom(car(args))) &&
                                                              (is_atom(car(cdr(args)))) &&
                                                              eq(car(args), car(cdr(args)))) {
                                                            increment_count(val = true);
                                                          } else { val = NIL; }
                                                          pop_args();
                                                          args = cons(val, args);
                                                          increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
                                                          if (is_function(val)) {
                                                            call_stack = cons(new_function((&function_216), env), call_stack);
                                                            return_location = address(val);
                                                            increment_count(env = environment(val));
                                                            decrement_count(val);
                                                            increment_count(args);
                                                            env = cons(args, env);
                                                            pop_args();
                                                          } else {
                                                            if (is_primitive(val)) {
                                                              temp = val;
                                                              val = eval_primitive(val, args);
                                                              decrement_count(temp);
                                                            } else {
                                                              decrement_count(val);
                                                              val = NIL;
                                                            }
                                                            pop_args();
                                                            return_location = (&function_216);
                                                          }
                                                        }
                                                      }
                                                    }
                                                  }
                                                }
                                              }
                                            }
                                          }
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
void function_218() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    val = NIL;
    args = cons(val, args);
    val = NIL;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    val = new_number(54);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    val = new_number(42);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_217), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_217);
    }
  }
}
void function_219() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(182);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(170);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_218), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_218);
  }
}
void function_220() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_219), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_213), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_116);
  }
}
void function_221() {
  pop_function();
}
void function_222() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_221);
  }
}
void function_223() {
  pop_function();
}
void function_224() {
  args = cons(val, args);
  increment_count(val = car(car(car(env))));
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_223);
  }
}
void function_225() {
  pop_function();
}
void function_226() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_225);
  }
}
void function_227() {
  push_args();
  val = NIL;
  args = val;
  val = -130;
  args = cons(val, args);
  increment_count(val = car(cdr(car(car(env)))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(cdr(car(env))))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(cdr(car(env)))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(car(env))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_224), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_224);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(cdr(car(env))))));
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(car(car(env))))));
      args = cons(val, args);
      increment_count(val = car(cdr(car(car(env)))));
      args = cons(val, args);
      increment_count(val = car(car(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_226), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_226);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_228() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_227), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_222), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_222);
  }
}
void function_229() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(car(env)))));
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_228), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_228);
  }
}
void function_230() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_229), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_220), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_115);
  }
}
void function_231() {
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_232() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(car(env)))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_231), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_231);
  }
}
void function_233() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_232), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_230), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_114);
  }
}
void function_234() {
  pop_function();
}
void function_235() {
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_234);
  }
}
void function_236() {
  pop_function();
}
void function_237() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(car(env)))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_236);
  }
}
void function_238() {
  push_args();
  val = NIL;
  args = val;
  val = -130;
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(cdr(cdr(cdr(car(env))))))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(cdr(cdr(cdr(car(env)))))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_235), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_235);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(cdr(cdr(cdr(car(env))))))));
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(env))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_237), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_237);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_239() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_238), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_233), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_113);
  }
}
void function_240() {
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_241() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  val = new_number(4000);
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_240), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_240);
  }
}
void function_242() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_241), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_239), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_112);
  }
}
void function_243() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_242), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_111);
  }
}
void function_244() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_243), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_243);
  }
}
void function_245() {
  pop_function();
}
void function_246() {
  args = cons(val, args);
  push_args();
  val = new_number(10);
  args = val;
  push_args();
  val = new_number(13);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_245);
  }
}
void function_247() {
  args = cons(val, args);
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_246), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_246);
  }
}
void function_248() {
  args = cons(val, args);
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_246), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_246);
  }
}
void function_249() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(cdr(car(env))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = -94;
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_247), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_247);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_248), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_248);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      args = cons(val, args);
      push_args();
      val = new_number(58);
      args = val;
      push_args();
      val = new_number(32);
      args = val;
      val = NIL;
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_246), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_246);
      }
    }
  }
}
void function_250() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_249), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_244), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_110);
  }
}
void function_251() {
  pop_function();
}
void function_252() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_251);
  }
}
void function_253() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_252), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_252);
  }
}
void function_254() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_253), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_250), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_109);
  }
}
void function_255() {
  pop_function();
}
void function_256() {
  args = cons(val, args);
  val = -94;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_255);
  }
}
void function_257() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_256), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_256);
  }
}
void function_258() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_257), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_254), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_108);
  }
}
void function_259() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_258), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_107);
  }
}
void function_260() {
  pop_function();
}
void function_261() {
  pop_function();
}
void function_262() {
  pop_function();
}
void function_263() {
  pop_function();
}
void function_264() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_263);
  }
}
void function_265() {
  pop_function();
}
void function_266() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_265);
  }
}
void function_267() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_266), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_266);
  }
}
void function_268() {
  pop_function();
}
void function_269() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_268);
  }
}
void function_270() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_269), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_269);
  }
}
void function_271() {
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_270), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_270);
  }
}
void function_272() {
  pop_function();
}
void function_273() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_272);
  }
}
void function_274() {
  pop_function();
}
void function_275() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_274);
  }
}
void function_276() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_275), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_275);
  }
}
void function_277() {
  if (is_function(val)) {
    call_stack = cons(new_function((&function_276), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_276);
  }
}
void function_278() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_262);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    val = COND;
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = car(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = cdr(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_264), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_264);
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      val = LET;
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(car(env)));
        args = cons(val, args);
        increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
        if (is_function(val)) {
          call_stack = cons(new_function((&function_267), env), call_stack);
          return_location = address(val);
          increment_count(env = environment(val));
          decrement_count(val);
          increment_count(args);
          env = cons(args, env);
          pop_args();
        } else {
          if (is_primitive(val)) {
            temp = val;
            val = eval_primitive(val, args);
            decrement_count(temp);
          } else {
            decrement_count(val);
            val = NIL;
          }
          pop_args();
          return_location = (&function_267);
        }
      } else {
        push_args();
        val = NIL;
        args = val;
        val = FN;
        args = cons(val, args);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if (length(args) == 1) {
          if (! is_atom(car(args))) {
          increment_count(val = car(car(args)));
          } else { val = NIL; }
        } else { val = NIL; }
        pop_args();
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          push_args();
          val = NIL;
          args = val;
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(cdr(car(env))));
          args = cons(val, args);
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(car(env)));
          args = cons(val, args);
          increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
          if (is_function(val)) {
            call_stack = cons(new_function((&function_271), env), call_stack);
            return_location = address(val);
            increment_count(env = environment(val));
            decrement_count(val);
            increment_count(args);
            env = cons(args, env);
            pop_args();
          } else {
            if (is_primitive(val)) {
              temp = val;
              val = eval_primitive(val, args);
              decrement_count(temp);
            } else {
              decrement_count(val);
              val = NIL;
            }
            pop_args();
            return_location = (&function_271);
          }
        } else {
          push_args();
          val = NIL;
          args = val;
          val = QUOTE;
          args = cons(val, args);
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if (length(args) == 1) {
            if (! is_atom(car(args))) {
            increment_count(val = car(car(args)));
            } else { val = NIL; }
          } else { val = NIL; }
          pop_args();
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = NIL;
            args = val;
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(car(env)));
            args = cons(val, args);
            increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
            if (is_function(val)) {
              call_stack = cons(new_function((&function_273), env), call_stack);
              return_location = address(val);
              increment_count(env = environment(val));
              decrement_count(val);
              increment_count(args);
              env = cons(args, env);
              pop_args();
            } else {
              if (is_primitive(val)) {
                temp = val;
                val = eval_primitive(val, args);
                decrement_count(temp);
              } else {
                decrement_count(val);
                val = NIL;
              }
              pop_args();
              return_location = (&function_273);
            }
          } else {
            increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
            if (val != NIL) {
              decrement_count(val);
              push_args();
              val = NIL;
              args = val;
              push_args();
              val = NIL;
              args = val;
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(car(env)));
              args = cons(val, args);
              if (length(args) == 1) {
                if (! is_atom(car(args))) {
                increment_count(val = cdr(car(args)));
                } else { val = NIL; }
              } else { val = NIL; }
              pop_args();
              args = cons(val, args);
              push_args();
              val = NIL;
              args = val;
              increment_count(val = car(cdr(car(env))));
              args = cons(val, args);
              increment_count(val = car(car(cdr(env))));
              args = cons(val, args);
              increment_count(val = car(car(cdr(cdr(env)))));
              if (is_function(val)) {
                call_stack = cons(new_function((&function_277), env), call_stack);
                return_location = address(val);
                increment_count(env = environment(val));
                decrement_count(val);
                increment_count(args);
                env = cons(args, env);
                pop_args();
              } else {
                if (is_primitive(val)) {
                  temp = val;
                  val = eval_primitive(val, args);
                  decrement_count(temp);
                } else {
                  decrement_count(val);
                  val = NIL;
                }
                pop_args();
                return_location = (&function_277);
              }
            } else {
              /** Warning, unhandled cond-default */ val = NIL;
              pop_function();
            }
          }
        }
      }
    }
  }
}
void function_279() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_261);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_278), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_278);
    }
  }
}
void function_280() {
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (is_number(car(args))) {
        increment_count(val = true);
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_260);
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_279), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_279);
      }
    }
  }
}
void function_281() {
  increment_count(env);
  val = new_function((&function_280), env);
  pop_function();
}
void function_282() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_281), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_259), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_259);
  }
}
void function_283() {
  pop_function();
}
void function_284() {
  pop_function();
}
void function_285() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_284);
  }
}
void function_286() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_285), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_285);
  }
}
void function_287() {
  pop_function();
}
void function_288() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_287);
  }
}
void function_289() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_288), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_286), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_283);
  }
}
void function_290() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_289), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_282), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_106);
  }
}
void function_291() {
  pop_function();
}
void function_292() {
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_291);
  }
}
void function_293() {
  if (is_function(val)) {
    call_stack = cons(new_function((&function_292), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_292);
  }
}
void function_294() {
  pop_function();
}
void function_295() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_294);
  }
}
void function_296() {
  args = cons(val, args);
  val = FN;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_295), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_295);
  }
}
void function_297() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_296), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_296);
  }
}
void function_298() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_297), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_297);
  }
}
void function_299() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_298), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_298);
  }
}
void function_300() {
  increment_count(val = car(cdr(cdr(car(cdr(env))))));
  pop_function();
}
void function_301() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(cdr(car(env))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_300), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_299), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_293), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_293);
  }
}
void function_302() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_301), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_290), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_105);
  }
}
void function_303() {
  pop_function();
}
void function_304() {
  pop_function();
}
void function_305() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_304);
  }
}
void function_306() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_305), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_305);
  }
}
void function_307() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_306), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_306);
  }
}
void function_308() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_307), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_307);
  }
}
void function_309() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_308), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_308);
  }
}
void function_310() {
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  pop_function();
}
void function_311() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_310), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_309), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_303);
  }
}
void function_312() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_311), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_302), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_104);
  }
}
void function_313() {
  pop_function();
}
void function_314() {
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  pop_function();
}
void function_315() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_314), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_313);
  }
}
void function_316() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_315), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_312), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_103);
  }
}
void function_317() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_316), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_102);
  }
}
void function_318() {
  pop_function();
}
void function_319() {
  pop_function();
}
void function_320() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(car(env)))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_318);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(cdr(car(env))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_319);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_321() {
  pop_function();
}
void function_322() {
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_321);
  }
}
void function_323() {
  pop_function();
}
void function_324() {
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(cdr(env))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_323);
  }
}
void function_325() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_324), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_324);
  }
}
void function_326() {
  pop_function();
}
void function_327() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_326);
  }
}
void function_328() {
  pop_function();
}
void function_329() {
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(cdr(cdr(env)))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_328);
  }
}
void function_330() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_329), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_329);
  }
}
void function_331() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(cdr(car(cdr(env)))))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_330), env);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_327), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_327);
  }
}
void function_332() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_320), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_320);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(env);
      val = new_function((&function_331), env);
      args = cons(val, args);
      increment_count(env);
      val = new_function((&function_325), env);
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_322), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_322);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_333() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_332), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_332);
  }
}
void function_334() {
  increment_count(env);
  val = new_function((&function_333), env);
  pop_function();
}
void function_335() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_334), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_317), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_317);
  }
}
void function_336() {
  pop_function();
}
void function_337() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_336);
  }
}
void function_338() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_337), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_337);
  }
}
void function_339() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_338), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_335), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_101);
  }
}
void function_340() {
  pop_function();
}
void function_341() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_340);
  }
}
void function_342() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_341), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_341);
  }
}
void function_343() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_342), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_339), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_100);
  }
}
void function_344() {
  pop_function();
}
void function_345() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_344);
  }
}
void function_346() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_345), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_345);
  }
}
void function_347() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_346), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_343), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_99);
  }
}
void function_348() {
  pop_function();
}
void function_349() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_348);
  }
}
void function_350() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_349), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_349);
  }
}
void function_351() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_350), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_347), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_98);
  }
}
void function_352() {
  pop_function();
}
void function_353() {
  pop_function();
}
void function_354() {
  pop_function();
}
void function_355() {
  args = cons(val, args);
  val = new_number(1);
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(env))))));
  args = cons(val, args);
  if (is_number(car(args))) {
    if ((length(args) == 2) &&
        is_number(car(cdr(args)))) {
      val = new_number(value(car(args)) - 
                       value(car(cdr(args))));
    } else if (length(args) == 1) {
      val = new_number(-value(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_354);
  }
}
void function_356() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_355), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_355);
  }
}
void function_357() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_356), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_356);
  }
}
void function_358() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_357), env);
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(env))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_353);
  }
}
void function_359() {
  pop_function();
}
void function_360() {
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(cdr(cdr(env)))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_359);
  }
}
void function_361() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_360), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_360);
  }
}
void function_362() {
  increment_count(env);
  val = new_function((&function_361), env);
  pop_function();
}
void function_363() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) < value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(env);
      val = new_function((&function_362), env);
      args = cons(val, args);
      increment_count(env);
      val = new_function((&function_358), env);
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_352);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_364() {
  increment_count(env);
  val = new_function((&function_363), env);
  pop_function();
}
void function_365() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_364), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_351), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_97);
  }
}
void function_366() {
  pop_function();
}
void function_367() {
  pop_function();
}
void function_368() {
  pop_function();
}
void function_369() {
  pop_function();
}
void function_370() {
  pop_function();
}
void function_371() {
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(cdr(cdr(env))))))));
  args = cons(val, args);
  if (is_number(car(args))) {
    if ((length(args) == 2) &&
        is_number(car(cdr(args)))) {
      val = new_number(value(car(args)) - 
                       value(car(cdr(args))));
    } else if (length(args) == 1) {
      val = new_number(-value(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_370);
  }
}
void function_372() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_371), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_371);
  }
}
void function_373() {
  pop_function();
}
void function_374() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_373);
  }
}
void function_375() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_374), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_372), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_369);
  }
}
void function_376() {
  pop_function();
}
void function_377() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(cdr(cdr(cdr(env))))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(cdr(env)))))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_376);
  }
}
void function_378() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_377), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_375), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_368);
  }
}
void function_379() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_378), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_367);
  }
}
void function_380() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_379), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_379);
  }
}
void function_381() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_380), env);
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_366);
  }
}
void function_382() {
  increment_count(env);
  val = new_function((&function_381), env);
  pop_function();
}
void function_383() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_382), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_365), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_96);
  }
}
void function_384() {
  pop_function();
}
void function_385() {
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_384);
  }
}
void function_386() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) < value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_385), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_385);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_387() {
  increment_count(env);
  val = new_function((&function_386), env);
  pop_function();
}
void function_388() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_387), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_383), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_95);
  }
}
void function_389() {
  pop_function();
}
void function_390() {
  pop_function();
}
void function_391() {
  pop_function();
}
void function_392() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(cdr(cdr(env)))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_391);
  }
}
void function_393() {
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(cdr(car(cdr(cdr(env))))));
    args = cons(val, args);
    increment_count(env);
    val = new_function((&function_392), env);
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_390);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      increment_count(val = car(cdr(cdr(car(cdr(cdr(env)))))));
      args = cons(val, args);
      increment_count(env);
      val = new_function((&function_392), env);
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_390);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      args = cons(val, args);
      increment_count(env);
      val = new_function((&function_392), env);
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_390);
      }
    }
  }
}
void function_394() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_393), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_393);
  }
}
void function_395() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_394), env);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if (is_number(car(args))) {
    if ((length(args) == 2) &&
        is_number(car(cdr(args)))) {
      val = new_number(value(car(args)) - 
                       value(car(cdr(args))));
    } else if (length(args) == 1) {
      val = new_number(-value(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_389);
  }
}
void function_396() {
  increment_count(env);
  val = new_function((&function_395), env);
  pop_function();
}
void function_397() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_396), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_388), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_94);
  }
}
void function_398() {
  pop_function();
}
void function_399() {
  pop_function();
}
void function_400() {
  pop_function();
}
void function_401() {
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) < value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(cdr(cdr(car(cdr(env))))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_400);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_402() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_401), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_401);
  }
}
void function_403() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_402), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_399);
  }
}
void function_404() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_403), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_403);
  }
}
void function_405() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_404), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_398);
  }
}
void function_406() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_405), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_397), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_93);
  }
}
void function_407() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_406), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_92);
  }
}
void function_408() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_407), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_407);
  }
}
void function_409() {
  pop_function();
}
void function_410() {
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_409);
  }
}
void function_411() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_410), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_410);
  }
}
void function_412() {
  increment_count(env);
  val = new_function((&function_411), env);
  pop_function();
}
void function_413() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_412), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_408), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_91);
  }
}
void function_414() {
  pop_function();
}
void function_415() {
  pop_function();
}
void function_416() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_415);
  }
}
void function_417() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_416), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_414);
  }
}
void function_418() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_417), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_417);
  }
}
void function_419() {
  increment_count(env);
  val = new_function((&function_418), env);
  pop_function();
}
void function_420() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_419), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_413), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_90);
  }
}
void function_421() {
  pop_function();
}
void function_422() {
  pop_function();
}
void function_423() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_422);
  }
}
void function_424() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_423), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_421);
  }
}
void function_425() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_424), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_424);
  }
}
void function_426() {
  increment_count(env);
  val = new_function((&function_425), env);
  pop_function();
}
void function_427() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_426), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_420), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_89);
  }
}
void function_428() {
  pop_function();
}
void function_429() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_428);
  }
}
void function_430() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_429), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_427), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_88);
  }
}
void function_431() {
  pop_function();
}
void function_432() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_431);
  }
}
void function_433() {
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_432), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_432);
  }
}
void function_434() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_433), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_433);
  }
}
void function_435() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_434), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_430), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_87);
  }
}
void function_436() {
  pop_function();
}
void function_437() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_436);
  }
}
void function_438() {
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) < value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(cdr(env)))));
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_437), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_437);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_439() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_438), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_438);
  }
}
void function_440() {
  increment_count(env);
  val = new_function((&function_439), env);
  pop_function();
}
void function_441() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_440), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_435), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_86);
  }
}
void function_442() {
  pop_function();
}
void function_443() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_442);
  }
}
void function_444() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_443), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_443);
  }
}
void function_445() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_444), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_444);
  }
}
void function_446() {
  pop_function();
}
void function_447() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_446);
  }
}
void function_448() {
  pop_function();
}
void function_449() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_448);
  }
}
void function_450() {
  pop_function();
}
void function_451() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_450);
  }
}
void function_452() {
  pop_function();
}
void function_453() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_452);
  }
}
void function_454() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_453), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_453);
  }
}
void function_455() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_454), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_454);
  }
}
void function_456() {
  pop_function();
}
void function_457() {
  pop_function();
}
void function_458() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_457);
  }
}
void function_459() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  push_args();
  increment_count(val = car(car(env)));
  args = val;
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_458), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_458);
  }
}
void function_460() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_459), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_456);
  }
}
void function_461() {
  push_args();
  val = NIL;
  args = val;
  val = CONS;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_445), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_445);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    val = CAR;
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_447), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_447);
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      val = CDR;
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(cdr(car(env))));
        args = cons(val, args);
        increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
        if (is_function(val)) {
          call_stack = cons(new_function((&function_449), env), call_stack);
          return_location = address(val);
          increment_count(env = environment(val));
          decrement_count(val);
          increment_count(args);
          env = cons(args, env);
          pop_args();
        } else {
          if (is_primitive(val)) {
            temp = val;
            val = eval_primitive(val, args);
            decrement_count(temp);
          } else {
            decrement_count(val);
            val = NIL;
          }
          pop_args();
          return_location = (&function_449);
        }
      } else {
        push_args();
        val = NIL;
        args = val;
        val = ATOM;
        args = cons(val, args);
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = NIL;
          args = val;
          push_args();
          val = NIL;
          args = val;
          increment_count(val = car(cdr(car(env))));
          args = cons(val, args);
          increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
          if (is_function(val)) {
            call_stack = cons(new_function((&function_451), env), call_stack);
            return_location = address(val);
            increment_count(env = environment(val));
            decrement_count(val);
            increment_count(args);
            env = cons(args, env);
            pop_args();
          } else {
            if (is_primitive(val)) {
              temp = val;
              val = eval_primitive(val, args);
              decrement_count(temp);
            } else {
              decrement_count(val);
              val = NIL;
            }
            pop_args();
            return_location = (&function_451);
          }
        } else {
          push_args();
          val = NIL;
          args = val;
          val = EQ;
          args = cons(val, args);
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = NIL;
            args = val;
            push_args();
            val = NIL;
            args = val;
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(cdr(car(env))));
            args = cons(val, args);
            increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
            if (is_function(val)) {
              call_stack = cons(new_function((&function_455), env), call_stack);
              return_location = address(val);
              increment_count(env = environment(val));
              decrement_count(val);
              increment_count(args);
              env = cons(args, env);
              pop_args();
            } else {
              if (is_primitive(val)) {
                temp = val;
                val = eval_primitive(val, args);
                decrement_count(temp);
              } else {
                decrement_count(val);
                val = NIL;
              }
              pop_args();
              return_location = (&function_455);
            }
          } else {
            push_args();
            val = NIL;
            args = val;
            push_args();
            val = NIL;
            args = val;
            increment_count(val = car(cdr(car(env))));
            args = cons(val, args);
            increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
            if (is_function(val)) {
              call_stack = cons(new_function((&function_460), env), call_stack);
              return_location = address(val);
              increment_count(env = environment(val));
              decrement_count(val);
              increment_count(args);
              env = cons(args, env);
              pop_args();
            } else {
              if (is_primitive(val)) {
                temp = val;
                val = eval_primitive(val, args);
                decrement_count(temp);
              } else {
                decrement_count(val);
                val = NIL;
              }
              pop_args();
              return_location = (&function_460);
            }
          }
        }
      }
    }
  }
}
void function_462() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_461), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_441), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_85);
  }
}
void function_463() {
  pop_function();
}
void function_464() {
  pop_function();
}
void function_465() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_464);
  }
}
void function_466() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_465), env);
  args = cons(val, args);
  val = -130;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_463);
  }
}
void function_467() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_466), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_462), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_84);
  }
}
void function_468() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_467), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_83);
  }
}
void function_469() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(79);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(102);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(109);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(121);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  val = -88;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_468), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_468);
  }
}
void function_470() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_469), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_82);
  }
}
void function_471() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_470), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_470);
  }
}
void function_472() {
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_473() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_472), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_472);
  }
}
void function_474() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_473), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_473);
  }
}
void function_475() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_474), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_471), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_471);
  }
}
void function_476() {
  pop_function();
}
void function_477() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_476);
  }
}
void function_478() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_477), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_477);
  }
}
void function_479() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_478), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_478);
  }
}
void function_480() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_479), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_479);
  }
}
void function_481() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_480), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_480);
  }
}
void function_482() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_481), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_481);
  }
}
void function_483() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_482), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_475), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_81);
  }
}
void function_484() {
  pop_function();
}
void function_485() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_484);
  }
}
void function_486() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_485), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_485);
  }
}
void function_487() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_486), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_483), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_80);
  }
}
void function_488() {
  pop_function();
}
void function_489() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_488);
  }
}
void function_490() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_489), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_489);
  }
}
void function_491() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_490), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_490);
  }
}
void function_492() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_491), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_491);
  }
}
void function_493() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_492), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_492);
  }
}
void function_494() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_493), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_487), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_79);
  }
}
void function_495() {
  pop_function();
}
void function_496() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_495);
  }
}
void function_497() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_496), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_496);
  }
}
void function_498() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_497), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_497);
  }
}
void function_499() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_498), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_498);
  }
}
void function_500() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_499), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_499);
  }
}
void function_501() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_500), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_494), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_78);
  }
}
void function_502() {
  pop_function();
}
void function_503() {
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_502);
  }
}
void function_504() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_503), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_503);
  }
}
void function_505() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_504), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_504);
  }
}
void function_506() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_505), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_505);
  }
}
void function_507() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_506), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_506);
  }
}
void function_508() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_507), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_507);
  }
}
void function_509() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_508), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_501), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_77);
  }
}
void function_510() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_509), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_76);
  }
}
void function_511() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_510), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_510);
  }
}
void function_512() {
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_513() {
  args = cons(val, args);
  val = new_number(1);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_512), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_512);
  }
}
void function_514() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_513), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_513);
  }
}
void function_515() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_514), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_514);
  }
}
void function_516() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_515), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_511), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_75);
  }
}
void function_517() {
  pop_function();
}
void function_518() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    val = new_number(0);
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_517);
    }
  }
}
void function_519() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_518), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_516), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_74);
  }
}
void function_520() {
  pop_function();
}
void function_521() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    val = new_number(0);
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_520);
    }
  }
}
void function_522() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_521), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_519), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_73);
  }
}
void function_523() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_number(car(args))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(env)));
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (is_atom(car(args))) {
        increment_count(val = true); 
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      val = new_number(0);
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      pop_function();
    }
  }
}
void function_524() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_523), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_522), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_72);
  }
}
void function_525() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = cdr(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_526() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_525), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_524), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_71);
  }
}
void function_527() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_528() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_527), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_526), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_70);
  }
}
void function_529() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_530() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_529), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_528), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_69);
  }
}
void function_531() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_530), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_68);
  }
}
void function_532() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_531), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_531);
  }
}
void function_533() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_532), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_67);
  }
}
void function_534() {
  pop_function();
}
void function_535() {
  pop_function();
}
void function_536() {
  pop_function();
}
void function_537() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_536);
  }
}
void function_538() {
  pop_function();
}
void function_539() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_538);
  }
}
void function_540() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))));
  args = cons(val, args);
  val = CDR;
  args = cons(val, args);
  val = CAR;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_539), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_539);
  }
}
void function_541() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_540), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_540);
  }
}
void function_542() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(cdr(env))));
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_537), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_537);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(cdr(env))));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(env)))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_541), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_541);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_543() {
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(cdr(env))));
    args = cons(val, args);
    push_args();
    val = new_number(40);
    args = val;
    push_args();
    val = new_number(41);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_534);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (is_function(car(args))) {
        increment_count(val = true);
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(cdr(env))));
      args = cons(val, args);
      push_args();
      val = new_number(60);
      args = val;
      push_args();
      val = new_number(70);
      args = val;
      push_args();
      val = new_number(85);
      args = val;
      push_args();
      val = new_number(78);
      args = val;
      push_args();
      val = new_number(67);
      args = val;
      push_args();
      val = new_number(84);
      args = val;
      push_args();
      val = new_number(73);
      args = val;
      push_args();
      val = new_number(79);
      args = val;
      push_args();
      val = new_number(78);
      args = val;
      push_args();
      val = new_number(62);
      args = val;
      val = NIL;
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_535);
      }
    } else {
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_542), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_542);
      }
    }
  }
}
void function_544() {
  increment_count(env);
  val = new_function((&function_543), env);
  pop_function();
}
void function_545() {
  increment_count(env);
  val = new_function((&function_544), env);
  pop_function();
}
void function_546() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_545), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_533), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_533);
  }
}
void function_547() {
  pop_function();
}
void function_548() {
  args = cons(val, args);
  push_args();
  val = new_number(40);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_547);
  }
}
void function_549() {
  if (is_function(val)) {
    call_stack = cons(new_function((&function_548), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_548);
  }
}
void function_550() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_549), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_549);
  }
}
void function_551() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_550), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_550);
  }
}
void function_552() {
  increment_count(env);
  val = new_function((&function_551), env);
  pop_function();
}
void function_553() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_552), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_546), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_66);
  }
}
void function_554() {
  pop_function();
}
void function_555() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(40);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    val = new_number(41);
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = cdr(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = car(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = cdr(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      pop_function();
    } else {
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if (length(args) == 1) {
          if (! is_atom(car(args))) {
          increment_count(val = cdr(car(args)));
          } else { val = NIL; }
        } else { val = NIL; }
        pop_args();
        args = cons(val, args);
        val = new_number(32);
        args = cons(val, args);
        if (length(args) == 2) {
          increment_count(car(args));
          increment_count(car(cdr(args)));
          val = cons(car(args), car(cdr(args)));
        } else { val = NIL; }
        pop_args();
        pop_function();
      } else {
        /** Warning, unhandled cond-default */ val = NIL;
        pop_function();
      }
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = new_number(41);
      args = val;
      val = NIL;
      increment_count(args);
      val = cons(args, val);
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      push_args();
      val = new_number(32);
      args = val;
      push_args();
      val = new_number(46);
      args = val;
      push_args();
      val = new_number(32);
      args = val;
      val = NIL;
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_554);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_556() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_555), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_553), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_65);
  }
}
void function_557() {
  pop_function();
}
void function_558() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_number(car(args))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_557);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    val = QUOTE;
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = new_number(113);
      args = val;
      push_args();
      val = new_number(117);
      args = val;
      push_args();
      val = new_number(111);
      args = val;
      push_args();
      val = new_number(116);
      args = val;
      push_args();
      val = new_number(101);
      args = val;
      val = NIL;
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      increment_count(args);
      val = cons(args, val);
      pop_args();
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      val = CAR;
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = new_number(99);
        args = val;
        push_args();
        val = new_number(97);
        args = val;
        push_args();
        val = new_number(114);
        args = val;
        val = NIL;
        increment_count(args);
        val = cons(args, val);
        pop_args();
        increment_count(args);
        val = cons(args, val);
        pop_args();
        increment_count(args);
        val = cons(args, val);
        pop_args();
        pop_function();
      } else {
        push_args();
        val = NIL;
        args = val;
        val = CDR;
        args = cons(val, args);
        increment_count(val = car(car(env)));
        args = cons(val, args);
        if ((length(args) == 2) &&
            (is_atom(car(args))) &&
            (is_atom(car(cdr(args)))) &&
            eq(car(args), car(cdr(args)))) {
          increment_count(val = true);
        } else { val = NIL; }
        pop_args();
        if (val != NIL) {
          decrement_count(val);
          push_args();
          val = new_number(99);
          args = val;
          push_args();
          val = new_number(100);
          args = val;
          push_args();
          val = new_number(114);
          args = val;
          val = NIL;
          increment_count(args);
          val = cons(args, val);
          pop_args();
          increment_count(args);
          val = cons(args, val);
          pop_args();
          increment_count(args);
          val = cons(args, val);
          pop_args();
          pop_function();
        } else {
          push_args();
          val = NIL;
          args = val;
          val = NUMBER;
          args = cons(val, args);
          increment_count(val = car(car(env)));
          args = cons(val, args);
          if ((length(args) == 2) &&
              (is_atom(car(args))) &&
              (is_atom(car(cdr(args)))) &&
              eq(car(args), car(cdr(args)))) {
            increment_count(val = true);
          } else { val = NIL; }
          pop_args();
          if (val != NIL) {
            decrement_count(val);
            push_args();
            val = new_number(110);
            args = val;
            push_args();
            val = new_number(117);
            args = val;
            push_args();
            val = new_number(109);
            args = val;
            push_args();
            val = new_number(98);
            args = val;
            push_args();
            val = new_number(101);
            args = val;
            push_args();
            val = new_number(114);
            args = val;
            val = NIL;
            increment_count(args);
            val = cons(args, val);
            pop_args();
            increment_count(args);
            val = cons(args, val);
            pop_args();
            increment_count(args);
            val = cons(args, val);
            pop_args();
            increment_count(args);
            val = cons(args, val);
            pop_args();
            increment_count(args);
            val = cons(args, val);
            pop_args();
            increment_count(args);
            val = cons(args, val);
            pop_args();
            pop_function();
          } else {
            push_args();
            val = NIL;
            args = val;
            val = FUNCTION;
            args = cons(val, args);
            increment_count(val = car(car(env)));
            args = cons(val, args);
            if ((length(args) == 2) &&
                (is_atom(car(args))) &&
                (is_atom(car(cdr(args)))) &&
                eq(car(args), car(cdr(args)))) {
              increment_count(val = true);
            } else { val = NIL; }
            pop_args();
            if (val != NIL) {
              decrement_count(val);
              push_args();
              val = new_number(102);
              args = val;
              push_args();
              val = new_number(117);
              args = val;
              push_args();
              val = new_number(110);
              args = val;
              push_args();
              val = new_number(99);
              args = val;
              push_args();
              val = new_number(116);
              args = val;
              push_args();
              val = new_number(105);
              args = val;
              push_args();
              val = new_number(111);
              args = val;
              push_args();
              val = new_number(110);
              args = val;
              val = NIL;
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              increment_count(args);
              val = cons(args, val);
              pop_args();
              pop_function();
            } else {
              push_args();
              val = NIL;
              args = val;
              val = ATOM;
              args = cons(val, args);
              increment_count(val = car(car(env)));
              args = cons(val, args);
              if ((length(args) == 2) &&
                  (is_atom(car(args))) &&
                  (is_atom(car(cdr(args)))) &&
                  eq(car(args), car(cdr(args)))) {
                increment_count(val = true);
              } else { val = NIL; }
              pop_args();
              if (val != NIL) {
                decrement_count(val);
                push_args();
                val = new_number(97);
                args = val;
                push_args();
                val = new_number(116);
                args = val;
                push_args();
                val = new_number(111);
                args = val;
                push_args();
                val = new_number(109);
                args = val;
                val = NIL;
                increment_count(args);
                val = cons(args, val);
                pop_args();
                increment_count(args);
                val = cons(args, val);
                pop_args();
                increment_count(args);
                val = cons(args, val);
                pop_args();
                increment_count(args);
                val = cons(args, val);
                pop_args();
                pop_function();
              } else {
                push_args();
                val = NIL;
                args = val;
                val = CONS;
                args = cons(val, args);
                increment_count(val = car(car(env)));
                args = cons(val, args);
                if ((length(args) == 2) &&
                    (is_atom(car(args))) &&
                    (is_atom(car(cdr(args)))) &&
                    eq(car(args), car(cdr(args)))) {
                  increment_count(val = true);
                } else { val = NIL; }
                pop_args();
                if (val != NIL) {
                  decrement_count(val);
                  push_args();
                  val = new_number(99);
                  args = val;
                  push_args();
                  val = new_number(111);
                  args = val;
                  push_args();
                  val = new_number(110);
                  args = val;
                  push_args();
                  val = new_number(115);
                  args = val;
                  val = NIL;
                  increment_count(args);
                  val = cons(args, val);
                  pop_args();
                  increment_count(args);
                  val = cons(args, val);
                  pop_args();
                  increment_count(args);
                  val = cons(args, val);
                  pop_args();
                  increment_count(args);
                  val = cons(args, val);
                  pop_args();
                  pop_function();
                } else {
                  push_args();
                  val = NIL;
                  args = val;
                  val = COND;
                  args = cons(val, args);
                  increment_count(val = car(car(env)));
                  args = cons(val, args);
                  if ((length(args) == 2) &&
                      (is_atom(car(args))) &&
                      (is_atom(car(cdr(args)))) &&
                      eq(car(args), car(cdr(args)))) {
                    increment_count(val = true);
                  } else { val = NIL; }
                  pop_args();
                  if (val != NIL) {
                    decrement_count(val);
                    push_args();
                    val = new_number(99);
                    args = val;
                    push_args();
                    val = new_number(111);
                    args = val;
                    push_args();
                    val = new_number(110);
                    args = val;
                    push_args();
                    val = new_number(100);
                    args = val;
                    val = NIL;
                    increment_count(args);
                    val = cons(args, val);
                    pop_args();
                    increment_count(args);
                    val = cons(args, val);
                    pop_args();
                    increment_count(args);
                    val = cons(args, val);
                    pop_args();
                    increment_count(args);
                    val = cons(args, val);
                    pop_args();
                    pop_function();
                  } else {
                    push_args();
                    val = NIL;
                    args = val;
                    val = LET;
                    args = cons(val, args);
                    increment_count(val = car(car(env)));
                    args = cons(val, args);
                    if ((length(args) == 2) &&
                        (is_atom(car(args))) &&
                        (is_atom(car(cdr(args)))) &&
                        eq(car(args), car(cdr(args)))) {
                      increment_count(val = true);
                    } else { val = NIL; }
                    pop_args();
                    if (val != NIL) {
                      decrement_count(val);
                      push_args();
                      val = new_number(108);
                      args = val;
                      push_args();
                      val = new_number(101);
                      args = val;
                      push_args();
                      val = new_number(116);
                      args = val;
                      val = NIL;
                      increment_count(args);
                      val = cons(args, val);
                      pop_args();
                      increment_count(args);
                      val = cons(args, val);
                      pop_args();
                      increment_count(args);
                      val = cons(args, val);
                      pop_args();
                      pop_function();
                    } else {
                      push_args();
                      val = NIL;
                      args = val;
                      val = FN;
                      args = cons(val, args);
                      increment_count(val = car(car(env)));
                      args = cons(val, args);
                      if ((length(args) == 2) &&
                          (is_atom(car(args))) &&
                          (is_atom(car(cdr(args)))) &&
                          eq(car(args), car(cdr(args)))) {
                        increment_count(val = true);
                      } else { val = NIL; }
                      pop_args();
                      if (val != NIL) {
                        decrement_count(val);
                        push_args();
                        val = new_number(102);
                        args = val;
                        push_args();
                        val = new_number(110);
                        args = val;
                        val = NIL;
                        increment_count(args);
                        val = cons(args, val);
                        pop_args();
                        increment_count(args);
                        val = cons(args, val);
                        pop_args();
                        pop_function();
                      } else {
                        push_args();
                        val = NIL;
                        args = val;
                        val = URSHIFT;
                        args = cons(val, args);
                        increment_count(val = car(car(env)));
                        args = cons(val, args);
                        if ((length(args) == 2) &&
                            (is_atom(car(args))) &&
                            (is_atom(car(cdr(args)))) &&
                            eq(car(args), car(cdr(args)))) {
                          increment_count(val = true);
                        } else { val = NIL; }
                        pop_args();
                        if (val != NIL) {
                          decrement_count(val);
                          push_args();
                          val = new_number(62);
                          args = val;
                          push_args();
                          val = new_number(62);
                          args = val;
                          push_args();
                          val = new_number(62);
                          args = val;
                          val = NIL;
                          increment_count(args);
                          val = cons(args, val);
                          pop_args();
                          increment_count(args);
                          val = cons(args, val);
                          pop_args();
                          increment_count(args);
                          val = cons(args, val);
                          pop_args();
                          pop_function();
                        } else {
                          push_args();
                          val = NIL;
                          args = val;
                          val = SRSHIFT;
                          args = cons(val, args);
                          increment_count(val = car(car(env)));
                          args = cons(val, args);
                          if ((length(args) == 2) &&
                              (is_atom(car(args))) &&
                              (is_atom(car(cdr(args)))) &&
                              eq(car(args), car(cdr(args)))) {
                            increment_count(val = true);
                          } else { val = NIL; }
                          pop_args();
                          if (val != NIL) {
                            decrement_count(val);
                            push_args();
                            val = new_number(62);
                            args = val;
                            push_args();
                            val = new_number(62);
                            args = val;
                            val = NIL;
                            increment_count(args);
                            val = cons(args, val);
                            pop_args();
                            increment_count(args);
                            val = cons(args, val);
                            pop_args();
                            pop_function();
                          } else {
                            push_args();
                            val = NIL;
                            args = val;
                            val = LSHIFT;
                            args = cons(val, args);
                            increment_count(val = car(car(env)));
                            args = cons(val, args);
                            if ((length(args) == 2) &&
                                (is_atom(car(args))) &&
                                (is_atom(car(cdr(args)))) &&
                                eq(car(args), car(cdr(args)))) {
                              increment_count(val = true);
                            } else { val = NIL; }
                            pop_args();
                            if (val != NIL) {
                              decrement_count(val);
                              push_args();
                              val = new_number(60);
                              args = val;
                              push_args();
                              val = new_number(60);
                              args = val;
                              val = NIL;
                              increment_count(args);
                              val = cons(args, val);
                              pop_args();
                              increment_count(args);
                              val = cons(args, val);
                              pop_args();
                              pop_function();
                            } else {
                              push_args();
                              val = NIL;
                              args = val;
                              val = LT;
                              args = cons(val, args);
                              increment_count(val = car(car(env)));
                              args = cons(val, args);
                              if ((length(args) == 2) &&
                                  (is_atom(car(args))) &&
                                  (is_atom(car(cdr(args)))) &&
                                  eq(car(args), car(cdr(args)))) {
                                increment_count(val = true);
                              } else { val = NIL; }
                              pop_args();
                              if (val != NIL) {
                                decrement_count(val);
                                push_args();
                                val = new_number(60);
                                args = val;
                                val = NIL;
                                increment_count(args);
                                val = cons(args, val);
                                pop_args();
                                pop_function();
                              } else {
                                push_args();
                                val = NIL;
                                args = val;
                                val = GT;
                                args = cons(val, args);
                                increment_count(val = car(car(env)));
                                args = cons(val, args);
                                if ((length(args) == 2) &&
                                    (is_atom(car(args))) &&
                                    (is_atom(car(cdr(args)))) &&
                                    eq(car(args), car(cdr(args)))) {
                                  increment_count(val = true);
                                } else { val = NIL; }
                                pop_args();
                                if (val != NIL) {
                                  decrement_count(val);
                                  push_args();
                                  val = new_number(62);
                                  args = val;
                                  val = NIL;
                                  increment_count(args);
                                  val = cons(args, val);
                                  pop_args();
                                  pop_function();
                                } else {
                                  push_args();
                                  val = NIL;
                                  args = val;
                                  val = EQ;
                                  args = cons(val, args);
                                  increment_count(val = car(car(env)));
                                  args = cons(val, args);
                                  if ((length(args) == 2) &&
                                      (is_atom(car(args))) &&
                                      (is_atom(car(cdr(args)))) &&
                                      eq(car(args), car(cdr(args)))) {
                                    increment_count(val = true);
                                  } else { val = NIL; }
                                  pop_args();
                                  if (val != NIL) {
                                    decrement_count(val);
                                    push_args();
                                    val = new_number(61);
                                    args = val;
                                    val = NIL;
                                    increment_count(args);
                                    val = cons(args, val);
                                    pop_args();
                                    pop_function();
                                  } else {
                                    push_args();
                                    val = NIL;
                                    args = val;
                                    val = PLUS;
                                    args = cons(val, args);
                                    increment_count(val = car(car(env)));
                                    args = cons(val, args);
                                    if ((length(args) == 2) &&
                                        (is_atom(car(args))) &&
                                        (is_atom(car(cdr(args)))) &&
                                        eq(car(args), car(cdr(args)))) {
                                      increment_count(val = true);
                                    } else { val = NIL; }
                                    pop_args();
                                    if (val != NIL) {
                                      decrement_count(val);
                                      push_args();
                                      val = new_number(43);
                                      args = val;
                                      val = NIL;
                                      increment_count(args);
                                      val = cons(args, val);
                                      pop_args();
                                      pop_function();
                                    } else {
                                      push_args();
                                      val = NIL;
                                      args = val;
                                      val = MINUS;
                                      args = cons(val, args);
                                      increment_count(val = car(car(env)));
                                      args = cons(val, args);
                                      if ((length(args) == 2) &&
                                          (is_atom(car(args))) &&
                                          (is_atom(car(cdr(args)))) &&
                                          eq(car(args), car(cdr(args)))) {
                                        increment_count(val = true);
                                      } else { val = NIL; }
                                      pop_args();
                                      if (val != NIL) {
                                        decrement_count(val);
                                        push_args();
                                        val = new_number(45);
                                        args = val;
                                        val = NIL;
                                        increment_count(args);
                                        val = cons(args, val);
                                        pop_args();
                                        pop_function();
                                      } else {
                                        push_args();
                                        val = NIL;
                                        args = val;
                                        val = MULT;
                                        args = cons(val, args);
                                        increment_count(val = car(car(env)));
                                        args = cons(val, args);
                                        if ((length(args) == 2) &&
                                            (is_atom(car(args))) &&
                                            (is_atom(car(cdr(args)))) &&
                                            eq(car(args), car(cdr(args)))) {
                                          increment_count(val = true);
                                        } else { val = NIL; }
                                        pop_args();
                                        if (val != NIL) {
                                          decrement_count(val);
                                          push_args();
                                          val = new_number(42);
                                          args = val;
                                          val = NIL;
                                          increment_count(args);
                                          val = cons(args, val);
                                          pop_args();
                                          pop_function();
                                        } else {
                                          push_args();
                                          val = NIL;
                                          args = val;
                                          val = DIV;
                                          args = cons(val, args);
                                          increment_count(val = car(car(env)));
                                          args = cons(val, args);
                                          if ((length(args) == 2) &&
                                              (is_atom(car(args))) &&
                                              (is_atom(car(cdr(args)))) &&
                                              eq(car(args), car(cdr(args)))) {
                                            increment_count(val = true);
                                          } else { val = NIL; }
                                          pop_args();
                                          if (val != NIL) {
                                            decrement_count(val);
                                            push_args();
                                            val = new_number(47);
                                            args = val;
                                            val = NIL;
                                            increment_count(args);
                                            val = cons(args, val);
                                            pop_args();
                                            pop_function();
                                          } else {
                                            push_args();
                                            val = NIL;
                                            args = val;
                                            val = MOD;
                                            args = cons(val, args);
                                            increment_count(val = car(car(env)));
                                            args = cons(val, args);
                                            if ((length(args) == 2) &&
                                                (is_atom(car(args))) &&
                                                (is_atom(car(cdr(args)))) &&
                                                eq(car(args), car(cdr(args)))) {
                                              increment_count(val = true);
                                            } else { val = NIL; }
                                            pop_args();
                                            if (val != NIL) {
                                              decrement_count(val);
                                              push_args();
                                              val = new_number(37);
                                              args = val;
                                              val = NIL;
                                              increment_count(args);
                                              val = cons(args, val);
                                              pop_args();
                                              pop_function();
                                            } else {
                                              push_args();
                                              val = NIL;
                                              args = val;
                                              val = BAND;
                                              args = cons(val, args);
                                              increment_count(val = car(car(env)));
                                              args = cons(val, args);
                                              if ((length(args) == 2) &&
                                                  (is_atom(car(args))) &&
                                                  (is_atom(car(cdr(args)))) &&
                                                  eq(car(args), car(cdr(args)))) {
                                                increment_count(val = true);
                                              } else { val = NIL; }
                                              pop_args();
                                              if (val != NIL) {
                                                decrement_count(val);
                                                push_args();
                                                val = new_number(38);
                                                args = val;
                                                val = NIL;
                                                increment_count(args);
                                                val = cons(args, val);
                                                pop_args();
                                                pop_function();
                                              } else {
                                                push_args();
                                                val = NIL;
                                                args = val;
                                                val = BOR;
                                                args = cons(val, args);
                                                increment_count(val = car(car(env)));
                                                args = cons(val, args);
                                                if ((length(args) == 2) &&
                                                    (is_atom(car(args))) &&
                                                    (is_atom(car(cdr(args)))) &&
                                                    eq(car(args), car(cdr(args)))) {
                                                  increment_count(val = true);
                                                } else { val = NIL; }
                                                pop_args();
                                                if (val != NIL) {
                                                  decrement_count(val);
                                                  push_args();
                                                  val = new_number(124);
                                                  args = val;
                                                  val = NIL;
                                                  increment_count(args);
                                                  val = cons(args, val);
                                                  pop_args();
                                                  pop_function();
                                                } else {
                                                  push_args();
                                                  val = NIL;
                                                  args = val;
                                                  val = BXOR;
                                                  args = cons(val, args);
                                                  increment_count(val = car(car(env)));
                                                  args = cons(val, args);
                                                  if ((length(args) == 2) &&
                                                      (is_atom(car(args))) &&
                                                      (is_atom(car(cdr(args)))) &&
                                                      eq(car(args), car(cdr(args)))) {
                                                    increment_count(val = true);
                                                  } else { val = NIL; }
                                                  pop_args();
                                                  if (val != NIL) {
                                                    decrement_count(val);
                                                    push_args();
                                                    val = new_number(94);
                                                    args = val;
                                                    val = NIL;
                                                    increment_count(args);
                                                    val = cons(args, val);
                                                    pop_args();
                                                    pop_function();
                                                  } else {
                                                    push_args();
                                                    val = NIL;
                                                    args = val;
                                                    val = BNEG;
                                                    args = cons(val, args);
                                                    increment_count(val = car(car(env)));
                                                    args = cons(val, args);
                                                    if ((length(args) == 2) &&
                                                        (is_atom(car(args))) &&
                                                        (is_atom(car(cdr(args)))) &&
                                                        eq(car(args), car(cdr(args)))) {
                                                      increment_count(val = true);
                                                    } else { val = NIL; }
                                                    pop_args();
                                                    if (val != NIL) {
                                                      decrement_count(val);
                                                      push_args();
                                                      val = new_number(126);
                                                      args = val;
                                                      val = NIL;
                                                      increment_count(args);
                                                      val = cons(args, val);
                                                      pop_args();
                                                      pop_function();
                                                    } else {
                                                      push_args();
                                                      val = NIL;
                                                      args = val;
                                                      increment_count(val = car(car(env)));
                                                      args = cons(val, args);
                                                      if (length(args) == 1) {
                                                        if (! is_atom(car(args))) {
                                                        increment_count(val = cdr(car(args)));
                                                        } else { val = NIL; }
                                                      } else { val = NIL; }
                                                      pop_args();
                                                      pop_function();
                                                    }
                                                  }
                                                }
                                              }
                                            }
                                          }
                                        }
                                      }
                                    }
                                  }
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
void function_559() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_558), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_556), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_64);
  }
}
void function_560() {
  args = cons(val, args);
  val = new_number(45);
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_561() {
  if (is_function(val)) {
    call_stack = cons(new_function((&function_560), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_560);
  }
}
void function_562() {
  pop_function();
}
void function_563() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_562);
  }
}
void function_564() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) < value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    val = NIL;
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (is_number(car(args))) {
      if ((length(args) == 2) &&
          is_number(car(cdr(args)))) {
        val = new_number(value(car(args)) - 
                         value(car(cdr(args))));
      } else if (length(args) == 1) {
        val = new_number(-value(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_561), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_561);
    }
  } else {
    push_args();
    val = NIL;
    args = val;
    val = new_number(0);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = new_number(48);
      args = val;
      val = NIL;
      increment_count(args);
      val = cons(args, val);
      pop_args();
      pop_function();
    } else {
      increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
      if (val != NIL) {
        decrement_count(val);
        push_args();
        val = NIL;
        args = val;
        val = NIL;
        args = cons(val, args);
        push_args();
        val = NIL;
        args = val;
        increment_count(val = car(car(env)));
        args = cons(val, args);
        increment_count(val = car(car(cdr(env))));
        if (is_function(val)) {
          call_stack = cons(new_function((&function_563), env), call_stack);
          return_location = address(val);
          increment_count(env = environment(val));
          decrement_count(val);
          increment_count(args);
          env = cons(args, env);
          pop_args();
        } else {
          if (is_primitive(val)) {
            temp = val;
            val = eval_primitive(val, args);
            decrement_count(temp);
          } else {
            decrement_count(val);
            val = NIL;
          }
          pop_args();
          return_location = (&function_563);
        }
      } else {
        /** Warning, unhandled cond-default */ val = NIL;
        pop_function();
      }
    }
  }
}
void function_565() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_564), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_559), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_63);
  }
}
void function_566() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_565), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_62);
  }
}
void function_567() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(10);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(cdr(args))) == 0) {
      val = NIL;
    } else {
      val = new_number(value(car(args)) %
                       value(car(cdr(args))));
    }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_568() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(10);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(cdr(args))) == 0) {
      val = NIL;
    } else {
      val = new_number(value(car(args)) / 
                       value(car(cdr(args))));
    }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_569() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_570() {
  pop_function();
}
void function_571() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(48);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_570);
  }
}
void function_572() {
  increment_count(env);
  val = new_function((&function_571), env);
  pop_function();
}
void function_573() {
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  pop_function();
}
void function_574() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_573), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_572), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_569), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_568), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_567), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_566), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_566);
  }
}
void function_575() {
  pop_function();
}
void function_576() {
  pop_function();
}
void function_577() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  val = -130;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_576);
  }
}
void function_578() {
  pop_function();
}
void function_579() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = new_number(80);
  args = val;
  push_args();
  val = new_number(97);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(115);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_578);
  }
}
void function_580() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_579), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_577), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_575);
  }
}
void function_581() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_580), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_574), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_61);
  }
}
void function_582() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_581), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_60);
  }
}
void function_583() {
  pop_function();
}
void function_584() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_583);
  }
}
void function_585() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_584), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_584);
  }
}
void function_586() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(32);
  args = cons(val, args);
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_585), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_585);
  }
}
void function_587() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_586), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_586);
  }
}
void function_588() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(34);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_587), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_587);
  }
}
void function_589() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_588), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_588);
  }
}
void function_590() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(39);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_589), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_589);
  }
}
void function_591() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_590), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_590);
  }
}
void function_592() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_591), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_591);
  }
}
void function_593() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(40);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_592), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_592);
  }
}
void function_594() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_593), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_593);
  }
}
void function_595() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_594), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_594);
  }
}
void function_596() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(41);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_595), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_595);
  }
}
void function_597() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_596), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_596);
  }
}
void function_598() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(46);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_597), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_597);
  }
}
void function_599() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_598), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_598);
  }
}
void function_600() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(59);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_599), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_599);
  }
}
void function_601() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_600), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_600);
  }
}
void function_602() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_601), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_601);
  }
}
void function_603() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_602), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_602);
  }
}
void function_604() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(57);
  args = cons(val, args);
  val = new_number(48);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_603), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_603);
  }
}
void function_605() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_604), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_604);
  }
}
void function_606() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_605), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_582), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_582);
  }
}
void function_607() {
  pop_function();
}
void function_608() {
  pop_function();
}
void function_609() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_608);
  }
}
void function_610() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_609), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_609);
  }
}
void function_611() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(32);
  args = cons(val, args);
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_610), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_610);
  }
}
void function_612() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_611), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_611);
  }
}
void function_613() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(59);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_612), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_612);
  }
}
void function_614() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_613), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_613);
  }
}
void function_615() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_614), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_614);
  }
}
void function_616() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(35);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_615), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_615);
  }
}
void function_617() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_616), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_616);
  }
}
void function_618() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_617), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_617);
  }
}
void function_619() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(41);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_618), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_618);
  }
}
void function_620() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_619), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_619);
  }
}
void function_621() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(46);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_620), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_620);
  }
}
void function_622() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_621), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_621);
  }
}
void function_623() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_622), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_622);
  }
}
void function_624() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_623), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_623);
  }
}
void function_625() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_624), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_607);
  }
}
void function_626() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_625), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_606), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_59);
  }
}
void function_627() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_626), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_58);
  }
}
void function_628() {
  pop_function();
}
void function_629() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_628);
  }
}
void function_630() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_629), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_629);
  }
}
void function_631() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(32);
  args = cons(val, args);
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_630), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_630);
  }
}
void function_632() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_631), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_631);
  }
}
void function_633() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(59);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_632), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_632);
  }
}
void function_634() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_633), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_633);
  }
}
void function_635() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_634), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_634);
  }
}
void function_636() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(35);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_635), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_635);
  }
}
void function_637() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_636), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_636);
  }
}
void function_638() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_637), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_637);
  }
}
void function_639() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(41);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_638), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_638);
  }
}
void function_640() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_639), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_627), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_627);
  }
}
void function_641() {
  pop_function();
}
void function_642() {
  pop_function();
}
void function_643() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_642);
  }
}
void function_644() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_643), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_643);
  }
}
void function_645() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(10);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_644), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_644);
  }
}
void function_646() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_645), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_645);
  }
}
void function_647() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(13);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_646), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_646);
  }
}
void function_648() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_647), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_647);
  }
}
void function_649() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_648), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_648);
  }
}
void function_650() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_649), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_641);
  }
}
void function_651() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_650), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_640), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_57);
  }
}
void function_652() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_651), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_56);
  }
}
void function_653() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_652), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_652);
  }
}
void function_654() {
  pop_function();
}
void function_655() {
  pop_function();
}
void function_656() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  val = QUOTE;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_655);
  }
}
void function_657() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_656), env);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_654);
  }
}
void function_658() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_657), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_653), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_55);
  }
}
void function_659() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_658), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_54);
  }
}
void function_660() {
  pop_function();
}
void function_661() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_660);
  }
}
void function_662() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_661), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_661);
  }
}
void function_663() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(34);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_662), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_662);
  }
}
void function_664() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_663), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_663);
  }
}
void function_665() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(92);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_664), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_664);
  }
}
void function_666() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_665), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_665);
  }
}
void function_667() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_666), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_666);
  }
}
void function_668() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_667), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_667);
  }
}
void function_669() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_668), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_659), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_659);
  }
}
void function_670() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_669), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_53);
  }
}
void function_671() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_670), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_670);
  }
}
void function_672() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_671), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_52);
  }
}
void function_673() {
  pop_function();
}
void function_674() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_673);
  }
}
void function_675() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_674), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_674);
  }
}
void function_676() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_675), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_675);
  }
}
void function_677() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_676), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_676);
  }
}
void function_678() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_677), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_677);
  }
}
void function_679() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(32);
  args = cons(val, args);
  val = new_number(0);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_678), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_678);
  }
}
void function_680() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_679), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_679);
  }
}
void function_681() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_680), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_680);
  }
}
void function_682() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(35);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_681), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_681);
  }
}
void function_683() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_682), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_682);
  }
}
void function_684() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_683), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_683);
  }
}
void function_685() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(41);
  args = cons(val, args);
  val = new_number(39);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_684), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_684);
  }
}
void function_686() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_685), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_685);
  }
}
void function_687() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_686), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_686);
  }
}
void function_688() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(46);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_687), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_687);
  }
}
void function_689() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_688), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_688);
  }
}
void function_690() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_689), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_689);
  }
}
void function_691() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(59);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_690), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_690);
  }
}
void function_692() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_691), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_691);
  }
}
void function_693() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_692), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_692);
  }
}
void function_694() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_693), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_672), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_672);
  }
}
void function_695() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_694), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_51);
  }
}
void function_696() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_695), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_695);
  }
}
void function_697() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_696), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_50);
  }
}
void function_698() {
  pop_function();
}
void function_699() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_698);
  }
}
void function_700() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_699), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_699);
  }
}
void function_701() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_700), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_700);
  }
}
void function_702() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(57);
  args = cons(val, args);
  val = new_number(48);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_701), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_701);
  }
}
void function_703() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_702), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_702);
  }
}
void function_704() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_703), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_703);
  }
}
void function_705() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_704), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_697), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_697);
  }
}
void function_706() {
  pop_function();
}
void function_707() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_706);
  }
}
void function_708() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_709() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_708), env);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_707), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_707);
  }
}
void function_710() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_709), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_705), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_49);
  }
}
void function_711() {
  pop_function();
}
void function_712() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_711);
  }
}
void function_713() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = cdr(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_714() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_713), env);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_712), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_712);
  }
}
void function_715() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_714), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_710), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_48);
  }
}
void function_716() {
  pop_function();
}
void function_717() {
  pop_function();
}
void function_718() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) < value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(1);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  if (is_number(car(args))) {
    if ((length(args) == 2) &&
        is_number(car(cdr(args)))) {
      val = new_number(value(car(args)) - 
                       value(car(cdr(args))));
    } else if (length(args) == 1) {
      val = new_number(-value(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    if (value(car(args)) > value(car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_717);
  }
}
void function_719() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_718), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_716);
  }
}
void function_720() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_719), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_715), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_47);
  }
}
void function_721() {
  pop_function();
}
void function_722() {
  pop_function();
}
void function_723() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_722);
  }
}
void function_724() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_723), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_721);
  }
}
void function_725() {
  increment_count(env);
  val = new_function((&function_724), env);
  pop_function();
}
void function_726() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_725), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_720), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_46);
  }
}
void function_727() {
  pop_function();
}
void function_728() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_727);
  }
}
void function_729() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_728), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_726), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_45);
  }
}
void function_730() {
  pop_function();
}
void function_731() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_730);
  }
}
void function_732() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_731), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_731);
  }
}
void function_733() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_732), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_729), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_44);
  }
}
void function_734() {
  pop_function();
}
void function_735() {
  pop_function();
}
void function_736() {
  pop_function();
}
void function_737() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  if (length(args) == 2) {
    increment_count(car(args));
    increment_count(car(cdr(args)));
    val = cons(car(args), car(cdr(args)));
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_736);
  }
}
void function_738() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_737), env);
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(env))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_735);
  }
}
void function_739() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_738), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_734);
  }
}
void function_740() {
  increment_count(env);
  val = new_function((&function_739), env);
  pop_function();
}
void function_741() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_740), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_733), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_43);
  }
}
void function_742() {
  pop_function();
}
void function_743() {
  pop_function();
}
void function_744() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_743);
  }
}
void function_745() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_744), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_744);
  }
}
void function_746() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_745), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_742);
  }
}
void function_747() {
  increment_count(env);
  val = new_function((&function_746), env);
  pop_function();
}
void function_748() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_747), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_741), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_42);
  }
}
void function_749() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_748), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_41);
  }
}
void function_750() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_749), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_749);
  }
}
void function_751() {
  pop_function();
}
void function_752() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_751);
  }
}
void function_753() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_752), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_750), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_750);
  }
}
void function_754() {
  pop_function();
}
void function_755() {
  pop_function();
}
void function_756() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_755);
  }
}
void function_757() {
  pop_function();
}
void function_758() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(cdr(env))))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_757);
  }
}
void function_759() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_758), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_756), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_754);
  }
}
void function_760() {
  increment_count(env);
  val = new_function((&function_759), env);
  pop_function();
}
void function_761() {
  increment_count(env);
  val = new_function((&function_760), env);
  pop_function();
}
void function_762() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_761), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_753), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_40);
  }
}
void function_763() {
  increment_count(val = car(car(cdr(env))));
  pop_function();
}
void function_764() {
  increment_count(env);
  val = new_function((&function_763), env);
  pop_function();
}
void function_765() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_764), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_762), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_39);
  }
}
void function_766() {
  pop_function();
}
void function_767() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_768() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_767), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_766);
  }
}
void function_769() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_768), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_765), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_38);
  }
}
void function_770() {
  pop_function();
}
void function_771() {
  pop_function();
}
void function_772() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    increment_count(val = car(cdr(car(env))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_770);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = cdr(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(env)));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_771);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_773() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_772), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_769), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_37);
  }
}
void function_774() {
  pop_function();
}
void function_775() {
  pop_function();
}
void function_776() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = cdr(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(cdr(car(env)))));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = car(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_774);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(cdr(car(env)))));
      args = cons(val, args);
      increment_count(val = car(cdr(car(env))));
      if (is_function(val)) {
        decrement_count(env);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_775);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_777() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_776), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_776);
  }
}
void function_778() {
  increment_count(env);
  val = new_function((&function_777), env);
  pop_function();
}
void function_779() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_778), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_773), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_36);
  }
}
void function_780() {
  pop_function();
}
void function_781() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_780);
  }
}
void function_782() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_781), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_779), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_35);
  }
}
void function_783() {
  pop_function();
}
void function_784() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(cdr(car(env)))));
  args = cons(val, args);
  increment_count(val = car(cdr(car(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_783);
  }
}
void function_785() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_784), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_782), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_34);
  }
}
void function_786() {
  pop_function();
}
void function_787() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = new_number(69);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(114);
  args = val;
  push_args();
  val = new_number(58);
  args = val;
  push_args();
  val = new_number(32);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_786);
  }
}
void function_788() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_787), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_785), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_33);
  }
}
void function_789() {
  push_args();
  val = NIL;
  args = val;
  val = -88;
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_790() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_789), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_788), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_32);
  }
}
void function_791() {
  pop_function();
}
void function_792() {
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  val = -88;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_791);
  }
}
void function_793() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_792), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_792);
  }
}
void function_794() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_793), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_790), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_31);
  }
}
void function_795() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_794), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_30);
  }
}
void function_796() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(48);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (is_number(car(args))) {
    if ((length(args) == 2) &&
        is_number(car(cdr(args)))) {
      val = new_number(value(car(args)) - 
                       value(car(cdr(args))));
    } else if (length(args) == 1) {
      val = new_number(-value(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  val = new_number(10);
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) * value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_797() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_796), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_795), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_795);
  }
}
void function_798() {
  push_args();
  val = NIL;
  args = val;
  val = new_number(116);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    val = new_number(9);
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    val = new_number(114);
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      val = new_number(13);
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      val = new_number(110);
      args = cons(val, args);
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if ((length(args) == 2) &&
          (is_atom(car(args))) &&
          (is_atom(car(cdr(args)))) &&
          eq(car(args), car(cdr(args)))) {
        increment_count(val = true);
      } else { val = NIL; }
      pop_args();
      if (val != NIL) {
        decrement_count(val);
        val = new_number(10);
        pop_function();
      } else {
        increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))))))));
        if (val != NIL) {
          decrement_count(val);
          increment_count(val = car(car(env)));
          pop_function();
        } else {
          /** Warning, unhandled cond-default */ val = NIL;
          pop_function();
        }
      }
    }
  }
}
void function_799() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_798), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_797), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_29);
  }
}
void function_800() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_799), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_28);
  }
}
void function_801() {
  increment_count(val = car(cdr(car(env))));
  pop_function();
}
void function_802() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = CDR;
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_801), env);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  val = CDR;
  args = cons(val, args);
  val = CAR;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_800), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_800);
  }
}
void function_803() {
  pop_function();
}
void function_804() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(13);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  val = new_number(10);
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_803);
  }
}
void function_805() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_804), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_802), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_27);
  }
}
void function_806() {
  if (val != NIL) {
    decrement_count(val);
    val = BNEG;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    val = -71;
    args = cons(val, args);
    if (length(args) == 2) {
      increment_count(car(args));
      increment_count(car(cdr(args)));
      val = cons(car(args), car(cdr(args)));
    } else { val = NIL; }
    pop_args();
    pop_function();
  }
}
void function_807() {
  if (val != NIL) {
    decrement_count(val);
    val = BXOR;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(126);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_806), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_806);
    }
  }
}
void function_808() {
  if (val != NIL) {
    decrement_count(val);
    val = BOR;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(94);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_807), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_807);
    }
  }
}
void function_809() {
  if (val != NIL) {
    decrement_count(val);
    val = BAND;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(124);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_808), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_808);
    }
  }
}
void function_810() {
  if (val != NIL) {
    decrement_count(val);
    val = MOD;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(38);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_809), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_809);
    }
  }
}
void function_811() {
  if (val != NIL) {
    decrement_count(val);
    val = DIV;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(37);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_810), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_810);
    }
  }
}
void function_812() {
  if (val != NIL) {
    decrement_count(val);
    val = MULT;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(47);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_811), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_811);
    }
  }
}
void function_813() {
  if (val != NIL) {
    decrement_count(val);
    val = MINUS;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(42);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_812), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_812);
    }
  }
}
void function_814() {
  if (val != NIL) {
    decrement_count(val);
    val = PLUS;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(45);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_813), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_813);
    }
  }
}
void function_815() {
  if (val != NIL) {
    decrement_count(val);
    val = EQ;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(43);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_814), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_814);
    }
  }
}
void function_816() {
  if (val != NIL) {
    decrement_count(val);
    val = GT;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(61);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_815), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_815);
    }
  }
}
void function_817() {
  if (val != NIL) {
    decrement_count(val);
    val = LT;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(62);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_816), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_816);
    }
  }
}
void function_818() {
  if (val != NIL) {
    decrement_count(val);
    val = LSHIFT;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(60);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_817), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_817);
    }
  }
}
void function_819() {
  if (val != NIL) {
    decrement_count(val);
    val = SRSHIFT;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(60);
    args = val;
    push_args();
    val = new_number(60);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_818), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_818);
    }
  }
}
void function_820() {
  if (val != NIL) {
    decrement_count(val);
    val = URSHIFT;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(62);
    args = val;
    push_args();
    val = new_number(62);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_819), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_819);
    }
  }
}
void function_821() {
  if (val != NIL) {
    decrement_count(val);
    val = FN;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(62);
    args = val;
    push_args();
    val = new_number(62);
    args = val;
    push_args();
    val = new_number(62);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_820), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_820);
    }
  }
}
void function_822() {
  if (val != NIL) {
    decrement_count(val);
    val = LET;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(102);
    args = val;
    push_args();
    val = new_number(110);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_821), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_821);
    }
  }
}
void function_823() {
  if (val != NIL) {
    decrement_count(val);
    val = COND;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(108);
    args = val;
    push_args();
    val = new_number(101);
    args = val;
    push_args();
    val = new_number(116);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_822), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_822);
    }
  }
}
void function_824() {
  if (val != NIL) {
    decrement_count(val);
    val = CONS;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(99);
    args = val;
    push_args();
    val = new_number(111);
    args = val;
    push_args();
    val = new_number(110);
    args = val;
    push_args();
    val = new_number(100);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_823), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_823);
    }
  }
}
void function_825() {
  if (val != NIL) {
    decrement_count(val);
    val = CDR;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(99);
    args = val;
    push_args();
    val = new_number(111);
    args = val;
    push_args();
    val = new_number(110);
    args = val;
    push_args();
    val = new_number(115);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_824), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_824);
    }
  }
}
void function_826() {
  if (val != NIL) {
    decrement_count(val);
    val = CAR;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(99);
    args = val;
    push_args();
    val = new_number(100);
    args = val;
    push_args();
    val = new_number(114);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_825), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_825);
    }
  }
}
void function_827() {
  if (val != NIL) {
    decrement_count(val);
    val = ATOM;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(99);
    args = val;
    push_args();
    val = new_number(97);
    args = val;
    push_args();
    val = new_number(114);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_826), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_826);
    }
  }
}
void function_828() {
  if (val != NIL) {
    decrement_count(val);
    val = FUNCTION;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(97);
    args = val;
    push_args();
    val = new_number(116);
    args = val;
    push_args();
    val = new_number(111);
    args = val;
    push_args();
    val = new_number(109);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_827), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_827);
    }
  }
}
void function_829() {
  if (val != NIL) {
    decrement_count(val);
    val = NUMBER;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(102);
    args = val;
    push_args();
    val = new_number(117);
    args = val;
    push_args();
    val = new_number(110);
    args = val;
    push_args();
    val = new_number(99);
    args = val;
    push_args();
    val = new_number(116);
    args = val;
    push_args();
    val = new_number(105);
    args = val;
    push_args();
    val = new_number(111);
    args = val;
    push_args();
    val = new_number(110);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_828), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_828);
    }
  }
}
void function_830() {
  if (val != NIL) {
    decrement_count(val);
    val = QUOTE;
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = new_number(110);
    args = val;
    push_args();
    val = new_number(117);
    args = val;
    push_args();
    val = new_number(109);
    args = val;
    push_args();
    val = new_number(98);
    args = val;
    push_args();
    val = new_number(101);
    args = val;
    push_args();
    val = new_number(114);
    args = val;
    val = NIL;
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    increment_count(args);
    val = cons(args, val);
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_829), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_829);
    }
  }
}
void function_831() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = new_number(113);
  args = val;
  push_args();
  val = new_number(117);
  args = val;
  push_args();
  val = new_number(111);
  args = val;
  push_args();
  val = new_number(116);
  args = val;
  push_args();
  val = new_number(101);
  args = val;
  val = NIL;
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  increment_count(args);
  val = cons(args, val);
  pop_args();
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_830), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_830);
  }
}
void function_832() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_831), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_805), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_26);
  }
}
void function_833() {
  if (val != NIL) {
    decrement_count(val);
    val = NIL;
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = cdr(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      pop_function();
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_834() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_833), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_833);
  }
}
void function_835() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_834), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_832), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_25);
  }
}
void function_836() {
  if (val != NIL) {
    decrement_count(val);
    val = NIL;
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      pop_function();
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_837() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_836), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_836);
  }
}
void function_838() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_837), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_835), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_24);
  }
}
void function_839() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_840() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_839), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_838), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_23);
  }
}
void function_841() {
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
    pop_function();
  } else {
    /** Warning, unhandled cond-default */ val = NIL;
    pop_function();
  }
}
void function_842() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))))))))));
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      call_stack = cons(new_function((&function_841), env), call_stack);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_841);
    }
  }
}
void function_843() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_842), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_840), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_22);
  }
}
void function_844() {
  push_args();
  val = NIL;
  args = val;
  val = -71;
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_atom(car(args))) &&
      (is_atom(car(cdr(args)))) &&
      eq(car(args), car(cdr(args)))) {
    increment_count(val = true);
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_845() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_844), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_843), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_21);
  }
}
void function_846() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_845), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_20);
  }
}
void function_847() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_846), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_846);
  }
}
void function_848() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_847), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_847);
  }
}
void function_849() {
  pop_function();
}
void function_850() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_849);
  }
}
void function_851() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  val = CONS;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_850), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_850);
  }
}
void function_852() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_851), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_848), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_19);
  }
}
void function_853() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_852), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_18);
  }
}
void function_854() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(env))));
  args = cons(val, args);
  val = new_number(1);
  args = cons(val, args);
  if ((length(args) == 2) &&
      (is_number(car(args))) &&
      (is_number(car(cdr(args))))) {
    val = new_number(value(car(args)) + value(car(cdr(args)))); 
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_855() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_854), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(env))))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_853), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_853);
  }
}
void function_856() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_855), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_17);
  }
}
void function_857() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_856), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_856);
  }
}
void function_858() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = new_number(0);
  args = cons(val, args);
  val = PLUS;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(cdr(env)))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_857), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_857);
  }
}
void function_859() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_858), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_16);
  }
}
void function_860() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_859), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_859);
  }
}
void function_861() {
  increment_count(val = car(car(env)));
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
    if (val != NIL) {
      decrement_count(val);
      increment_count(val = car(cdr(car(env))));
      pop_function();
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_862() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  val = NIL;
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_861), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_860), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_860);
  }
}
void function_863() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_862), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_15);
  }
}
void function_864() {
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_863), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_863);
  }
}
void function_865() {
  increment_count(val = car(car(env)));
  if (val != NIL) {
    decrement_count(val);
    increment_count(val = car(cdr(car(env))));
    pop_function();
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))))));
    if (val != NIL) {
      decrement_count(val);
      val = NIL;
      pop_function();
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_866() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))));
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_865), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_864), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_864);
  }
}
void function_867() {
  pop_function();
}
void function_868() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(env));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_867);
  }
}
void function_869() {
  increment_count(env);
  val = new_function((&function_868), env);
  pop_function();
}
void function_870() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_869), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_866), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_14);
  }
}
void function_871() {
  pop_function();
}
void function_872() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_871);
  }
}
void function_873() {
  if (is_function(val)) {
    call_stack = cons(new_function((&function_872), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_872);
  }
}
void function_874() {
  pop_function();
}
void function_875() {
  args = cons(val, args);
  increment_count(val = car(cdr(car(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_874);
  }
}
void function_876() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_875), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_875);
  }
}
void function_877() {
  increment_count(env);
  val = new_function((&function_876), env);
  pop_function();
}
void function_878() {
  increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(cdr(env))))))))))));
  pop_function();
}
void function_879() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(cdr(car(cdr(env)))));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_878), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_877), env);
  args = cons(val, args);
  val = ATOM;
  args = cons(val, args);
  val = CDR;
  args = cons(val, args);
  val = CAR;
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(cdr(env))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_873), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_873);
  }
}
void function_880() {
  increment_count(env);
  val = new_function((&function_879), env);
  pop_function();
}
void function_881() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_880), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_870), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_13);
  }
}
void function_882() {
  pop_function();
}
void function_883() {
  increment_count(val = car(cdr(car(cdr(env)))));
  pop_function();
}
void function_884() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_883), env);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  args = cons(val, args);
  val = ATOM;
  args = cons(val, args);
  val = CDR;
  args = cons(val, args);
  val = CAR;
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_882);
  }
}
void function_885() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_884), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_881), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_12);
  }
}
void function_886() {
  pop_function();
}
void function_887() {
  pop_function();
}
void function_888() {
  pop_function();
}
void function_889() {
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(cdr(car(cdr(cdr(env))))))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_888);
  }
}
void function_890() {
  args = cons(val, args);
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_889), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_889);
  }
}
void function_891() {
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_890), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_890);
  }
}
void function_892() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    increment_count(val = car(cdr(cdr(cdr(cdr(car(cdr(cdr(env)))))))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_887);
    }
  } else {
    increment_count(val = car(car(cdr(cdr(cdr(cdr(cdr(cdr(env)))))))));
    if (val != NIL) {
      decrement_count(val);
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      increment_count(val = car(cdr(car(cdr(cdr(env))))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_891), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_891);
      }
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_893() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  increment_count(val = car(cdr(cdr(car(cdr(cdr(env)))))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_892), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_892);
  }
}
void function_894() {
  increment_count(env);
  val = new_function((&function_893), env);
  pop_function();
}
void function_895() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_894), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(cdr(env)))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_886);
  }
}
void function_896() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_895), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_885), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_11);
  }
}
void function_897() {
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_896), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_10);
  }
}
void function_898() {
  pop_function();
}
void function_899() {
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = cdr(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if (length(args) == 1) {
      if (! is_atom(car(args))) {
      increment_count(val = cdr(car(args)));
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    args = cons(val, args);
    increment_count(val = car(car(cdr(env))));
    if (is_function(val)) {
      decrement_count(env);
      return_location = address(val);
      increment_count(env = environment(val));
      decrement_count(val);
      increment_count(args);
      env = cons(args, env);
      pop_args();
    } else {
      if (is_primitive(val)) {
        temp = val;
        val = eval_primitive(val, args);
        decrement_count(temp);
      } else {
        decrement_count(val);
        val = NIL;
      }
      pop_args();
      return_location = (&function_898);
    }
  } else {
    /** Warning, unhandled cond-default */ val = NIL;
    pop_function();
  }
}
void function_900() {
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (is_atom(car(args))) {
      increment_count(val = true); 
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  if (val != NIL) {
    decrement_count(val);
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    increment_count(val = car(car(env)));
    args = cons(val, args);
    if ((length(args) == 2) &&
        (is_atom(car(args))) &&
        (is_atom(car(cdr(args)))) &&
        eq(car(args), car(cdr(args)))) {
      increment_count(val = true);
    } else { val = NIL; }
    pop_args();
    pop_function();
  } else {
    push_args();
    val = NIL;
    args = val;
    increment_count(val = car(cdr(car(env))));
    args = cons(val, args);
    if (length(args) == 1) {
      if (is_atom(car(args))) {
        increment_count(val = true); 
      } else { val = NIL; }
    } else { val = NIL; }
    pop_args();
    if (val != NIL) {
      decrement_count(val);
      val = NIL;
      pop_function();
    } else {
      push_args();
      val = NIL;
      args = val;
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(cdr(car(env))));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      push_args();
      val = NIL;
      args = val;
      increment_count(val = car(car(env)));
      args = cons(val, args);
      if (length(args) == 1) {
        if (! is_atom(car(args))) {
        increment_count(val = car(car(args)));
        } else { val = NIL; }
      } else { val = NIL; }
      pop_args();
      args = cons(val, args);
      increment_count(val = car(car(cdr(env))));
      if (is_function(val)) {
        call_stack = cons(new_function((&function_899), env), call_stack);
        return_location = address(val);
        increment_count(env = environment(val));
        decrement_count(val);
        increment_count(args);
        env = cons(args, env);
        pop_args();
      } else {
        if (is_primitive(val)) {
          temp = val;
          val = eval_primitive(val, args);
          decrement_count(temp);
        } else {
          decrement_count(val);
          val = NIL;
        }
        pop_args();
        return_location = (&function_899);
      }
    }
  }
}
void function_901() {
  increment_count(env);
  val = new_function((&function_900), env);
  pop_function();
}
void function_902() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_901), env);
  args = cons(val, args);
  increment_count(val = car(car(env)));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_897), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_897);
  }
}
void function_903() {
  pop_function();
}
void function_904() {
  pop_function();
}
void function_905() {
  pop_function();
}
void function_906() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_905);
  }
}
void function_907() {
  push_args();
  increment_count(val = car(env));
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_906), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_906);
  }
}
void function_908() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_907), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_904);
  }
}
void function_909() {
  pop_function();
}
void function_910() {
  pop_function();
}
void function_911() {
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_910);
  }
}
void function_912() {
  push_args();
  increment_count(val = car(env));
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(cdr(env))));
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    call_stack = cons(new_function((&function_911), env), call_stack);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_911);
  }
}
void function_913() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_912), env);
  args = cons(val, args);
  increment_count(val = car(car(cdr(env))));
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_909);
  }
}
void function_914() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_913), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_908), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_903);
  }
}
void function_915() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_914), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_902), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_9);
  }
}
void function_916() {
  increment_count(val = car(car(env)));
  if (val != NIL) {
    decrement_count(val);
    val = NIL;
    pop_function();
  } else {
    increment_count(val = car(car(cdr(env))));
    if (val != NIL) {
      decrement_count(val);
      increment_count(val = car(car(cdr(env))));
      pop_function();
    } else {
      /** Warning, unhandled cond-default */ val = NIL;
      pop_function();
    }
  }
}
void function_917() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_916), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_915), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_8);
  }
}
void function_918() {
  push_args();
  val = NIL;
  args = val;
  val = -42;
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_917), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_7);
  }
}
void function_919() {
  increment_count(val = car(car(env)));
  pop_function();
}
void function_920() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_919), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_918), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_6);
  }
}
void function_921() {
  increment_count(val = car(env));
  pop_function();
}
void function_922() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_921), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_920), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_5);
  }
}
void function_923() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = cdr(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = cdr(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_924() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_923), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_922), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_4);
  }
}
void function_925() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = cdr(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_926() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_925), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_924), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_3);
  }
}
void function_927() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = cdr(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_928() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_927), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_926), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_2);
  }
}
void function_929() {
  push_args();
  val = NIL;
  args = val;
  push_args();
  val = NIL;
  args = val;
  increment_count(val = car(car(env)));
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  args = cons(val, args);
  if (length(args) == 1) {
    if (! is_atom(car(args))) {
    increment_count(val = car(car(args)));
    } else { val = NIL; }
  } else { val = NIL; }
  pop_args();
  pop_function();
}
void function_930() {
  push_args();
  val = NIL;
  args = val;
  increment_count(env);
  val = new_function((&function_929), env);
  args = cons(val, args);
  increment_count(env);
  val = new_function((&function_928), env);
  if (is_function(val)) {
    decrement_count(env);
    return_location = address(val);
    increment_count(env = environment(val));
    decrement_count(val);
    increment_count(args);
    env = cons(args, env);
    pop_args();
  } else {
    if (is_primitive(val)) {
      temp = val;
      val = eval_primitive(val, args);
      decrement_count(temp);
    } else {
      decrement_count(val);
      val = NIL;
    }
    pop_args();
    return_location = (&function_1);
  }
}
#ifdef BARE_HARDWARE
void main (unsigned long magic, multiboot_data* boot_data) {
  int i = 0;
  if ((magic == BOOT_MAGIC) &&
      getFlag(boot_data->flags, 0)) {
    init_mem((void*)0x200000, ((boot_data->mem_upper*1024) - 0x100000));
    init_io();
    init_eval();
    arguments = get_cmd(boot_data->flags, (char*)(boot_data->cmd_line));
    call_stack = cons(new_function((&top_level), NIL), call_stack);
    call_stack = cons(new_function((&function_0), NIL), call_stack);
    return_location = (&function_930);
    while (return_location != (&end_function)) {
      (*return_location)();
    }
    end_function();
  } else {
    error(ERR_BAD_BOOT);
  }
}
#else
int main(int argc, char** argv) {
  pointer memory_limit = 104857600;
  void* memory = malloc(memory_limit);
  init_mem(memory, memory_limit);
  init_io();
  init_eval();
  call_stack = cons(new_function((&top_level), NIL), call_stack);
  call_stack = cons(new_function((&function_0), NIL), call_stack);
  return_location = (&function_930);
  while (return_location != (&end_function)) {
    (*return_location)();
  }
  end_function();
  end_io();
}
#endif
