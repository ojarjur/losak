;; This program is a Quine; when compiled and run it
;; generates its own source code as output.
(define (quine prolog postscript)
  (append prolog '(34) prolog '(34 10 34) postscript '(34 10) postscript))
(fn args (quine ";; This program is a Quine; when compiled and run it
;; generates its own source code as output.
(define (quine prolog postscript)
  (append prolog '(34) prolog '(34 10 34) postscript '(34 10) postscript))
(fn args (quine "
"))"
))